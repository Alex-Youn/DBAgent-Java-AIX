# DBAgent-Java 커넥션 풀 / Trace 차트 / 비밀번호 암호화 수정 내역 (2026-08-18)

이 프로젝트(`DBAgent-Java`) 쪽 작업 기록입니다. 1~2번 항목은 원본 Python 프로젝트(`C:\AI-PROJECTS\DBAgent`)에도 동일하게 이식했고, 그쪽 기록은 해당 폴더에 별도로 없고 이 문서에 같이 남겨둡니다 (Python 쪽은 `api_server.py`, `web/app.js`, `databases.json`, `oracle.env` 수정).

## 수정한 파일

- `src/main/java/com/dbagent/oracle/TargetDbConfig.java`
- `src/main/java/com/dbagent/oracle/DatabaseConfigService.java`
- `src/main/java/com/dbagent/oracle/OracleConnectionPoolManager.java`
- `src/main/java/com/dbagent/oracle/PasswordCipher.java` (신규)
- `src/main/java/com/dbagent/DbAgentApplication.java`
- `src/main/resources/application.properties`
- `src/main/resources/static/app.js`
- `application.properties`, `databases.json` (프로젝트 루트, 개발용)
- `dist/application.properties`, `dist/databases.json`, `dist/dbagent-java-0.1.0.jar` (배포 산출물)

## 1. 커넥션 풀(HikariCP) 크기를 DB 인스턴스별로 설정 가능하게 변경

**요청 배경**: 커넥션 풀 개수가 `OracleConnectionPoolManager`에 `minimumIdle=2`, `maximumPoolSize=10`으로 하드코딩되어 있어 DB별로 조정이 불가능했음.

**설계**: `databases.json`의 인스턴스에 값이 있으면 그 값을 쓰고, 없으면(`null`) `application.properties`의 전역 기본값을 쓰는 2단계 우선순위.

- `TargetDbConfig`에 `Integer poolMinIdle`, `poolMaxSize` 필드 추가 (nullable = "설정 안 됨")
- `DatabaseConfigService.resolve()`: `databases.json` 인스턴스의 `pool_min_idle`/`pool_max_size`를 `inst.hasNonNull(...)`로 읽음. **주의**: 처음엔 `has()`를 썼다가, JSON에 `"pool_min_idle": null`처럼 필드는 있고 값만 없는 경우 `has()`가 `true`를 반환해서 `asInt()`가 `0`을 반환 → 풀 크기 0으로 깨지는 버그를 발견해 `hasNonNull()`로 수정
- `OracleConnectionPoolManager`: `@Value("${dbagent.oracle.pool.min-idle:2}")` / `max-size:10}` 전역 기본값을 주입받고, `target.poolMinIdle()`/`poolMaxSize()`가 `null`이 아니면 그 값을 우선 사용
- `databases.json`의 6개 인스턴스 전부에 `pool_min_idle`/`pool_max_size: null` 필드를 미리 추가해둠 (필요한 인스턴스만 숫자로 바꾸면 바로 적용됨)

**검증**: 실제 로컬 Oracle 19c(`patent_integ_1`)에 커스텀 `pool_min_idle=3, pool_max_size=7`을 주고 풀 생성 → `pool.opened=3`으로 정확히 반영, 재호출 시 풀 재사용 확인. `/api/pool/test`로 실접속 성공까지 확인.

## 2. Trace(세션 Duration scatter) 차트가 세션 카운트 라인 차트보다 한 박자 늦게 나타나는 문제 수정

**증상**: Current Session 탭에서 세션 카운트 라인 차트("실시간 세션 추이")는 즉시 뜨는데, Trace(sec) scatter 차트는 다음 폴링(기본 5초 뒤)에야 나타남.

**원인**: `app.js`의 `fetchSessions()`에서 Trace 차트를 생성하는 코드 전체가 세션 라인 차트의 `if (!sessionChart) {...} else {...}` 중 **`else` 블록 안에만** 있었음. 즉 `sessionChart`가 아직 없는 첫 폴링(페이지 로드/DB 전환 직후)에는 `if` 절만 실행되어 Trace 차트 코드 자체가 실행되지 않고, 두 번째 폴링부터 `else` 절로 들어가야 비로소 처음 그려짐.

**수정**: Trace 차트 생성/갱신 로직을 `sessionChart`의 `if/else` 블록 밖으로 독립시켜, 매 폴링마다 자기 자신의 존재 여부(`!sessionScatterChart`)만으로 판단하도록 분리. 이제 첫 폴링부터 두 차트가 동시에 나타남.

**검증**: 이 환경(샌드박스)에서 헤드리스 브라우저 렌더링이 아예 동작하지 않아(Edge headless로 example.com조차 렌더 실패) 실제 화면 캡처는 못 했음. 대신 문자열/템플릿리터럴/주석을 구분하는 중괄호 균형 검사 스크립트를 직접 작성해서 수정한 `fetchSessions()` 함수만 떼어 검사 → 완전히 균형 확인. 이후 사용자가 직접 반영 확인함.

## 3. 대시보드 DB 전환 시 "DB에 연결할 수 없습니다" 메시지가 잠깐 뜨는 문제 수정

**증상**: 로그인 후(또는 DB 클릭 시) 대시보드의 세션/이벤트 위젯에 "DB에 연결할 수 없습니다."가 잠깐 떴다가 실제 데이터로 바뀜. 실제 접속 실패가 아닌데 에러 문구가 보임.

**원인**: `app.js`에서 DB를 선택하면 `fetchDashboard()`가 데이터를 받아오기 *전에* `resetAllDashboardWidgets()`가 먼저 호출되어 세션/이벤트 테이블을 에러 문구로 초기화함. 로딩 중 상태에 에러 문구를 재사용한 것이 원인.

**수정**: `resetSessionData`/`resetEvents`에 메시지 파라미터를 추가. DB 선택 직후의 로딩 상태(`resetAllDashboardWidgets()`)에서는 "접속 중..."을 표시하고, 실제 조회 실패(기존 6곳의 호출부)는 그대로 "DB에 연결할 수 없습니다."를 유지하도록 분리.

## 4. 서버 포트 변경 가능 여부 확인 (코드 수정 없음)

**질문**: `application.properties`로 서버 포트(현재 8005)를 바꿀 수 있는가?

**답변**: 이미 가능함. `server.port=8005`가 Spring Boot 표준 설정으로 `src/main/resources/application.properties`에 있어서, jar 옆의 외부 `application.properties`에 `server.port=원하는포트`만 추가하면 재빌드 없이 적용됨. 단, `dist/start.ps1`의 `$port = 8005`는 시작 스크립트가 "포트 열릴 때까지 대기 + 브라우저 자동 오픈"에 쓰는 별도 하드코딩 값이라, 포트를 바꾸면 이 스크립트 값도 같이 맞춰야 함 (이번엔 실제로 바꾸지 않음, 확인만 함).

## 5. `databases.json` / `oracle.env` 비밀번호 base64 인코딩으로 평문 노출 방지

**배경**: `databases.json`에 Oracle 접속 비밀번호가 평문으로 저장되어 있어 암호화 가능 여부 문의.

**1차 시도 (AES-256-GCM, 롤백됨)**: 키를 OS 환경변수(`DBAGENT_ENC_KEY`)로만 관리하는 진짜 암호화 방식으로 구현 → 실제 Oracle 19c로 접속까지 검증 성공. 하지만 키 생성/서버별 환경변수 등록/키 분실 시 복구 불가 등 운영 부담이 크다는 사용자 피드백을 받아, "평문만 아니면 된다"는 요구 수준에 맞춰 더 단순한 방식으로 전면 교체함. AES 관련 코드(`PasswordCipher` 클래스, `DbAgentApplication`의 `--enc-tool` 분기)는 전부 삭제하고 실제 파일도 원래 평문으로 되돌린 뒤 아래 방식으로 다시 전환.

**최종 방식 (base64 인코딩, 키 없음)**:
- `DatabaseConfigService.resolvePassword()`: 값이 `B64(...)`로 감싸져 있으면 base64 디코딩, 아니면 평문 그대로 통과 — 기존 평문 비밀번호와 100% 호환
- **주의**: base64는 암호화가 아니라 인코딩이라 키가 없고 누구나 즉시 원문 복원 가능. "파일을 얼핏 봤을 때 비밀번호가 그대로 보이지 않게"만 하는 용도이며, 파일 접근 권한이 있는 사람 기준으로는 평문과 동일한 수준의 보호임 — 사용자가 이 트레이드오프를 명시적으로 확인하고 선택함
- 키 관리, 환경변수, CLI 도구 전부 불필요 — PowerShell `[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("비밀번호"))` 한 줄로 누구나 `B64(...)` 값을 직접 만들 수 있음
- 루트 `databases.json`(6개 인스턴스) + `oracle.env`(SYS), `dist/databases.json`(6개 인스턴스) + `dist/oracle.env`(SYS) 전부 평문 → `B64(...)`로 교체 완료

**검증**: `dist/databases.json`, `dist/oracle.env`(실전환된 파일)를 그대로 가리키는 서버를 기동 → `patent_integ_1`(databases.json, dbagent_user)과 `default`(oracle.env, SYS) 양쪽 다 `/api/pool/test`로 실제 Oracle 19c 접속 성공 확인. `/api/config` 응답에 비밀번호(인코딩값 포함)가 노출되지 않는 것도 확인.

**폐쇄망 반영 시 절차**: 최신 `databases.json`, `oracle.env`, `dbagent-java-0.1.0.jar`만 그대로 이관하면 됨 — 환경변수나 별도 키 설정 절차 없음 (AES 방식과 달리 서버마다 추가로 해줘야 할 작업이 없어짐).

## 6. `dist/` 배포 패키지 갱신

`dist/dbagent-java-0.1.0.jar`을 위 1~3, 5번 변경사항이 모두 반영된 최신 빌드로 교체 (jar 안의 `app.js`/클래스 파일 내용을 압축 해제해서 직접 대조 확인). `dist/databases.json`에도 `pool_min_idle`/`pool_max_size: null` 필드 반영, `dist/application.properties`에 `dbagent.oracle.pool.min-idle`/`max-size` 기본값(2/10) 추가.
