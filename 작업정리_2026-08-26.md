# 2026-08-26 작업 정리

`DBAgent-Java`(Java 17/Spring Boot 3.3.5, 원본)에서 같은 날 진행된 기능들을 이 프로젝트(Java 8/Spring Boot 2.7 이관본)에 포팅한 내역. 원본 쪽 배경/설계 논의는 `DBAgent-Java\세션상세팝업_CurrentSession개선_대시보드상태바_작업정리_2026-08-26.md` 참고 — 이 문서는 AIX 이관본에서 실제로 어떻게 옮겼는지, 어디가 원본과 다른지에 집중.

> **⚠️ 작업 원칙 (2026-08-26부로 확정)**: 앞으로 이 프로젝트의 수정 작업은 **`DBAgent-Java`와 `DBAgent-Java-AIX`를 항상 같이 진행**한다. 한쪽만 고치고 끝내지 말 것 — 매번 두 프로젝트 다 반영(Java 8 문법 치환 등 이 프로젝트 쪽 재작성 포함)하고, 재빌드/`dist`·`dist-aix` 배포까지 마친 뒤 작업을 완료로 본다.

---

## 1. 세션 상세정보에 HASH_VALUE 추가

`MonitorService.getSessionQuery()`가 `v$sql` 조회 시 `sql_fulltext`와 함께 `hash_value`도 같이 가져와 응답에 포함하도록 수정. 신규 팝업 페이지(`session-detail.html`, 아래 2번)에 표시.

## 2. 세션 상세정보를 별도 브라우저 창(팝업)으로 표시

- 신규 파일 `session-detail.html` — 독립 문서라 `sessionStorage`에서 토큰을 직접 읽어(`window.open`으로 띄운 팝업은 같은 오리진이면 열 당시의 sessionStorage를 그대로 물려받음) `/api/session_query`를 스스로 호출. SQL_ID/HASH_VALUE/SQL·Plan·Bind 탭 + "튜닝" 버튼 렌더링, 저장된 테마(`localStorage`)도 반영
- `app.js`의 `.clickable-session-row` 전역 delegate가 in-page fetch/렌더링 대신 `window.open()`으로 이 팝업을 띄우도록 변경. 같은 SID 재클릭 시 새 창 대신 기존 창 포커스
- OS 창이라 다른 모니터로 자유롭게 옮길 수 있음(브라우저 탭 안에서만 움직이는 드래그 패널 방식은 검토 후 폐기)

## 3. Current Session 화면 개선

- **좌측 그래프**: 기존 3계열(ACTIVE/INACTIVE/ACTIVE TRANSACTION, INACTIVE는 죽은 라인이었음) → **Active Transaction**(초록 `#10b981`) / **Parallel Session**(올리브 `#808000`) / **2pc Pending Transaction**(파랑 `#3b82f6`) / **Lock Wait**(자주 `#9333ea`) 4계열로 교체
- **신규 백엔드**: `GET /api/session_extra` (`MonitorController`/`MonitorService.getSessionExtra()`) — `v$transaction`⋈`v$session`, `v$px_session`⋈`v$session`, `dba_2pc_pending`, `v$lock` 4개 쿼리를 한 커넥션에서 조회, 각각 개별 try/catch로 감싸서 권한 없는 계정이어도 해당 탭만 비고 나머지는 정상 동작
- **"Active 세션목록" → 4탭 메뉴**: Active Session / Active Transaction / Parallel Session / 2pc Pending Transaction (2pc 제외 나머지는 `.clickable-session-row`라 클릭 시 세션 상세 팝업으로 연결)
- **리셋**: `resetSessionMonitor()` 신설 — 좌측 라인차트+우측 Trace 산점도 둘 다 `destroy()` 후 히스토리 초기화, DB 전환 시/메뉴 재진입 시 항상 호출
- **X축/포인트 모양** (같은 날 후속 요청으로 추가 조정):
  - 우측 Trace 산점도 포인트 모양 `star`(★) → `crossRot`(Chart.js에서 X자 모양)
  - 좌우 그래프 X축 범위를 `CHART_WINDOW_MS = 20 * 10 * 60 * 1000`(10분×20틱=200분)으로 통일, 좌측 차트 라벨 가로 고정(`maxRotation/minRotation: 0`), 우측 산점도는 `type:'linear'` 수동 포맷팅 방식에서 좌측과 동일한 `type:'time'` 방식으로 통일
  - **⚠️ Chart.js v4 `stepSize` 버그**: 처음에 `time: { unit:'minute', stepSize:10 }`로 넣었더니 10분 간격이 전혀 안 먹히고 6분처럼 애매하게 나옴 → 번들 `lib/chart.js`(v4.5.1) 소스를 직접 grep해서 확인한 결과 TimeScale이 `stepSize`를 **`ticks.stepSize`에서만 읽고 `time.stepSize`는 무시**하는 게 원인. `ticks.stepSize`로 옮겨서 해결. (앞으로 Chart.js v4 시간축에 고정 간격 강제할 때는 `time.unit` + `ticks.stepSize`를 서로 다른 옵션 객체에 나눠 넣어야 함)

## 4. 대시보드 상단 상태바에 Max Session / Max Process 추가

- **Max Session/Process**: `v$parameter`의 `sessions`/`processes` **설정값**(실시간 사용량 아님)
- **Active/Inactive**: `v$session.status`, `type != 'BACKGROUND'` 기준 현재 세션 수
- **Dedicate/Dispatcher**: `v$session.server = 'DEDICATED'`/`'SHARED'` 기준 현재 세션 수 — **Dispatcher = shared server로 접속한 세션 개수**로 확정(dispatcher 프로세스 개수 아님)
- `MonitorService.getHealth()`에 6개 값 추가 조회(각각 개별 try/catch, 인스턴스/리스너 판정에 영향 없음), `index.html` 상태바에 `Max Session -- EA [Active -- EA / Inactive -- EA]` / `Max Process -- EA [Dedicate -- EA / Dispatcher -- EA]` 추가, 조회 실패 시 `0`이 아니라 `--` 표시

## 5. 화면 하단 여백 버그 — **해당 없음**

원본은 `.app-container`에 `zoom: 90%`가 적용되어 있어서 `height: 100vh`와 안 맞물려 하단에 빈 공간이 생기는 버그가 있었음. AIX는 애초에 이 90% 줌 기능 자체가 이관 범위 밖(이전 세션에 확인됨)이라 이 버그도 없고 수정도 불필요.

## 6. "SQL 정합성/튜닝" 메뉴 신규 이관 (모델 없이 성능점검만)

처음엔 AIX에 이 메뉴 자체가 없어서 세션 상세 팝업의 "튜닝" 버튼을 뺐었는데, "모델 없이 DBA가 성능점검만 할 수 있는 기능은 필요하다"는 요청에 따라 메뉴를 통째로 이관:

- `com.dbagent.query.ExecutionPlanService` 신규 이식 — `.strip()`/`.stripLeading()`/`.stripTrailing()`(Java 11+)을 `Strings.strip()`/`Strings.stripTrailing()`로 치환(그 외엔 record/`var` 등 없어서 기계적 이식)
- `SqlTuningRequest`/`SqlTuningAutoRequest`/`BindCaptureRequest`(원본은 record) → 기존 AIX 패턴대로 일반 클래스 + `@JsonCreator`로 재작성
- `SqlTuningController` 신규 작성 — **모델이 필요한 `analyze`/`analyze_from_query`/`analyze_from_query_actual` 3개는 항상 "이 환경에는 SQL 튜닝 sLLM 서버가 연동되어 있지 않습니다" 메시지만 반환하도록 스텁 처리**. WSL/GPU가 필요한 원본 `SqlTuningService`(`java.net.http.HttpClient` 사용, Java 11+)는 아예 이식하지 않음 — 호출할 서버가 없으니 이식할 필요 자체가 없어짐(Java 8 HttpClient 이슈도 자연히 회피)
- `quick_check`(1차 성능점검)/`bind_capture`(V$SQL_BIND_CAPTURE 조회)는 모델이 필요 없는 순수 Oracle 조회라 원본과 100% 동일하게 완전히 동작
- 프런트엔드(`index.html`/`app.js`)는 원본 코드 그대로 재사용 — 모델 버튼도 특별한 분기 없이 서버가 돌려주는 스텁 메시지를 표시할 뿐이라 프런트 수정 불필요(버튼/설명 문구에 "AI 모델 필요"만 표기 추가)
- 세션 상세 팝업의 "튜닝" 버튼 + `window.openSqlTuningFromPopup`도 이제 정상 동작 — 세션 상세 → 튜닝 메뉴로 점프해서 1차 성능점검 이어가기 가능
- 이번 요청으로 두 버튼("실제 실행 통계로 분석", "1차 성능점검")의 `confirm()` 확인 팝업도 같은 날 후속 요청으로 제거함(바로 실행되도록 변경)

## 7. 대시보드 "장애발생 가능성" 라벨 포맷 변경

`(TM Lock: --건) (TX Lock: --건)` → `[TM Lock -- EA / TX Lock -- EA]`로 변경 (`index.html` placeholder + `app.js` 실제 값 대입 템플릿 둘 다).

---

## 트러블슈팅: `git checkout -- target/`로 인한 정적 리소스 배포 누락 (⚠️ 중요, 재발 방지)

### 증상
로그인 화면이 CSS 없이 그냥 위에서 아래로 요소가 쌓여서 보임(username/password/큰 로고 이미지가 상단, 그 아래 대시보드 미리보기 텍스트).

### 원인
이 프로젝트는 `target/` 폴더 자체가 git에 커밋되어 있는 특이한 구조. 재빌드 후 "정리 차원"에서 `git checkout -- target/`을 반복 실행했는데, 이게 `target/classes/static/style.css` 등을 예전 커밋 시점 내용으로 되돌리면서 mtime도 "방금"으로 갱신시킴 → 이후 소스를 다시 안 건드리면 Maven이 "target이 소스보다 최신"이라고 판단해 **리소스 재복사를 스킵**, 낡은 정적 리소스가 계속 jar에 실림. 실제로 그 jar를 로컬에 띄워서 `curl`로 서빙되는 `style.css`를 받아보니 로그인 리디자인 CSS(262줄)가 통째로 빠져 있었음.

### 수정 및 재발 방지
- `git checkout -- target/` 사용 중단
- 재빌드는 항상 **`mvn clean package`**로 전환
- 재빌드 후 배포 전 확인 절차: `wc -l target/classes/static/{app.js,index.html,style.css}`를 소스 파일과 비교해서 줄 수가 정확히 일치하는지 확인

### 부수 발견: 디스크 공간 100% 소진
같은 시점에 C: 드라이브가 238GB 중 0바이트 남을 정도로 꽉 찼던 것도 발견. `dist`/`dist-aix`에 쌓인 `.bak`~`.bak5` 백업 파일들(세션 중 재배포마다 자동 생성)이 일부 원인이었으나, 238GB 전체가 이 프로젝트와 무관하게 꽉 찬 건 별개의 더 큰 문제 — 사용자가 직접 불필요 파일 정리해서 해결. 이후 재배포부터는 디스크 여유가 넉넉하지 않은 동안 `.bak` 백업 생략하고 진행.

---

## 캐시버스터

세션 시작 시점 `app.js?v=32`, `style.css?v=13`에서 시작해 이번 세션 동안 여러 번 수정하면서 매번 올림 → 최종 `app.js?v=45`, `style.css?v=15`.

한 차례 "로그인 화면이 안 바뀐다"는 문제가 실제로는 위 `target/` 캐시 오염 버그였는데, 처음엔 이것도 단순 브라우저 캐시(버전 미갱신) 문제인 줄 알고 버전만 올렸다가 재현되어 추가로 파고들어 진짜 원인을 찾은 것 — **앞으로 화면이 안 바뀌는 것처럼 보이면 브라우저 캐시(버전 갱신)와 `target/` 빌드 캐시(`mvn clean` 여부/소스-타겟 줄 수 비교) 둘 다 확인할 것.**

---

## 8. 이후 추가 반영 (같은 날, `DBAgent-Java` 후속 요청 포팅분)

- **confirm() 팝업 제거**: "실제 실행 통계로 분석"/"1차 성능점검" 버튼의 확인창 제거, 바로 실행되도록 변경
- **차트 X축/포인트 모양**: 우측 Trace 산점도 포인트 `star`→`crossRot`(X자 모양), 좌우 그래프 X축을 `CHART_WINDOW_MS = 20*10*60*1000`(10분×20틱)으로 통일, 좌측 차트 라벨 가로 고정. **`stepSize`는 `time.*`이 아니라 `ticks.*`에 넣어야 함**(Chart.js v4 TimeScale이 `ticks.stepSize`만 읽음 — `DBAgent-Java`에서 먼저 발견된 버그, 자세한 내용은 원본 문서 Part 7 참고)
- **대시보드 "장애발생 가능성" 라벨**: `(TM Lock: --건) (TX Lock: --건)` → `[TM Lock -- EA / TX Lock -- EA]`
- **`ExecutionPlanService.explainActual()` SELECT/WITH 판정 버그 수정**: 쿼리 위에 `-- 설명` 같은 주석 줄이 있으면 진짜 SELECT인데도 거부되던 문제 → 판정 시에만 앞쪽 주석(`--...`, `/*...*/`)을 걷어내고 검사하도록 `stripLeadingComments()` 신설(실제 실행되는 쿼리 텍스트 자체는 주석 그대로 유지)
- **바인드 변수 입력칸 가로 배치**: 6개 초과 시 `flex-direction: column`(세로 한 줄에 하나씩)으로 바뀌던 걸 제거, 항상 `flex-wrap: wrap`(가로로 나열, 넘치면 다음 줄)로 통일
- **90% 줌 + 하단 여백**: AIX는 원래 이 90% 줌 기능 자체가 없었는데(로컬보다 화면이 커 보인다는 피드백으로 발견), `.app-container`에 `style="zoom: 90%;"` 추가 + `height: calc(100vh / 0.9)`로 보정, `.content-area` 하단에 `1cm` 여유 공간도 추가(로컬과 동일)
- 캐시버스터 최종: `app.js` → **v46**, `style.css` → **v16**

---

## 배포

- 모든 변경사항 `mvn clean package`로 재빌드 → 소스와 `target/classes/static/*` 줄 수 일치 확인 → `dist-aix\dbagent-java-0.1.0.jar`, `dist\dbagent-java-0.1.0.jar` 양쪽에 재배포(빌드 결과물과 byte 단위 동일한 것까지 확인)
- `databases.json`(`session_thresholds` 필드, 이전 세션 작업분)도 `dist-aix`/`dist`에 동기화 완료
- **재기동 아직 안 함** — AIX 서버에서 `stop-aix.sh` → `start-aix.sh` 필요. 재기동 후 브라우저 강력 새로고침(Ctrl+Shift+R) 권장(캐시버스터를 올렸지만 만약을 위해)

---

## 다음에 할 작업 (아직 미착수, 아이디어 단계)

### `databases.json` DB 목록을 화면에서 직접 관리 (추가/삭제)
- **배경**: 지금은 `databases.json` 파일을 직접 편집해야 모니터링 대상 DB를 추가/삭제할 수 있음. 계정 관리 화면처럼 관리자 전용 환경설정 UI로 만들 수 있는지 질문 받음 → 가능하다고 답변, 실제 구현은 아직 안 함
- **구현 방향**: 계정 관리 모달과 동일한 패턴(관리자 전용 모달 + `POST/PUT/DELETE` API + JSON 파일 갱신)로 DB 추가/수정/삭제 UI 구성
- **⚠️ 반드시 같이 처리해야 할 것**: `DatabaseConfigService`가 `databases.json`을 앱 **기동 시 딱 한 번만** `@PostConstruct`로 읽어 메모리(`JsonNode config`)에 캐싱하는 구조라, 파일만 갱신해서는 재시작 전까지 반영 안 됨. **파일 쓰기 + 메모리 캐시 재로드(reload)를 함께 구현**해야 재시작 없이 즉시 반영됨(동시 수정 시 스레드 안전성도 고려 필요)
- 비밀번호는 기존 관례대로 `B64(...)` 형태(실제 암호화 아님, 평문 감추기 수준)로 저장 — UI 쪽 입력/저장 방식도 이 관례에 맞출 것
- **`DBAgent-Java`/`DBAgent-Java-AIX` 둘 다 적용 대상** (위 작업 원칙 참고)

### 로그인 후 첫 화면을 "Fleet Overview"(전체 DB 현황판)로 재설계
- **설계 문서**: `DBAgent-Java\db_모니터링_첫화면_설계안_1.md`, 목업 HTML `db_fleet_overview_mockup.html`(현재 `C:\Users\윤인수\Downloads\`에 있음)
- **배경/목적**: 로그인 직후 특정 DB 상세 대시보드로 바로 들어가는 지금 구조 대신, "관리 중인 여러 DB 상태를 한눈에 스캔하고 문제 있는 DB로 바로 진입"하는 요약 화면(Fleet Overview 패턴)을 앞단에 추가. 기존 개별 DB 상세 모니터링(Active Session/Transaction, Lock Wait, Parallel Session, 2PC Pending 시계열 차트) 화면은 그대로 유지, 카드/행 클릭 시 그 화면으로 드릴다운
- **화면 구성 요약**: 상단 요약 바(전체/정상/경고/위험 개수) · DB 목록(카드↔테이블 토글, 위험도순 고정 정렬) · 검색+필터(DB명/환경/상태) · 우측 "주의 필요 Top 5" 고정 순위 패널(CPU 기준, 검색/필터 무관하게 항상 전체 기준) · 목록 박스는 콘텐츠 양만큼만 차지
- **색상**: 고정 다크 테마, 배경 `#0f172a`/카드 `#1e293b` 등 네이비 계열. **상태색(정상 `#0ca30c`/경고 `#fab219`/위험 `#d03b3b`)은 DB 상태 전용**으로 고정하고 상세 화면 지표 구분색과 겹치지 않게 유지, 상태는 색+텍스트 라벨 병행. WCAG 대비(텍스트 4.5:1/그래픽 3:1) 검증 포함
- **⚠️ 검토 필요 사항**: 정상/경고/위험 판정 임계치 기준(기존 `session_thresholds`/CPU·메모리 임계치 재사용 여부), 전체 DB 동시 폴링으로 인한 커넥션 풀 부하, 다크 전용 vs 기존 라이트/다크 토글과의 정합성
- 자세한 화면 구성/색상표는 `DBAgent-Java`의 동일 날짜 문서 참고 — **`DBAgent-Java`/`DBAgent-Java-AIX` 둘 다 적용 대상** (위 작업 원칙 참고)
