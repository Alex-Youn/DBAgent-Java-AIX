# 작업정리 2026-09-21 (야간)

> 대상: `DBAgent-Java` / `DBAgent-Java-AIX` 양쪽
> 상태: 전부 완료 + 커밋/푸시 완료 (main `2a379d3`, AIX `a286740`)
>
> 같은 날 낮에 회사에서 진행한 4~5단계 내용은 `작업정리_2026-09-21.md`에 별도로 있다.
> 이 문서는 그 결과를 개발 PC로 pull 받은 뒤 이어서 진행한 야간 작업 기록이다.

---

## 1. Pull과 그 과정에서 겪은 것

낮 작업분(`oracle.env` 제거 4~5단계)을 양쪽 저장소로 pull.

- main `ac425f9→2d48b22`, AIX `004de06→b1fa9e2`
- **skip-worktree가 걸려 있어도 pull이 막혔다**: 원격에서 파일 자체가 삭제되는 커밋(`oracle.env`, `databases.json`)은 skip-worktree로 보호되지 않는다. 백업 → `--no-skip-worktree` → `checkout` → pull → 재적용 순서로 처리.
- pull 결과 이 PC의 로컬 Docker Oracle 8개가 **host 빈값이라 접속 불가** 상태가 됐다(5단계 alias 경로 삭제의 예고된 영향). 접속하려면 관리 화면에서 host를 채워야 한다.

## 2. 오늘자 변경분 정밀 점검 → 레이스 컨디션 발견·수정

낮에 고친 "v2 대시보드 DB 전환 안 됨" 수정(`iv2FetchInFlightForDbId`)을 다시 점검하다가 **남아 있던 레이스**를 찾았다.

- **증상이 될 수 있는 구조**: in-flight 가드가 "마지막으로 시작한 db_id 하나"만 기억하는 단일 변수였다. A→B→A로 빠르게 되돌아가면 A의 첫 라운드가 안 끝난 상태에서 두 번째 A 라운드가 시작되고, 먼저 끝난 첫 라운드의 `finally`가 "지금 값이 A니까 내 것"이라고 착각해 **진행 중인 두 번째 라운드의 in-flight 상태를 지워버린다.** 그 틈에 10초 폴링이 겹쳐 쌓이면 원래 고치려던 hang 버그와 같은 클래스가 재현된다.
- **수정**: db_id별 `Set`으로 독립 추적(`iv2FetchInFlightDbIds`). main/AIX 동일 반영.

## 3. 커넥션 풀 튜닝

"환경설정 DB 리스트 불러오는 게 가끔 느리다"는 질문에서 출발.

- **원인**: `resolve()`가 거의 모든 API 진입부에서 호출되는데 `dbconfig` 풀이 3이었다. v2 대시보드는 폴링 한 번에 API 6개를 동시에 쏘므로 그 시점에 바로 바닥난다. 등록된 DB **개수**(테이블 행 수)는 인덱스 단건 조회라 거의 무관하고, **동시 열람 탭/사용자 수**가 실제 병목이다.
- **조치**: users/metrics/dbconfig 세 풀을 전부 10으로 통일 + SQLite `busy_timeout=5000` / H2 `LOCK_TIMEOUT 5000` 추가.
- **함정 하나**: `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000`처럼 세미콜론으로 이어 붙이면 **sqlite-jdbc가 두 번째 문장을 조용히 무시한다**(실측: busy_timeout이 5000이 아니라 드라이버 기본값 3000으로 남음). busy_timeout은 URL 쿼리 파라미터로 줘야 한다.

## 4. DB 파일을 `db_config/` 하위로 분리

`users`/`metrics`/`dbconfig` 세 DB 파일이 앱 폴더 바로 밑에 흩어져 있던 것을 `db_config/` 서브디렉터리로 모았다. **5곳 전부**(main root/dist, AIX root/dist/dist-aix) 동일 패턴.

- 소스 변경 없이 각 위치의 외부 `application.properties`에서 URL만 바꿔 처리
- 4개 로컬 인스턴스 실제 기동해서 새 위치에 파일이 생기고 `/api/config`가 정상 응답하는 것까지 확인
- `.gitignore`도 `/db_config/` 디렉터리 단위로 갱신

> **반복해서 걸린 함정**: 외부 `application.properties`가 url을 **통째로** 덮어쓰기 때문에, jar 기본값 url에 붙인 쿼리 파라미터(`busy_timeout`, `DEFRAG_ALWAYS`)가 조용히 사라진다. url 옵션을 추가할 땐 packaged + 외부 override 파일 **전부**에 같이 적어야 한다. 오늘 이 함정에 두 번 걸렸다.

## 5. 6단계 — 대상 DB 비밀번호 AES 암호화 (설계 → 구현 → 검증 → 배포)

### 설계

design-advisor 에이전트로 설계 진행. 산출물은 `설계문서/비밀번호_암호화_설계_2026-09-21.md`.

**가장 중요한 선행 사실**: 2026-08-18에 AES-256-GCM을 **이미 한 번 구현했다가 롤백한 이력**이 있다(`수정내역/커넥션풀_암호화_수정내역_2026-08-18.md` 56~64행). 롤백 사유는 "AES가 과하다"가 아니라 **키 생성 / 서버별 환경변수 등록 / 키 분실 시 복구 불가**라는 운영 부담이었다. 이번 설계는 그 3가지를 없애는 것을 전제로 삼았다:

| 2026-08 롤백 사유 | 이번 대응 |
|---|---|
| 키 생성 CLI 실행 필요 | 최초 기동 시 **자동 생성**(운영자 조작 0회) |
| 서버별 환경변수 등록 | 환경변수는 **선택적 오버라이드**, 키 파일이 기본 |
| 키 분실 = 복구 불가 | 4-1단계로 관리 화면 CRUD가 생겨서, **비밀번호 재입력으로 복구** 가능 |

키 관리는 **환경별 독립 키**로 확정(오케스트레이터 결정). `dbconfig.db`만 다른 서버로 복사하면 복호화 안 되는 게 의도된 동작이고, 이 프로젝트는 지금도 DB 파일을 환경 간 복사하지 않으므로 실질 비용이 없다.

### 구현

- `com.dbagent.security.MasterKeyProvider`: 키 파일 로드/자동 생성. **AES-256을 허용하지 않는 JVM이면 128비트로 자동 폴백** — 폐쇄망 IBM J9는 무제한 정책 파일이 없을 수 있고 현장에서 받을 수도 없어, 기동이 막히는 쪽을 피했다. 키를 못 읽거나 길이가 안 맞으면 fail-fast.
- `com.dbagent.security.CredentialCipher`: `ENC(v1:<keyId>:<base64(IV 12B ‖ 암호문 ‖ 태그 16B)>)`. 버전/키ID를 포맷에 넣어 로테이션·혼재·롤백을 판별할 수 있게 했고, 레거시 `B64(...)`와 평문은 그대로 읽어준다.
- `DatabaseConfigService`: `resolvePassword`/`encodePassword`의 **시그니처를 유지한 채 본문만 위임**으로 교체 → 호출부 5곳 무변경. `accounts` JSON 안의 추가 계정 비밀번호도 같이 암호화.
- 기동 시 `reencryptAllPasswords()`가 **매번 멱등 스캔**. 1회성 플래그를 피한 이유는 롤백·키 로테이션·부분 실패에서 자기치유가 안 되기 때문.

### 구현 중 발견한, 설계에 없던 문제 2건

**(1) 재암호화해도 파일에 예전 B64 값이 남는다**

암호화를 마친 뒤 DB 파일을 직접 열어보니 예전 `B64(...)` 값이 그대로 검출됐다. B64는 키 없이 디코딩되므로, 이걸 안 지우면 "파일이 유출돼도 안전하다"는 암호화의 목적 자체가 무너진다. 저장 엔진이 UPDATE 시 옛 페이지/청크를 파일에 남기기 때문.

| | main (SQLite) | AIX (H2) |
|---|---|---|
| 방법 | `wal_checkpoint(TRUNCATE)` + `VACUUM` | url에 `DEFRAG_ALWAYS=TRUE` |
| 시점 | **기동 중 즉시** | JVM **정상 종료 시** |
| 실측 | 23건 → 0건 | 6건 → 0건 |

H2도 즉시 처리하려고 `SHUTDOWN COMPACT`을 넣어봤으나 **DB가 닫혀서 이어지는 질의가 전부 깨지고 앱이 아예 기동 실패**했다(되돌림, 코드 주석에 "다시 시도하지 말 것"으로 남김). 그래서 H2는 종료 시점 정리로 갔고, **AIX는 업그레이드 후 한 번 정상 종료해야 예전 값이 지워진다**(`stop-aix.sh`는 SIGTERM이라 훅이 돌지만, Windows `stop.ps1`은 강제 종료라 안 돈다).

**(2) 마이그레이션 중 손상된 값이 기동을 죽이는 회귀** — 커밋 직전 점검에서 발견

평문이 DB 파일에 한 번도 닿지 않게 하려고 `migrateFromJsonFile()`에 즉시 암호화를 넣었는데, **try/catch가 없어서** `databases.json`에 깨진 `B64(...)`가 하나라도 있으면 `@PostConstruct`가 터져 **앱이 아예 기동 못 하는** 상태였다(그 전에는 원본을 그대로 넣기만 해서 예외가 날 일이 없었으니, 오늘 새로 만든 회귀다).

수정 후 격리 환경에서 재현·검증: 깨진 값이 섞여 있어도 **기동 성공(1.9~2.1초)**, 3개 인스턴스 **전부 이관**, 문제 인스턴스 ID만 ERROR 로그, 재암호화 카운트도 `실패 1건`으로 정확.

### 검증 (main/AIX 양쪽)

| 항목 | 결과 |
|---|---|
| 키 자동 생성 | 256bit, 운영자 조작 0회 |
| 기존 13건 재암호화 | 변경 13건 / 실패 0건 |
| 저장 형태 | `ENC(v1:k1:...)`, `accounts` 안 추가 계정도 암호화 |
| 독립 복호화 | **앱과 별개로 작성한 코드**가 키 파일만으로 13건 전부 복호화, 평문 길이/첫 글자가 알려진 도커 비번과 일치 |
| 실제 DB 접속 | 복호화된 값으로 도커 MySQL 접속 → 세션 목록 조회 성공 |
| 재기동 멱등성 | 2회차 기동에서 재암호화 0건 |
| 관리 화면 CRUD | 신규 등록 시 암호화, **빈 비밀번호=기존 유지** 계약 유지, 삭제 정상 |
| 파일 잔재 | main 0건(즉시), AIX는 정상 종료 후 0건 |
| Java 8 호환 | AIX는 `--release 8`이라 Java 9+ API가 컴파일 단계에서 차단됨(바이트코드 major 52 확인) |

## 6. git 정리

- `dist/application.properties`를 포함해 **skip-worktree를 전부 해제**했다. `oracle.env`/TNS 의존이 사라져 환경별로 값이 달라야 할 이유가 없어졌기 때문.
- 이 과정에서 `src/main/resources/application.properties`(jar 내장 기본값)도 skip-worktree였다는 걸 발견 — **오늘 한 풀 크기/busy_timeout 작업이 git에 전혀 안 보이고 있었다.** 그대로 뒀으면 통째로 커밋 누락될 뻔했다.
- 마스터 키(`dbagent.key`, `*.key`)는 `.gitignore`로 커밋 차단.

---

## 배포

main `dist`, AIX `dist`, AIX `dist-aix` **3곳 전부** 최종 빌드로 교체. 로컬 4개 인스턴스는 기동·검증 후 전부 정지 상태.

> `dist-aix/`는 `.gitignore`에 통째로 제외돼 있어 거기 넣은 설정(`db_config` 경로, `DEFRAG_ALWAYS`)은 **커밋에 안 들어간다.** 물리적으로 옮기는 폴더라 의도된 동작이지만, 폐쇄망 반입 시 그 폴더를 직접 가져가야 한다.

## 백업 / 롤백 세트

- `backup_before_aes_20260921-2240/` — 암호화 전 DB 파일(root/dist/dist-aix) + 수정 대상 소스·설정
- `dist/dbagent-java-0.1.0.jar.bak-2026092*` — 롤백용 구 jar
- **롤백은 "구 jar + 백업 DB 파일"이 한 세트.** 재암호화가 끝난 DB를 구 jar로 되돌리면 `ENC(...)`를 평문으로 오인해 인증 실패한다.

## 남은 것 (폐쇄망에서만 가능)

- 폐쇄망 IBM J9의 `Cipher.getMaxAllowedKeyLength("AES")` 실측 (개발기는 256)
- 폐쇄망 Oracle 인스턴스 실접속 회귀 확인
- 키 파일을 앱 디렉터리 밖으로 이동 + `dbagent.security.key-file` 지정 + 사본 1부 보관(운영 전환 시)
- 폐쇄망 실서버의 잔존 `oracle.env`/`databases.json` 삭제(4~5단계부터 남아 있던 항목)
