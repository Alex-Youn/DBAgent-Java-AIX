# 계정관리/권한 기능 추가 + AIX 이관 작업 정리 (2026-08-21)

오늘 세션은 두 갈래로 진행됨:
1. **`DBAgent-Java`** — 계정관리/메뉴·DB 접근 권한 기능 신규 추가 (완료)
2. **`DBAgent-Java-AIX`**(`DBAgent-Java`의 AIX 이관용 복사본) — Java 8/Spring Boot 2.7 다운그레이드 1단계 (완료), SQLite→H2 마이그레이션 등 후속 단계는 미착수

---

## Part 1. 계정관리 / 메뉴·DB 접근 권한 기능 (`DBAgent-Java`, 완료)

### 배경
기존엔 `admin` 계정 하나만 하드코딩되어 있었음. "일반 계정 생성 가능한가?"라는 질문에서 출발해서, role 기반 권한 + 계정별 메뉴 노출 + 계정별 DB 접근 제한까지 확장됨.

### 수정/신규 파일
**백엔드**
- `src/main/java/com/dbagent/auth/AuthService.java` — `users` 테이블에 `role`/`hidden_menus`/`hidden_dbs` 컬럼 추가(SQLite `ALTER TABLE ... ADD COLUMN`, 부팅 시 자동 마이그레이션), `AuthSession` 레코드, `canAccessDb()`, `createUser`/`updateUser`/`deleteUser`/`listUsers`
- `src/main/java/com/dbagent/auth/UserController.java`(신규) — `POST/GET/PUT/DELETE /api/users`
- `src/main/java/com/dbagent/auth/CreateUserRequest.java`, `UpdateUserRequest.java`(신규)
- `src/main/java/com/dbagent/auth/AuthController.java` — login/check-auth 응답에 `role`/`hidden_menus`/`hidden_dbs` 포함
- `src/main/java/com/dbagent/monitor/MonitorController.java` — `db_id` 받는 API 전부(tmlock/session/tablespace/dashboard/top_events/relation/session_query/table_info/failure_prob/history_sessions/health/history_top_sessions/db_users)에 `canAccessDb()` 체크 추가, `kill_session`은 기존 admin 체크 유지
- `src/main/java/com/dbagent/monitor/KillSessionRequest.java` — `token` 필드 추가
- `src/main/java/com/dbagent/query/SqlQueryController.java`, `SqlQueryRequest.java` — `execute`/`accounts`에 권한 체크 추가

**프런트엔드** (`index.html`, `app.js`, `style.css`)
- 우측 상단 톱니바퀴 → 드롭다운("계정 관리" / "메뉴 표시 설정")
- 계정 관리 모달: 목록 화면 ↔ 수정 화면 전환 구조 (인라인으로 안 붙임)
- 계정 생성/수정 시 "관리자(Admin) 권한 부여" 체크박스 1개 + "표시할 메뉴"/"접속 대상 DB" 체크박스 그리드 — 둘 다 화면에 이미 그려진 `.nav-item`/사이드바 DB 목록을 코드가 그대로 읽어서 만드는 방식이라, 나중에 메뉴나 DB가 추가돼도 코드 수정 없이 자동 반영됨
- `.menu-checkbox-grid` CSS 클래스 신설 (2열 그리드, `max-content` 컬럼폭 + `white-space: nowrap`으로 긴 라벨 줄바꿈 방지) — 처음엔 `.login-form-group input` 규칙이 체크박스에도 적용돼서 큰 사각형으로 깨졌던 버그를 수정하면서 도입
- 일반 계정은 SQL 실행/세션 Kill/장애조치/톱니바퀴가 `admin-only` 클래스로 숨겨지고, 서버단에서도 동일하게 막힘(브라우저 devtools로 직접 호출해도 403)

### 핵심 설계 원칙
- **확장 가능한 체크리스트**: 메뉴 목록도 DB 목록도 하드코딩이 아니라 DOM/사이드바에서 직접 읽어옴 → `index.html`에 메뉴 추가되거나 `databases.json`에 DB 추가되면 체크박스 목록에 자동으로 나타남
- **UI 숨김 ≠ 보안**: 메뉴/사이드바에서 숨기는 건 실수 방지용이고, 실제 권한 강제는 전부 서버단(`isAdmin()`, `canAccessDb()`)에서 함
- **admin 계정 보호**: `admin` 계정 자체는 삭제/역할변경 불가

### 검증 결과 (curl 기반, 이번 세션에서 전부 통과 확인)
- 일반 계정 생성 시 role/메뉴/DB 제한 지정 → 로그인 응답에 반영 확인
- 제한된 DB로 API 직접 호출 → 403 확인, 허용된 DB는 200
- 토큰 없이 호출 → 403 (부가효과로 이 API들이 원래 로그인 여부도 검증 안 하던 상태였는데 이번에 같이 막힘)
- admin 계정 삭제/역할변경 시도 → 거부 확인
- RAC처럼 한 그룹에 인스턴스가 여럿이고 일부만 허용한 경우도 그룹은 보이고 허용 안 한 인스턴스만 숨겨지는지 확인 (그룹 전체 숨김은 "그룹 내 전 인스턴스가 제한된 경우"만)

### 빌드/배포
- `.\build.ps1 -Dist`로 `dist\`까지 동기화 완료 (실행 정책 오류 시 `powershell -ExecutionPolicy Bypass -File .\build.ps1 -Dist` 또는 `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`)

> 참고: 이 Part 1 작업은 `DBAgent-Java`에서 이루어졌고, `DBAgent-Java-AIX`는 그 이후 `DBAgent-Java`를 복사해서 만든 것이라 이 내용을 이미 포함한 상태로 시작함. 즉 `DBAgent-Java-AIX`엔 Part 1 + Part 2가 전부 적용되어 있고, `DBAgent-Java`(원본)엔 Part 1만 있고 Part 2(Java 8 다운그레이드)는 적용 안 됨(Java 17/Spring Boot 3.3.5 그대로).

---

## Part 2. AIX 서버 이관 (`DBAgent-Java-AIX`, 1단계 완료)

전체 배경/결정 근거는 같은 폴더의 `AIX-DEPLOYMENT-REVIEW.md`에 있음 — 다음에 이어갈 때 그 파일부터 읽을 것.

### 확정된 결정
- **Java 8 + Spring Boot 2.7로 확정** (Java 17/Semeru 아님)
- 이유: AIX 서버가 정부 관리 센터 소속이라 OS 레벨 설치(XL C++ 업그레이드)를 임의로 못 함. 실측한 XL C++ 버전(`16.1.0.3`)이 Semeru 17 요구치(`16.1.0.10`)에는 못 미치지만, Java 8 요구치(`13.1.0.1`)는 이미 충족 → Java 8만이 센터에 아무것도 요청할 필요 없는 유일한 경로

### 1단계: Java 8/Spring Boot 2.7 언어 다운그레이드 — 완료
- **범위**: `AIX-DEPLOYMENT-REVIEW.md`가 원래 잡았던 것보다 컸음. 전수 조사 결과 `String.isBlank()`(22곳)/`strip()`/`stripTrailing()`(Java 11+)과 **`java.util.HexFormat`(Java 17+, 토큰 생성에 쓰고 있었음)**이 누락되어 있었음 — 이것들 없이는 Java 8 컴파일 자체가 안 됨
- `com.dbagent.util.{Maps, Lists, Strings}` 유틸 3개 신설해서 `Map.of`/`List.of`/`isBlank`/`strip`/`stripTrailing`/`HexFormat` 전부 드롭인 교체
- record 10개 → 일반 클래스 (접근자 메서드명 `token()` 스타일 그대로 유지해서 호출부 무변경)
- `jakarta.*` → `javax.*`, `var`/`instanceof` 패턴매칭 제거
- `ojdbc11` → `ojdbc8`, `pom.xml` parent `spring-boot-starter-parent` `2.7.18`
- Ollama 챗봇 기능 전체 제거 (`OllamaChatService.java`/`ChatRequest.java` 삭제, `/api/aidba/chat` 라우트 제거, 프런트 챗봇 탭 제거, 부수적으로 죽은 코드가 된 `ErrorSearchService`의 RAG 검색 메서드도 정리)
- **검증**: `<maven.compiler.release>8</maven.compiler.release>`로 JDK17 컴파일러가 Java 8 API 표면 기준 엄격 검사하도록 강제 → 컴파일 성공, 산출 클래스 파일 메이저 버전 **52(Java 8)** 확인. 로컬(`jdk17\bin\java`, 상위 호환 실행)로 기동 후 로그인/계정관리/SQL실행/세션Kill/에러검색 API 스모크 테스트 전부 통과, `/api/aidba/chat`은 404 확인

### 다음에 이어서 할 일 (2단계 당시 기준)
아래 목록은 2단계 완료 직후 시점 기록. **1번은 같은 날 후속 세션에서 완료** — 최신 진행 상황은 Part 3 참고.
1. ~~SQLite → H2 마이그레이션~~ → **완료 (Part 3-1)**
2. `start.sh`/`stop.sh` 작성 → **완료 (Part 3-2, `start-aix.sh`/`stop-aix.sh`라는 이름으로)**
3. `oracle.env`의 `ORACLE_HOME`을 AIX 서버 실제 경로로 수정 → **방향 전환, Part 3-4 참고** (tns_admin은 오히려 `ORACLE_HOME` 안 건드리는 쪽으로 정리됨)
4. AIX용 **IBM SDK, Java Technology Edition 8** tar.gz 확보 → **방법 확정, 아직 미실행. Part 3-3 참고**
5. (선택) `dbagent.oracle.tns-admin` 등 배포 환경별 `application.properties` 오버라이드 준비 → Part 3-4에서 진행 방향 확정

---

## Part 3. AIX 이관 후속 세션 (같은 날, 2026-08-21)

### 3-1. SQLite → H2 마이그레이션 — 완료
- `pom.xml`: `sqlite-jdbc` 제거, `com.h2database:h2:1.4.200` 추가 (마이그레이션 작업 중에는 sqlite-jdbc를 임시로 같이 넣어뒀다가, 완료 후 제거)
- `src/main/resources/application.properties`: `spring.datasource.url=jdbc:sqlite:users.db` → `jdbc:h2:file:./users` (+ `driver-class-name=org.h2.Driver`, `username=sa`), `aidba.errors-db-path=data/oracle_errors.db` → `data/oracle_errors`(확장자 없는 H2 base path)
- `AuthService.init()`: `PRAGMA table_info(users)` 존재 체크 로직 제거, H2 네이티브 `ALTER TABLE users ADD COLUMN IF NOT EXISTS ...`로 단순화
- `ErrorSearchService.java`: `jdbc:sqlite:` → `jdbc:h2:file:`, 파일 존재 체크를 `<path>.mv.db` 규칙에 맞게 조정
- **데이터 이전**: 임시 마이그레이션 도구(`SqliteToH2Migrator`, 실행 후 삭제)로 실제 데이터 복사
  - `users.db` (2 rows: `admin`, 커스텀 계정 `apdba` — role/hidden_menus/hidden_dbs 권한 설정 포함) → `users.mv.db`. 원래 "새로 시작해도 무방"이라 적었었지만 실제로 열어보니 `apdba` 계정에 실사용 권한 설정이 있어서 그대로 이전함
  - `data/oracle_errors.db` (6,864 rows, 한글 텍스트) → `data/oracle_errors.mv.db`, 원문과 바이트 단위 일치 확인
  - `dist/`의 사본도 동일 처리: `dist/data/oracle_errors.db`는 root와 완전히 동일한 파일이라 마이그레이션된 H2 파일을 그대로 복사, `dist/users.db`는 role 컬럼도 없는 옛날 스키마(커스텀 계정 없음)라 새로 부트스트랩되게 둠
  - 기존 SQLite 원본 파일은 삭제하지 않고 `*.sqlite.bak`로 root/dist 양쪽에 보관
- **검증**: `mvn package` 성공, 클래스 파일 major version 52(Java 8) 유지 확인. root/`dist` 양쪽 인스턴스 실제 기동 후 로그인(`admin`), 계정 목록(권한 포함), 에러코드 검색(`ORA-01555`, `ORA-00060`) API 전부 정상 응답 확인. `build.ps1 -Dist`로 `dist\` jar도 재동기화 완료

### 3-2. AIX 기동/종료 스크립트 — 작성 완료, AIX 실기 검증은 아직
- `start-aix.sh` / `stop-aix.sh` 신규 (프로젝트 루트, LF 줄바꿈, 실행권한 부여됨) — `start.ps1`/`stop.ps1`과 대칭 설계, ksh/bash 양쪽에서 돌아가도록 POSIX 문법만 사용(`-E`/bash 전용 문법 회피)
- Windows 버전과 다르게 바꾼 부분:
  - JDK 경로: `jdk17\bin\javaw.exe` → `jdk8/bin/java` (프로젝트 폴더에 동봉 예정, 아직 미배치)
  - **자동 빌드 로직 제거**: AIX엔 Maven/JDK17이 없으므로, jar 없으면 "Windows에서 빌드해서 복사하라"는 에러만 출력
  - 브라우저 자동 오픈 로직 제거 (헤드리스 서버라 URL만 출력)
  - 포트 대기 확인: `Test-NetConnection` 대신 `netstat -an | grep LISTEN` 폴링
  - 프로세스 종료: PID 파일 우선, 없으면 `ps -ef | grep <jar명>`으로 fallback (AIX엔 포트→PID 역추적할 안전한 방법이 없음 — `rmsock`은 소켓을 실제로 닫아버리는 파괴적 방법이라 회피). 그리고 Windows `Stop-Process -Force`(강제 종료) 대신 평범한 `kill`(SIGTERM) 사용 — Spring Boot shutdown hook이 HikariCP 풀을 정상 종료하도록
- 로컬에 ksh가 없어 `bash -n`으로 문법 체크만 통과시킴. **실제 AIX ksh에서의 최종 검증은 서버에 올린 뒤 필요**

### 3-3. JDK8 확보 방법 — 방향 확정, 아직 미실행
- IBM 사이트에서 "IBM SDK, Java Technology Edition 8" tar.gz를 새로 받으려 했으나, **8버전은 애초에 포터블 tar.gz 배포가 없고 AIX installp fileset 형태로만 배포됨** (Semeru 11+ 부터만 tar.gz)
- 마침 `AIX-DEPLOYMENT-REVIEW.md`에 기록된 실측 결과, 대상 AIX 서버에 이미 설치돼 있음: `Java8_64.jre` / `Java8_64.sdk` (8.0.0.605, COMMITTED)
- **결론**: 새로 받을 필요 없이, 이미 설치된 걸 그대로 tar로 묶어서 옮기면 됨. "시스템 Java와 격리해서 프로젝트 폴더에 독립 동봉"이라는 기존 설계 원칙과도 부합
  ```bash
  lslpp -f Java8_64.sdk | grep '^/' | head -5   # 실제 설치 경로 확인 (보통 /usr/java8_64로 추정)
  cd /usr && tar -cvf java8_64.tar java8_64 && gzip java8_64.tar
  ```
  옮긴 뒤 `<옮긴경로>/jdk8/bin/java -version`으로 정상 동작 확인 필요
- 참고로 `/goracle/app/oracle/product/19.3.0/jdk/`(Oracle 19c 클라이언트 번들)에도 동일 계열 IBM J9 Java 8이 있어서 급하게 연결 테스트만 할 땐 그걸 `JAVA_HOME`으로 임시로 써도 되지만(`start-aix.sh`에 fallback 로직 있음), 실제 배포는 위 `Java8_64.sdk` 방식으로 — Oracle 클라이언트 패치/버전업 시 통보 없이 사라지거나 바뀔 수 있고, 라이선스 소관도 이 앱과 무관한 남의 설치물이라 리스크가 있음
- **아직 실행 전** — 사용자가 AIX 서버에서 위 명령 실행하고 tar.gz 파일 확보하면, 그다음 압축 해제/배치는 이어서 진행 예정

### 3-4. `oracle.env` / TNS_ADMIN 정리 방향 확정
- `oracle.env`는 두 가지 역할: (1) `ORACLE_HOME` → `TnsAdminInitializer`가 `dbagent.oracle.tns-admin`이 비어있을 때만 `{ORACLE_HOME}/network/admin`을 tnsnames.ora 위치로 유도, (2) `SID`/`USER`/`PASSWORD`/`HOST`/`PORT` → `databases.json`에 없는 `db_id` 요청 시 쓰이는 fallback 기본 연결값 (현재는 로컬 Docker 테스트 DB 값)
- **결론: `ORACLE_HOME` 경로를 굳이 안 알아내도 됨.** `application.properties`에 `dbagent.oracle.tns-admin=<AIX상의 임의 경로>`를 직접 지정하고 그 경로에 `tnsnames.ora`만 두면 됨 — dev용 root `application.properties`가 이미 이 방식(`dbagent.oracle.tns-admin=C:/oracle_docker_data`)을 쓰고 있어서 검증된 패턴
- `oracle.env`의 `ORACLE_HOME` 줄은 (tns_admin을 직접 지정하는 한) 이제 안 쓰여도 무방. 다만 `SID`/`USER`/`PASSWORD`/`HOST`/`PORT` fallback은 tns_admin 설정과 별개 경로라 여전히 유효 — 실제 운영에서 이 fallback이 쓰일 일이 있는지에 따라 정리 필요 (있으면 AIX 쪽 실제 기본 DB 값으로, 없으면 그대로 두거나 비워도 무방)
- **아직 미실행** — AIX 서버에 `tnsnames.ora` 둘 경로 정하고, 해당 파일 준비되면 `dbagent.oracle.tns-admin` 값 세팅 예정

### 다음에 이어서 할 일 (최신, 순서대로)
1. AIX 서버에서 `Java8_64.sdk` tar로 묶어 확보 → `jdk8\`에 배치 → `jdk8/bin/java -version`으로 동작 확인
2. `start-aix.sh`/`stop-aix.sh`를 실제 AIX 서버에 올려서 최종 동작 검증 (로컬은 ksh 자체가 없어 문법 체크만 함)
3. AIX용 `application.properties`에 `dbagent.oracle.tns-admin` 설정 + 해당 경로에 `tnsnames.ora` 배치
4. `oracle.env`의 `SID`/`USER`/`PASSWORD`/`HOST`/`PORT` fallback 값 실제 운영 기준으로 정리 (필요 여부부터 판단)
5. jar + `data/oracle_errors.mv.db` + `users.mv.db` + `databases.json`/`oracle.env`/`application.properties`/`jdk8/`/`start-aix.sh`/`stop-aix.sh`를 AIX 서버로 전체 이관, 첫 기동 스모크 테스트

### 참고
- `DBAgent-Java-AIX`는 (직전 메모엔 "git 저장소 아님"이라 적었으나) **현재는 git 저장소 상태**(`master` 브랜치, `Initial commit` 커밋 존재) — 이전 메모 작성 이후 누군가 `git init` 했거나 메모가 최신 상태를 반영 못 하고 있었던 것으로 보임. 문제 생기면 이 커밋 기준으로 비교/복구 가능
- 로컬엔 Java 8 JDK가 따로 없음 — 컴파일 검증은 `maven.compiler.release=8`로, 실행 검증은 `jdk17`로 대신함 (상위 JVM은 하위 타깃 바이트코드를 항상 실행 가능). 실제 AIX Java 8 바이너리 자체의 최종 확인은 서버에서만 가능
