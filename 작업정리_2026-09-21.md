# 작업정리 2026-09-21

> 대상: `DBAgent-Java`/`DBAgent-Java-AIX` (oracle.env 제거 마이그레이션 4~5단계, 양쪽 동기화)
> 상태: 4단계(oracle.env 제거)·4-1(databases.json → DB 저장소 이전)·5단계(alias 경로 완전 삭제) 전부 완료. 개발 PC + 폐쇄망(AIX) 양쪽에서 실측 검증까지 마침. 폐쇄망에서 발견된 버그 2건(v2 대시보드 hang, DB 전환 안 됨) 추가로 수정.

---

## 1. 4단계 — oracle.env 제거

- `DatabaseConfigService.resolve(dbId)`/`resolve(dbId, account)` — blank dbId가 이제 oracle.env fallback 대신 `null` 반환. `listAccounts()`도 동일.
- `fallback`/`oracleEnvPath`/`loadOracleEnvFallback()` 전부 제거.
- `app.js`의 `switchTab()`으로 탭 전환 시 DB 미선택 상태에서 자동 발사되던 tablespace/tmlock/session 3곳에 `if (!window.currentDbId)` 가드 추가 — "DB를 먼저 선택해주세요" 메시지로 변경.
- `oracle.env` 파일 삭제 — main/AIX 루트·dist·dist-aix 전부.
- main/AIX 양쪽 컴파일·빌드·회귀테스트 완료.

## 2. 4-1 — `databases.json` → DB 저장소 이전

파일 기반 설정 저장을 완전히 걷어내고 전용 내장 DB로 이전.

- **저장 방식**: `users.db`에 얹지 않고 `metrics.db`와 같은 선례로 전용 파일 분리 — main: `dbconfig.db`(SQLite), AIX: `dbconfig.mv.db`(H2). `DataSourceConfig`에 `dbConfigDataSource`/`dbConfigJdbcTemplate` 빈 추가.
- `DatabaseConfigService`를 JSON 파일 I/O → `JdbcTemplate` 기반 CRUD로 전면 재작성. `db_instances` 테이블: id/group_name/group_order/instance_order/name/db_type/host/port/sid/db_user/password/connect_mode/pool_min_idle/pool_max_size/session_thresholds(JSON)/accounts(JSON). `user` 컬럼은 H2 예약어 충돌 우려로 `db_user`로 명명.
- **1회성 자동 마이그레이션**: `db_config_meta.migrated_from_json` 플래그로 최초 기동 시 1회만 `databases.json`을 읽어 이관, 이후 재확인 안 함 — 폐쇄망에서 원하는 인스턴스만 남기고 트리밍한 뒤 첫 기동시키면 그만큼만 이관되는 방식으로 활용.
- **API 요청/응답 형식 100% 동일 유지** — 컨트롤러/프런트 코드 변경 없이 내부 저장소만 교체.
- AIX 빌드 중 H2 1.4.200이 `TEXT`(CLOB)를 PRIMARY KEY로 못 쓰는 이슈 발견 → `VARCHAR`로 전환. SQLite `INSERT OR IGNORE`/`INSERT OR REPLACE` 대신 AIX는 `INSERT...WHERE NOT EXISTS`/`MERGE INTO...KEY(...)` 사용(H2엔 해당 SQLite 방언이 없음).
- **빈 테이블 안전성 실측**: `dbconfig.db`/`databases.json` 둘 다 없는 상태로 기동 → `/api/config`가 `{"groups":[]}` 정상 반환, `err.log` 완전히 비어있음 확인.
- 개발 PC(13개 인스턴스, 인터넷망 데이터)와 실제 폐쇄망 참고 파일(`C:\AI-PROJECTS\Conf_file\databases.json`, 11개 인스턴스) 양쪽으로 마이그레이션 드라이런 — 그룹핑·session_thresholds·accounts까지 원본과 정확히 일치 확인.
- 6개 RDB 엔진(Oracle/MySQL/MariaDB/PostgreSQL/MSSQL/CUBRID) 전부 개별 접속 회귀 확인 완료.
- 폐쇄망 실환경(오케스트레이터 직접 테스트): dbconfig.db 전체 삭제 후 수동 등록, databases.json 기반 최초 구축 후 개별 추가/삭제 — 둘 다 정상 확인.

## 3. 5단계 — alias 경로 완전 삭제 (최종 정리)

- `buildDsn()`의 legacy alias 분기(`legacyDsn()`, host 빈값→SID를 TNS alias로 취급) 완전 삭제. connect_mode 미지정 시 `host:port:sid`로 고정.
- `TnsAdminInitializer` 클래스 삭제, `dbagent.oracle.tns-admin` 프로퍼티 삭제(관련 있던 모든 `application.properties`/`.sample`에서).
- **host 필수화**: 기동 시 `errorOnMissingOracleHost()`로 `db_type='oracle' AND host 빈값`인 행을 ERROR 로그로 남김(기동은 안 막음) + `createInstance`/`updateInstance`에서 신규 등록/수정 자체를 거부하도록 강화.
- `db-mgmt.html`에 Host "(필수)" 라벨 + `connect_mode` 드롭다운(기본/Service Name/전체 디스크립터) + `expected_instance_name` 입력 필드 추가.
- **`expected_instance_name` + `v$instance` 대조 검증**: `PoolTestController`가 Oracle이고 이 필드가 설정된 경우 `v$instance.instance_name`과 대조, 불일치 시 409로 실패 처리. 실측: 틀린 값 → 409, 올바른 값 → 200 확인.
- **향후 검토로 남긴 것**: `descriptor` connect_mode에서도 Host 필수 검증이 걸림(실사용 안 되는데도) — 오케스트레이터 지시로 지금은 안 고침.
- **6단계(암호화)로 미룬 것**: expected_instance_name 검증이 실제로는 `/api/pool/test`에만 있고 화면에 호출 버튼이 없어 "죽은 기능"이었다는 점 발견 — 나중에 검토하기로 함(수동 테스트 버튼 추가 또는 최초 접속 시 자동 체크).

## 4. code-inspector 점검 결과

오늘 수정 범위 전체(양쪽 프로젝트)를 점검. 실질적 문제 1건 발견 및 직접 수정:

- **발견**: 신규 `dbconfig.db`(main, SQLite)에 WAL 모드 누락 — `users.db`/`metrics.db`는 09-18 사고 이후 WAL 켜져 있는데 오늘 추가한 `dbconfig.db`만 빠짐. 풀 크기 3(>1)이라 관리자가 DB 추가/수정 중일 때 다른 커넥션의 `resolve()`가 "database is locked"로 실패할 실질적 위험.
- **조치**: `dbagent.dbconfig.datasource.hikari.connection-init-sql=PRAGMA journal_mode=WAL` 추가. AIX(H2)는 이 문제 자체가 없어 그대로 둠.
- 그 외(malformed JSON 처리, SQL 바인딩, 마이그레이션 1회성 실행, main↔AIX 포팅 일치, host 필수 검증의 부작용, PoolTestController 커넥션 정리 순서) 전부 문제 없음 확인.

## 5. 폐쇄망 테스트 중 발견된 버그 2건 수정

- **v2 대시보드(신규 대시보드) hang → 전체 먹통**: `fetchInstanceDashboardV2()`가 이전 라운드(6개 API) 완료 여부와 무관하게 `setInterval`로 10초마다 무조건 새로 쏘는 구조 — 느리거나 멈춘 인스턴스가 있으면 라운드가 계속 겹쳐 쌓이며 브라우저 탭이 먹통이 됨(서버는 정상, 클라이언트만 죽는 것으로 확인 — "브라우저 재시작하면 정상"이 결정적 단서). Current Session에서 09-14에 이미 겪었던 것과 같은 유형의 폴링 오버랩 버그. `Promise.all` + in-flight 가드로 수정.
- **가드 회귀 — DB 전환이 안 됨**: 위 수정의 in-flight 가드를 boolean 하나로 잡았더니, 느린/멈춘 DB의 라운드가 안 끝나면 "다른" DB로 전환해도 막혀버리는 회귀 발생. db_id별로 구분해서 추적하도록 재수정(`iv2FetchInFlightForDbId`) — 같은 DB의 중복 라운드만 막고 다른 DB로의 전환은 항상 즉시 통과.
- **Active Session DB 이름 표시(선택 기능)**: 전환이 실제로 반영됐는지 화면에서 바로 확인할 수 있도록 Active Session 목록 제목 옆에 현재 db_id 표시 추가. (SID로 바꿔달라는 요청이 있었으나 재검토 후 원래 dbId 표시로 원복 — 이 세션 안에서 최종 결정.)
- **캐시 버스터 미갱신**: 오늘 app.js를 수십 차례 수정하면서 `index.html`의 `app.js?v=` 캐시 버스터를 한 번도 안 올린 것을 뒤늦게 발견 — 그동안의 수정이 브라우저에 실제로 반영 안 됐을 가능성. main 156→157, AIX 144→145로 갱신.

---

## 빌드/배포

main/AIX 양쪽 `mvn clean package` 반복 빌드, 정적 리소스(app.js/index.html/style.css) 라인 수 일치 확인. **3곳 배포**(main `dist`, AIX `dist`, AIX `dist-aix`) 전 과정에서 지속 반영 — 마지막 배포는 캐시 버스터 갱신 + DB 전환 회귀 수정 포함 버전.

## 남은 것

- 6단계(비밀번호 실제 암호화) — 미착수, 설계 검토만 체크리스트에 정리됨.
- `expected_instance_name` 검증을 실제로 트리거할 UI(수동 테스트 버튼 또는 자동 체크) — 향후 검토.
- `descriptor` connect_mode의 Host 필수 검증 예외 처리 — 향후 검토.
- 폐쇄망에서 오늘자 최종 버전(캐시 버스터 갱신분) 재테스트 및 피드백 대기 중.
- 폐쇄망 실서버의 `oracle.env`/`databases.json` 삭제는 오케스트레이터가 직접 진행 필요 (개발 PC 쪽은 전부 삭제 완료).
