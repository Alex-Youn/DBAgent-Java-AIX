# 작업정리 2026-09-19

> 대상: `DBAgent-Java`/`DBAgent-Java-AIX` (oracle.env 제거 마이그레이션 1단계, 양쪽 동기화)
> 상태: 1단계(코드 변경, 동작 무변화) 구현 · 로컬 실측 검증 · 커밋+push까지 완료.
> 2단계(폐쇄망 8개 실인스턴스 접속 전환)는 오케스트레이터가 폐쇄망에서 진행 예정 — 결과 피드백 대기.

---

## 1. 체크리스트 1단계 항목이 실제로는 미구현 상태였음을 발견

`설계문서/oracle_env_제거_체크리스트.md`(main)의 1단계(코드 변경) 항목들이 전부 `[x]`로 표시돼
있었지만, 코드(`grep`)와 `git log`를 대조한 결과 `connect_mode` 분기, DSN INFO 로그, id/(host,port,
sid) 중복 WARN 로그, `PoolTestController`(`/api/pool/test`) 인증 체크 중 아무것도 실제로 구현돼
있지 않았다(작업 트리에도 관련 커밋 없음, 수정된 파일은 체크리스트 문서 하나뿐). 체크는 계획
표시였던 것으로 파악하고, 이번 세션에서 실제 구현부터 다시 진행했다.

## 2. 인터넷망(로컬) vs 폐쇄망 테스트 범위 정리

1단계(코드 로직 자체)는 로컬 Docker Oracle(XE)로 100% 검증 가능하지만, 2단계(8개 실인스턴스 데이터
전환)는 실제 폐쇄망 IP(`10.133.x.x`)가 이 PC에서 접근 자체가 안 되고, 로컬 Docker의 "8개 alias"가
사실 전부 같은 XE 컨테이너 하나를 가리키는 가짜 구성이라 RAC/개별 인스턴스 시나리오 자체를 재현할
수 없음을 확인 — 2단계 이후는 폐쇄망에서만 검증 가능하다는 원칙을 재확인했다.

## 3. 1단계 코드 구현 (main + AIX 동일 반영)

- `TargetDbConfig`에 `connectMode` 필드 추가.
- `OracleConnectionPoolManager.buildDsn()`에 `connect_mode`(`sid`/`service`/`descriptor`) 분기
  추가 — 미지정 시 기존 동작(host 빈값→alias, host 지정→`host:port:sid`) 그대로 유지. 풀 생성 시
  실제 DSN을 `oracle-<id> -> <dsn>` 형태로 INFO 로그에 남긴다.
- `DatabaseConfigService.init()`에 id 중복 / (host,port,sid) 중복 WARN 로그 추가.
- `PoolTestController`(`/api/pool/test`)에 `authService.canAccessDb()` 인증 체크 추가 — 기존엔
  토큰 없이 누구나 호출 가능했던 보안 이슈였는데 이번에 같이 막았다.
- `TnsAdminInitializer.deriveFromOracleHome()` 제거 — `application.properties`의
  `dbagent.oracle.tns-admin`이 폐쇄망에서도 항상 명시적으로 설정돼 있음이 확인되어(main 체크리스트
  0단계 C) `oracle.env`의 `ORACLE_HOME`에서 유추하던 옛 폴백 경로를 삭제했다.
- 본 프로젝트는 Java 8 타깃이라 `record` 대신 기존 필드/getter 클래스, `String.isBlank()` 대신
  `com.dbagent.util.Strings`, `Map.of()` 대신 `com.dbagent.util.Maps`, switch식 대신 전통적
  switch문으로 main과 동일한 로직을 포팅.

## 4. 로컬 Docker Oracle로 실측 검증 (main 기준)

`dist/databases.json`에 `connect_mode=sid`(`127.0.0.1:1521:XE`), `connect_mode=service`
(`127.0.0.1:1521/XEPDB1`) 테스트 인스턴스와 의도적 id 중복 항목을 임시로 추가해 검증(테스트 후 원복):

- `sid`/`service` 두 방식 모두 `/api/pool/test` 접속 성공 확인.
- 기존 alias 방식(`ORCL`~`ORCL4` 등) 회귀 없음 확인.
- 중복 id(`patent_integ_1`) WARN 로그 정상 출력 확인.
- `/api/pool/test`를 토큰 없이 호출 → 403, 관리자 토큰으로 호출 → 200 정상 확인.

AIX는 동일 로직을 Java 8 문법으로 포팅한 뒤 `mvn clean package` 컴파일 성공까지만 확인 — 실접속
검증은 main 쪽 로컬 Docker로 대신했다.

## 5. 폐쇄망 2단계 진행 방법 안내

`databases.json`의 `host`/`port`/`sid`(인스턴스별 SID, main 체크리스트 0단계 A/B표 기준)만 채우면
되고, `connect_mode`는 SID 방식이라 비워둬도 된다(`host`가 채워지면 기존 로직이 이미
`host:port:sid`로 처리하므로). `oracle.env`·`application.properties`는 3~4단계(8개 전부 2단계
통과 + 며칠 정상 운영 확인 후)까지 그대로 유지해야 한다고 안내 — 지금 같이 제거하면 아직 미검증된
나머지 인스턴스의 alias 폴백 안전망이 통째로 사라진다.

---

## 빌드/배포

**main**: `mvn clean package` → `dist`(8006) 배포/기동/실측 검증 후, 테스트 전 상태(정지 상태)로
원복. **AIX**: `mvn clean package`(Java 8 타깃, `jdk17 --release 8`로 컴파일) → `dist` +
`dist-aix`(폐쇄망 이관용) 모두 반영. 커밋+push 완료 — main `a9c6655..6c77289`, AIX
`6ab7236..7940da4`.
