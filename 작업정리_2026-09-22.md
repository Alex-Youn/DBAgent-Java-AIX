# 작업정리 2026-09-22

> 대상: `DBAgent-Java` / `DBAgent-Java-AIX` 양쪽 + 폐쇄망 AIX 실서버
> 상태: 전부 완료. `작업정리_2026-09-21-야간.md`의 "남은 것(폐쇄망에서만 가능)" 항목 전부 이 문서에서 마무리됨.

---

## 1. 개발 PC pull + 빌드/배포 3곳

- main `2d48b22→27d5987`, AIX `b1fa9e2→ac77130` (6단계 암호화 구현 + 폐쇄망 적용 가이드 커밋).
- pull 도중 로컬 미커밋 변경/미추적 산출물과 충돌 → `git stash`로 격리 후 진행(체크리스트 문서 로컬 편집분, `target/classes` 빌드 산출물). fast-forward 완료.
- 이번 pull에서 소스 변경분은 `app.js` 하나뿐(대시보드 DB 전환 in-flight 가드를 `Set` 기반으로 바꾼 레이스 수정) → 캐시버스터만 갱신: main `app.js?v=157→158`, AIX `app.js?v=145→146` (`git log -S`로 두 번호 다 미사용 확인).
- 두 저장소 `mvn clean package` → **배포 3곳**(main `dist`, AIX `dist`, AIX `dist-aix`) jar 교체(`.bak-20260922-0823` 백업). 로컬 인스턴스는 전부 정지 상태라 재기동 불필요.
- HTTP로 배포본 서빙 내용 확인: `app.js`에 수정된 코드(`iv2FetchInFlightDbIds`) 실제 포함, 캐시버스터 버전 일치.

## 2. 오래된 stash 정리

- `DBAgent-Java`: pull 전 만든 stash 1개 남기고, 09-14/그 이전 pull 때 쌓인 오래된 stash 3개(빌드 산출물, 옛 `databases.json`/`oracle.env` 등 지금은 무의미한 내용) 삭제.
- `DBAgent-Java-AIX`: 동일하게 오래된 stash 1개 삭제.

## 3. AIX `dist`(8007) DB 목록이 비어있던 문제 → 복구

로컬 검증 중 8007에서 "조건에 맞는 DB가 없습니다" 발견.

- **원인**: `application.properties`의 `db_config/` 경로 설정 자체는 정상이었으나, 이 dist 폴더가 새 구조로 처음 넘어올 때 옆에 있어야 할 `databases.json`이 이미 삭제된 상태였음 → 마이그레이션이 "완료(`migrated_from_json=1`)"로만 기록되고 실제로는 가져올 게 없어 `db_instances` 0건으로 남음. H2 파일을 직접 열어 `SELECT COUNT(*) FROM db_instances` → 0 확인.
- **복구**: 잔존 사본 `C:\AI-PROJECTS\Conf_file\databases.json`(레거시 형식, 실데이터 6그룹 11인스턴스 보유)을 `dist/`에 임시로 넣고, `db_config_meta.migrated_from_json`을 0으로 되돌린 뒤 재기동 → 앱이 이미 구현된 `migrateFromJsonFile()`을 다시 실행해 11개 인스턴스 정상 이관(각 인스턴스 비밀번호는 AIX dist 고유 키로 재암호화됨). `/api/config`로 6그룹 11인스턴스 확인 후 임시 `databases.json`은 즉시 삭제.
- 원본 `Conf_file/databases.json`은 손대지 않고 보존(다른 곳에 더 필요할 수 있어 삭제는 보류).

## 4. 폐쇄망 AIX 실서버 — 6단계(비밀번호 AES 암호화) 최종 검증

`작업정리_2026-09-21-야간.md`에서 "폐쇄망에서만 가능"으로 남겨뒀던 항목들을 이날 실서버에서 확인·완료.

| 항목 | 결과 |
|---|---|
| AES 키 길이 | **256bit** (개발기와 동일, 128bit 폴백 없음 — 최상 케이스) |
| Oracle 인스턴스 실접속 | 대시보드에 실제 데이터 정상 표시 (복호화된 비밀번호로 실접속 성공) |
| `err.log` | 비어있음 |
| 정상종료→재기동 (B64 잔재 정리) | 완료 |
| 빈 비밀번호 저장 시 기존 값 유지 | 확인 |
| 키 파일 권한/백업 | 600 확인, 별도 장소 사본 보관 |
| 실서버 잔존 `oracle.env`/`databases.json` 삭제 | 완료 |

→ 6단계 폐쇄망 적용 **전 과정 완료**로 판정. `설계문서/oracle_env_제거_체크리스트.md`의 "남은 것(폐쇄망에서만 가능)" 절을 완료 처리로 갱신.

> 재암호화 로그가 두 번째 재기동에서 안 보인 것에 대한 확인: `DatabaseConfigService.reencryptAllPasswords()`는 `changed>0 || failed>0`일 때만 INFO를 남기므로(`java:161`), 이미 전부 암호화된 상태에서 재기동하면 로그가 안 남는 게 정상 동작. 처음엔 이상하다고 의심했으나 코드 확인 후 정상으로 판정.

## 5. `dbagent.key` 앱 디렉터리 밖으로 이전

운영 안전을 위해 키 파일을 앱 폴더(`dist-aix/`) 밖으로 옮기는 작업 진행.

- 1차 시도 실패: `dbagent.security.key-file=/SFO_REPOSIT/DB-InsightFlow/ASE` (디렉터리까지만 지정, 파일명 누락) → `FileNotFoundException: ... (디렉토리에 쓸 수 없습니다)`로 기동 실패. 로그(`out.log`)는 재기동마다 `start-aix.sh`가 `>`(덮어쓰기)로 여는 구조라 실패 로그가 남아있지 않아, 재현 후 로그 파일을 별도 복사해 원인 확정.
- 수정: `dbagent.security.key-file=/SFO_REPOSIT/DB-InsightFlow/ASE/dbagent.key`(파일명까지 명시) → 정상 로드(`마스터 키 로드 완료`, 기존 키 `k1` 재사용, 새로 생성 아님) → 대시보드 데이터 정상 확인 → 원래 위치(`dist-aix/dbagent.key`) 삭제 완료.
- (차후 검토로 보류) 프로퍼티 파일에 키 경로가 그대로 보이는 것에 대한 우려 제기 → Spring Boot relaxed binding으로 `DBAGENT_SECURITY_KEY_FILE` 환경변수로 대체 가능하다는 방법만 안내, 이번엔 프로퍼티 방식 그대로 적용.

## 6. `dist-aix/java8/`(번들 JDK) 누락 발견 → 복구

`start-aix.sh` 실행 중 `WARNING: bundled java8/ not found, falling back to PATH java (Oracle 19.3.0 client JDK)` 발견.

- **배경**: `java8/`는 2026-08-24에 그 AIX 서버에 이미 설치돼 있던 시스템 Java 8(`Java8_64.sdk`/`Java8_64.jre`, IBM SDK 8.0.0.605, ppc64)을 tar로 묶어 프로젝트 폴더에 동봉한 것(`계정관리_및_AIX이관_작업정리_2026-08-21.md` 3-3절). 274MB라 `.gitignore` 대상이라 git으로 전파 안 됨 — 이번 dist-aix 이관 때 누락된 것으로 추정. 로컬 개발 PC(Windows)에도 이 폴더는 원래 없음(AIX 바이너리라 Windows에서 만들 수 없음).
- **조치 방법 안내**: 그 AIX 서버 자체에서 `lslpp -f Java8_64.sdk`로 실치 경로 확인 → `tar`+`gzip`으로 묶어서 `dist-aix/java8/`에 풀기. Oracle 클라이언트 JDK를 계속 빌려쓰면 "시스템 Java와 격리"라는 기존 설계 원칙에 어긋나고, DBA가 그 JDK를 바꾸면 이 앱도 영향받는 리스크가 있음.
- 사용자가 서버에서 직접 처리 완료(해결 확인).

## 7. v2 대시보드 — Active Session 목록 인스턴스명 라벨 버그 (오후)

사용자 보고: "신규 대시보드 Active Session 목록 옆에 SID가 들어가야 하는데 옛 `databases.json` 식별자(`patent_integ_1`)가 뜬다." 여러 겹의 문제가 순차로 드러나 단계적으로 고쳤다.

1. **1차 원인**: `app.js`의 `iv2-session-db-name` 라벨이 실제 Oracle 조회값이 아니라 DB 선택용 내부 식별자(`dbId`)를 그대로 찍고 있었음 — `/api/session` 응답의 `db_name`(`v$instance.instance_name` 라이브 조회)으로 교체.
2. **2차**: `/api/session`이 `WHERE status='ACTIVE'`로 필터링돼 있어 ACTIVE 세션 0건이면 응답 배열이 비어 `db_name`을 못 읽고 다시 `dbId`로 폴백 — 세션 유무와 무관하게 매 폴링 채워지는 `/api/instance_overview`의 값을 `iv2LastInstanceName` 공유변수로 저장해 대신 쓰도록 변경.
3. **3차(사용자 실측)**: 로컬 XE 테스트 계정에 `v$sysmetric` 등 조회 권한이 없어 `instance_overview` 전체가 예외로 실패 → `iv2LastInstanceName`이 아예 안 채워짐. 사용자가 권한 정정 후 `(XE)`로 정상 표시 확인 — 코드 문제 아니었음.
4. **4차**: 로그인 직후 첫 폴링 사이클에서 `fetchInstanceOverviewV2()`와 `fetchActiveSessionV2()`가 같은 `Promise.all`로 동시에 나가는데 응답 순서가 보장 안 돼, 아직 안 채워진 `iv2LastInstanceName`을 읽고 `dbId`로 폴백하는 순간이 있었음 → 값이 없으면 `dbId` 대신 그냥 비워두고 다음 폴링에 채워지게 변경.
5. **5차(code-inspector 에이전트 발견)**: DB 전환 시에도 같은 클래스의 레이스 — `iv2LastInstanceName`이 dbId와 무관한 단일 변수라, DB A→B 전환 시 아직 A의 값을 들고 있는 순간 B의 세션 목록에 A의 인스턴스명이 잠깐 뜰 수 있었음 → `iv2LastInstanceNameDbId`를 추가해 "이 값이 어느 dbId 기준인지" 같이 추적, 불일치 시 빈 값 처리.
6. **6차(사용자 실측)**: "DB 전환해도 라벨이 10초 후에나 뜬다" — `fetchInstanceOverviewV2()`(하위 쿼리 9개, 느림)와 `fetchActiveSessionV2()`(빠름)가 각자 완료 시점에 라벨을 딱 한 번만 그려서, 세션 쪽이 먼저 끝나면 그 사이클은 빈 값으로 굳고 다음 10초 폴링까지 재렌더링 계기가 없었음 → `iv2RenderSessionDbNameLabel()` 공유 렌더 함수로 통합해 **두 fetch 중 나중에 끝나는 쪽이 항상 최종 렌더**를 맡도록 변경 — DB 전환 즉시(1초 내외) 표시.

## 8. v2 대시보드 — "확인필요알림"(active_alerts) 무한 pending → 그 DB 전체 먹통

**증상**: 신규 대시보드에서 특정 DB(ORAKIPO1/ORAKIPO2, patent_integ_1/2, **11g**)로 전환하면 "확인필요알림" 프레임이 "데이터를 불러오는 중입니다"에서 영원히 안 넘어가고, 그 DB의 다른 위젯도 전부 멈춤. 다른(12cR2/19c) 인스턴스는 정상.

- **근본 원인**: `fetchInstanceDashboardV2()`가 6개 위젯 fetch를 `Promise.all`로 묶고 `iv2FetchInFlightDbIds` 가드로 dbId별 중복 폴링을 막는데, 그중 하나(`active_alerts`)가 응답 없이 멈추면 `finally`가 영영 안 돌아 그 dbId는 가드에 영구히 걸려버림.
- **쿼리 레벨 원인 확정**: 사용자가 SQL*Plus로 `getActiveAlerts()`의 쿼리 3개를 직접 실행해 범인을 특정 — TEMP 사용률/장시간쿼리 카운트는 빠름(0.002초), **v$lock+dba_objects 조인(Blocking Session 감지)만 무응답**. 이미 `setQueryTimeout(3초)`가 걸려있었는데도 안 끊긴 것으로 봐서, **락 대기로 블로킹된 쿼리는 JDBC 쿼리 타임아웃으로 못 끊는다**는 게 실측 확인됨(취소 신호도 같은 락 대기에 걸림).
- **코드 원인**: `dba_objects o`가 join만 되고 `o`의 컬럼은 어디에도 안 쓰임 — TM lock은 항상 실제 object에 걸리므로 존재 확인 자체가 불필요한 조인이었음. `getActiveAlerts()`의 이 쿼리와 `getFailureProb()`(장애조치 버튼, 같은 `Promise.all`에 걸림)의 본/fallback 쿼리 총 3곳에서 **`dba_objects` join 제거**.
- **이중 방어**: 쿼리를 못 끊는 문제에 대한 근본 대응으로, `MonitorController`에 `fleet_status`가 이미 쓰던 패턴(백그라운드 executor + `withTimeout` + db_id별 in-flight dedupe)을 `active_alerts`/`failure_prob`에도 적용 — 내부 쿼리가 아무리 오래 블로킹돼도 **HTTP 응답은 5초 안에 끝남**(안 끊긴 쿼리 자체는 백그라운드 스레드에서 계속 돌지만 커넥션은 `oracle.net.READ_TIMEOUT`(60초)이 결국 정리, in-flight dedupe라 커넥션 풀 고갈로는 안 번짐 — code-inspector 에이전트가 재검증).
- 프런트에도 `iv2Fetch()`(AbortController, 8초) 이중 방어 추가 — v2 6개 fetch 전부 적용.
- `failure_prob` 타임아웃 시엔 "이상 없음(0건)"이 아니라 503으로 응답해 장애조치 버튼을 숨김(판단 보류) — 잘못된 안심 신호 방지.

## 9. TEMP 테이블스페이스 사용률 쿼리 부정확 → 교체

사용자 지적: "`v$temp_space_header` 기반 TEMP 사용량이 실제와 다르다." `v$temp_space_header`는 파일 헤더 익스텐트 하이워터마크 기준이라 TEMP 세그먼트가 반납된 뒤에도 안 줄어드는 문제가 있음 — `dba_temp_free_space`(allocated_space - free_space, 테이블스페이스당 이미 집계된 뷰)로 교체. `NULLIF(allocated_space, 0)`로 미사용 TEMP 테이블스페이스의 0 나누기도 방어. (이후 "DB마다 결과가 다르다"는 추가 문의가 있었으나 사용자가 착오였다고 정정 — 실제 버그 아님.)

## 10. code-inspector 에이전트 정밀 점검

오늘 수정분(`MonitorController.java`, `MonitorService.java`, `app.js`, main/AIX 양쪽) 전체를 `code-inspector` 서브에이전트로 재검토. `MonitorController`/`MonitorService` 변경분은 전부 정상으로 확인(CompletableFuture 예외 unwrap, in-flight 맵 race, `Maps.of()`/`Lists.of()` Java 8 호환 치환, `dba_objects` 제거 후 결과 동일성, `NULLIF`/`GREATEST` 널 처리 등). `app.js`에서 위 7절 5차의 DB 전환 라벨 레이스 1건을 추가로 찾아 직접 수정 — main/AIX 동일 반영, 양쪽 빌드 성공 확인.

## 11. 배포 이력 (오후, 총 6회 빌드/배포/재기동)

7~10절 수정마다 `mvn clean package` → 배포 3곳(main `dist`, AIX `dist`, AIX `dist-aix`) → 로컬 8006/8007 재기동 → 배포본 반영 확인을 반복. 캐시버스터: main `app.js?v=158→164`, AIX `app.js?v=146→152` (매번 `git log -S`로 미사용 번호 확인).

---

## 남은 것

- **`dist-aix/` 실제 폐쇄망 서버 반입 대기** — 오늘 오후 수정분(SID/인스턴스명 라벨, active_alerts/failure_prob 타임아웃, dba_objects 제거, TEMP 쿼리 교체, DB 전환 레이스)이 로컬 dist까지만 배포됨. 사용자가 jar 반입 후 재기동하여 테스트, 결과 피드백 예정.
- 6단계(비밀번호 AES 암호화) 자체는 설계→구현→로컬 검증→폐쇄망 검증→키 파일 격리까지 전 과정 완료(4~6절).
- (선택, 급하지 않음) `dbagent.security.key-file` 경로를 프로퍼티 파일 대신 환경변수(`DBAGENT_SECURITY_KEY_FILE`)로 옮기는 것 — 차후 검토로 보류된 상태.
- 오늘 변경분 전체 커밋 여부 — 아직 미커밋.
