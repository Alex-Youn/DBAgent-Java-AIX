# 작업정리 2026-09-18

> 대상: `DBAgent-Java`/`DBAgent-Java-AIX` (전 항목 양쪽 동기화, `com.dbagent.aidba` 제외 원칙 유지)
> 상태: 전부 완료 · 빌드/배포(`dist` 2곳 + `dist-aix`) 및 폐쇄망 실측 피드백 반영까지 완료.
> 참고: 전날 세션에서 시작된 오라클 인스턴스 대시보드 UI-1(v2)/UI-2(v3) 작업이 폐쇄망 검증 전까지
> 커밋을 미뤄 왔던 부분이라, 이번 커밋에는 그 이후 누적된 수정분이 함께 포함된다.

---

## 1. UI-2(v3, 벤토형) 대시보드 완전 제거

Top SQL(Elapsed Time 기준)/Quick Stats 패널을 먼저 제거한 뒤, UI-2(v3) 자체를 화면에서 깨끗하게
들어냈다 — HTML(`#dashboard-v3-view` 블록 전체), JS(v3 전용 fetch/render 함수, `iv3PollingTimer`,
전환 버튼의 `v3` 옵션), CSS(`.iv3-bento-grid`/`.iv3-hero-*`/`.iv3-gauge-*` 등 v3 전용 클래스),
백엔드(`ConfigController`의 `health_score_*` 필드)까지 정리. 단 `.iv3-lock-grid`/`.iv3-lock-card`
+ `renderSparkline()`(구 `iv3RenderSparkline`)는 UI-1의 Lock 현재값 카드가 그대로 재사용 중이라
남겨 뒀다. 전환 버튼은 클래식/UI-1(v2) 2개만 남는다.

## 2. UI-1(v2) 레이아웃/성능 개선

- **Lock 추이 위치 이동**: CPU/DB Time 추이와 Top Wait Events 사이로 이동, 프레임 크기 동일화.
- **Active Session 목록 신설**: 기존 Lock 추이가 있던 자리에 압축형 목록 추가(`.iv2-alert-list`
  카드 스타일 재사용, 클릭 시 세션 상세 팝업 — 기존 `.clickable-session-row` 델리게이트 그대로 사용).
- **초기화면 로딩 지연 수정**: CDP 네트워크 타이밍 분석으로 원인 규명 — DB 자동선택 전에 빈
  `db_id`로 나가던 낭비성 요청 3종 가드 추가, DB 트리 클릭 시 다음 폴링(최대 10초)을 기다리지 않고
  즉시 갱신하도록 수정, "Aggressive DB Users 로더"의 in-flight 가드 누락(매초 중복요청) 수정 —
  체감 로딩 시간 약 10초 → 0.3초.
- 차트 프레임 높이(220px→150px)·타이틀/바디 패딩 축소.
- **Top Wait Events**: 막대 두께 고정, 이벤트별 색상 팔레트 적용, X축 24시간제 5분 단위(60분/12틱).

## 3. TM/TX Lock 현재값 실시간화

기존엔 최대 60초 지연되는 추이 샘플의 마지막 값을 썼는데, 장애조치 버튼(`/api/failure_prob`)과
같은 실시간 소스로 통일 — `fetchIncidentGateV2()`가 받아오는 값으로 현재값 카드/스파크라인을 갱신.

## 4. 8개 추가 요청 반영

1. CPU 사용률/메모리 사용률/Buffer Cache Hit Ratio KPI 타일에 얇은 가로 진행률 막대 추가.
2. UI-2(v3) 완전 제거 (1절 참고).
3. Active Session 목록·확인 필요 알림 프레임 높이 약 4cm 증가(`max-height` 220px→371px).
4. Lock 위험 색상 임계치 조정: TM Lock ≥6건, TX Lock ≥15건(기존엔 TX는 1건만 돼도 위험색;
   장애조치 버튼 자체 임계치와는 별개로 분리).
5. Lock Holder/Waiter Tree 자동갱신 기본값 OFF→ON.
6. 확인 필요 알림에 테이블스페이스 97% 알림 추가 — 최초엔 사용자 확인(AskUserQuestion)에 따라
   "실시간 조회" 방식으로 구현(→ 6절에서 폐쇄망 실측 후 방식 변경).
7. CPU/DB Time 추이 색을 Current Session 그래프 팔레트로 통일(→ 5절에서 사용자 요청으로 Rewind).
8. "Lock 추이" 문구를 "DBMS Lock 추이(TM/TX)"로 변경, DBMS Lock 추이 색도 Current Session 팔레트로
   통일(CPU/DB Time과 색이 겹치던 문제 해소).

## 5. 폐쇄망 1차 피드백 반영

1. **Top Wait Events 겹침 버그**: 이벤트 4개까지는 정상, 5개부터 막대가 겹치고 라벨이 3개만 보이는
   증상 — 원인은 `barThickness`(고정값)가 5개일 때 150px 프레임을 초과해서 발생. `maxBarThickness`
   (상한만 지정, 필요시 자동으로 얇아짐)로 교체 + y축 `autoSkip:false`로 해결.
2. **CPU/DB Time 색 Rewind**: 4절 7번에서 바꾼 Current Session 팔레트 통일을 원래 배색(CPU 파랑
   `#3987e5`, DB Time(AAS) 주황 `#d95926`)으로 되돌림. "DBMS Lock 추이(TM/TX)" 문구도 "DBMS Lock
   추이"로 축약(Lock 자체 색은 유지).
3. **차트 라벨 가독성**: CPU/DB Time·DBMS Lock 추이·Top Wait Events의 범례/축 눈금을 앱 기존 관례인
   `chartLineColor()`(반투명 흰색) 헬퍼로 통일 적용.
4. **전환 버튼 축소**: 38px→28px, 아이콘 18px→14px. UI-1(v2) 아이콘을 flask-conical(베타/실험
   느낌)에서 KPI 타일 그리드 구조를 더 잘 표현하는 layout-grid로 교체.

## 6. 폐쇄망 성능 이슈: 테이블스페이스 97% 알림 쿼리

**증상(사용자 보고)**: 인스턴스 전환 딜레이 심화, 클래식(origin) 대시보드 전환 시 busy, 인스턴스
전환 시 간헐적 "접속할 수 없습니다".

**원인**: `getActiveAlerts()`(`/api/active_alerts`)에 넣은 `dba_data_files`/`dba_free_space` 실시간
조인 쿼리. 이 API는 UI-1이 10초마다 폴링 + 인스턴스 전환마다 즉시 1회 추가 호출하는데, 테스트용
도커 Oracle은 테이블스페이스 수가 적어 가벼웠지만 실제 운영 규모(테이블스페이스/데이터파일 수가
훨씬 많음)에서는 이 딕셔너리 뷰 조인이 무겁게 나와 인스턴스당 커넥션 풀(최대 10개)을 다른 쿼리와
같이 오래 붙잡았다 — 이게 전환 지연/기존 대시보드 busy/간헐적 접속 실패로 번짐. 셋 다 최초 로드에만
국한된 게 아니라 이 쿼리가 호출될 때마다 지속적으로 발생하는 구조였음(코드 추적으로 확인, 별도
재현 없이 결론).

**수정**: 이 쿼리를 실시간 조회에서 제거하고, 이미 CPU/AAS/Lock을 주기적으로 수집하던
`InstanceMetricSamplerService`(기본 60초 주기 백그라운드 스케줄러)로 이동 — `sampleOne()`에서 같은
쿼리를 돌려 db_id별 `ConcurrentHashMap` 캐시(`tablespaceAlertsCache`)에 저장하고,
`getActiveAlerts()`는 이 캐시만 읽는다. 알림 반영이 최대 샘플링 주기(기본 60초)만큼 늦어질 수
있지만, 인스턴스 전환/화면 전환 시 부하는 사라진다. 테스트 DB로 응답시간 0.12~0.15초 확인.

**남은 과제(다음 스텝으로 보류)**: 사용자 지적 — "테이블스페이스 임계치 판단 쿼리가 생각보다 성능이
좋지 않은 것 같다". 위 수정으로 요청 경로에서는 빠졌지만, 쿼리 자체(`dba_data_files`/
`dba_free_space` 조인)가 실제 운영 규모에서 여전히 무거울 수 있어 60초 백그라운드 사이클
자체에서도 개선 여지가 있는지 다음 단계에서 검토 예정.

## 7. 신규 DB 등록 시 즉시 1회 수집

추이 그래프가 "수집이 시작된 시점부터만 그려진다"는 질문에서 출발 — 신규 등록 직후엔 다음 정기
사이클(최대 60초)까지 그래프/테이블스페이스 알림이 비어 보이는 문제 확인. `POST /api/db_configs`
성공 직후 `InstanceMetricSamplerService.sampleNowAsync()`를 비동기(HTTP 응답을 막지 않음)로
호출해 즉시 1회 수집하도록 추가. 테스트로 등록 3초 만에 `metric_history`/`active_alerts` 모두
채워짐을 확인.

**보류(다음 스텝에서 결정)**: 앱 재시작 직후엔 신규 등록 여부와 무관하게 전체 인스턴스가 동일하게
비어 보이는 문제는 별개 사안 — 기동 시 전체 인스턴스 일괄 즉시수집을 추가할지는 사용자가 추후
결정하기로 함.

## 8. 커넥션 풀/스레드 공유 여부 검토 (Q&A, 코드 변경 없음)

"신규 등록 즉시수집이 다른 DB에 영향을 주지 않냐"는 질문에 코드 근거로 답변:
- Oracle 커넥션 풀은 인스턴스별로 완전히 분리(`OracleConnectionPoolManager`가 pool key별 개별
  `ReentrantLock` 사용, 전역 락 아님) — 영향 없음.
- `SAMPLER_EXECUTOR`(고정 스레드 4개)는 정기 사이클과 즉시수집이 공유하지만, 각 스레드는 서로 다른
  DB에 독립적으로 연결하므로 실질적 경합 없음.
- 로컬 SQLite(`users.db`, 커넥션 풀 3개, 로그인 토큰 검증과 공유)만 유일하게 진짜 공유 자원 —
  즉시수집의 기록이 정기 사이클의 순차 기록과 타이밍이 겹칠 가능성은 이론상 있으나, 짧은 시간(밀리초
  단위) 1개 커넥션을 더 쓰는 정도라 실질 영향은 미미하다고 판단. 필요시 완전 직렬화하는 추가 방어
  코드도 가능하다고 안내했고, 현재는 보류.

---

## 빌드/배포

전 항목 main(`mvn clean package`) / AIX(`build.ps1`) 각각 빌드 → `dist`(메인 8006, AIX 8007) +
`dist-aix`(폐쇄망 이관용) 3곳 모두 반영. CDP 브라우저 자동화 + 직접 API 호출(도커 Oracle 테스트
DB, 신규 인스턴스 등록/삭제 포함)로 매 단계 검증 후 배포. 캐시 버스터(`app.js?v=`/`style.css?v=`)
매번 증가.
