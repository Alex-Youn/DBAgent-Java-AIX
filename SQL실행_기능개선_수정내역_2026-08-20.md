# DBAgent-Java SQL 실행 기능 개선 및 빌드 스크립트 추가 (2026-08-20)

## 수정한 파일

- `src/main/resources/static/style.css`
- `src/main/resources/static/app.js`
- `src/main/resources/static/index.html`
- `src/main/java/com/dbagent/query/SqlQueryRequest.java`
- `src/main/java/com/dbagent/query/SqlQueryService.java`
- `src/main/java/com/dbagent/query/SqlQueryController.java`
- `src/main/java/com/dbagent/oracle/ConfigController.java`
- `src/main/java/com/dbagent/oracle/DatabaseConfigService.java`
- `src/main/resources/application.properties`
- `build.ps1`, `build.bat` (신규)
- `databases.json`, `dist/databases.json` (`patent_integ_1`에 `accounts` 배열 추가)
- `target/dbagent-java-0.1.0.jar`, `dist/dbagent-java-0.1.0.jar` (재빌드/배포 산출물)

## 1. SQL 실행 결과 테이블 가로 스크롤 문제 수정

**증상**: SQL 실행 화면에서 `select * from v$session`처럼 컬럼이 많은(103개) 쿼리를 돌리면 컬럼이 화면 폭에 눌려서 글자가 잘리고, 가로 스크롤바도 뜨지 않음.

**원인**: 전역 `.data-table`이 `table-layout: fixed; width: 100%`로 고정돼 있어, 컬럼 수와 무관하게 항상 패널 폭 안에 모든 컬럼을 욱여넣는 구조였음. 그래서 컨테이너 스크롤(`overflow-x: auto`)이 있어도 테이블 자체가 컨테이너보다 넓어질 수가 없어 스크롤이 발생하지 않았음.

**수정**: SQL 실행 결과 테이블에만 `sql-result-table` 클래스를 추가하고, `table-layout: auto; width: auto; min-width: 100%`로 재정의해 컬럼이 내용에 맞게 자연스럽게 넓어지도록 함 (컬럼당 `max-width: 320px`로 과도하게 넓어지는 것만 제한). 대시보드 등 다른 고정 컬럼 테이블(`.data-table`)은 영향 없음.

**검증**: 실제 Oracle DB에 `select * from v$session` 실행 → 컬럼 103개, 가로 스크롤 정상 동작 확인.

## 2. SQL 실행 결과 조회 건수(Row 수) 설정 가능하게 추가

**요청 배경**: 기존엔 조회 건수 상한(500건)이 `application.properties`에 하드코딩되어 있어 사용자가 조정할 수 없었음.

**수정**:
- `SqlQueryRequest`에 `max_rows` 필드 추가
- `SqlQueryService.execute()`가 요청받은 `max_rows`를 사용하되, `dbagent.sql-runner.max-rows-limit`(신규, 기본 5000)으로 상한을 clamp — 사용자가 아무리 큰 값을 넣어도 이 값을 못 넘음
- `ConfigController`의 `/api/config`에 `sql_runner_max_rows`(기본값), `sql_runner_max_rows_limit`(상한값)을 추가해 프론트에서 읽어감
- SQL 실행 화면에 "조회 건수" 입력란 추가 (기본 500으로 자동 채워짐), 실행 시 `max_rows`로 서버에 전달
- 상한에 걸려 결과가 잘렸을 때 "조회 건수 상한 N건 도달" 메시지에 실제 적용된 값을 표시

**검증**: `max_rows=10`으로 `dual` 조회, `max_rows=5`로 `v$session` 조회(103컬럼, `truncated: true`, `max_rows: 5` 응답) 확인.

## 3. Enter 키로 SQL 즉시 실행되도록 변경

**요청**: 기존엔 Ctrl+Enter로만 실행됐는데, Enter만 눌러도 바로 실행되게.

**수정**: SQL 입력창(textarea)이 여러 줄 입력을 지원해야 해서, **Enter = 실행, Shift+Enter = 줄바꿈**으로 분리 (Ctrl+Enter도 그대로 동작). 버튼 라벨 "실행 (Ctrl+Enter)" → "실행 (Enter)"로 변경, placeholder에도 단축키 안내 추가.

## 4. 질의응답 (코드 수정 없음)

- **"30초 타임아웃이 뭔가?"** → `SqlQueryService`에서 JDBC `Statement.setQueryTimeout(30)`으로 설정된 값. 쿼리 실행 후 30초 안에 완료되지 않으면 드라이버가 강제로 취소하고 에러를 반환함. 늘리려면 `application.properties`의 `dbagent.sql-runner.timeout-seconds` 조정.
- **"DESC 같은 명령은 동작하지 않나?"** → 동작 안 함. `DESC`는 실제 SQL이 아니라 SQL*Plus 클라이언트가 로컬에서 해석해 데이터 딕셔너리 조회로 바꿔주는 명령어인데, 이 기능은 입력 문자열을 그대로 JDBC `Statement.execute()`에 넘기는 구조라 해석 계층이 없음. `ORA-00900` 에러가 남. 대신 `select column_name, data_type, ... from user_tab_columns where table_name = 'EMP'`처럼 실제 SQL로 대체해야 함.

## 5. 빌드 스크립트 신규 작성 (`build.ps1`, `build.bat`)

**계기**: 정적 리소스(css/js/html)나 자바 코드를 고쳐도 브라우저에 반영이 안 되는 문제가 있었음. 원인은 이 앱이 `mvn spring-boot:run`이 아니라 **프로젝트 루트의 `start.ps1`로 `target\dbagent-java-0.1.0.jar`를 빌드된 jar 그대로 실행**하는 구조라서, 소스만 고치고 재빌드/재기동을 안 하면 예전 jar가 계속 떠 있었던 것. 게다가 그 jar가 실행 중인 상태에서 `mvn package`를 돌리면 Windows가 jar 파일을 잠그고 있어 "Unable to rename ... .jar.original" 에러로 빌드 자체가 실패함.

**수정**: `build.ps1`을 새로 작성해 이 루틴을 자동화함.
1. `dbagent-java.pid`로 현재 실행 중인지 확인 → 실행 중이면 `stop.ps1`로 먼저 정지 (잠금 해제)
2. 번들 Maven(`maven39\bin\mvn.cmd`, PATH의 mvn보다 우선)으로 `mvn -q -DskipTests package`
3. 빌드 성공 후, 원래 실행 중이었을 때만 `start.ps1`로 자동 재시작 (꺼져 있었으면 빌드만 하고 끝)
4. `-Dist` 스위치를 주면 `dist\`(배포용 self-contained 패키지)도 같이 동기화: `dist\` 인스턴스가 떠 있으면 정지 → 기존 `dist` jar를 `.bak`으로 백업 → 새 jar 복사 → 원래 떠 있었으면 재기동

`build.bat`은 `%*`로 인자를 그대로 넘겨서 `build.bat -Dist`도 동작.

**검증**: `.\build.ps1`, `.\build.ps1 -Dist` 둘 다 직접 실행해서 정지→빌드→재기동 흐름과 `dist` jar 해시 일치(SHA256 동일) 확인.

## 6. SQL 실행 시 접속 계정(User) 선택 기능 추가

**요청 배경**: SQL 실행 메뉴는 왼쪽 사이드바에서 선택한 DB에 `databases.json`의 고정 계정(user/password) 하나로만 접속되던 구조라, 같은 DB를 다른 계정으로 접속해서 실행하는 게 불가능했음.

**설계**: 완전히 별도의 JSON 파일을 새로 만드는 대신, 기존 `databases.json`의 인스턴스 밑에 **선택적(optional)** `accounts` 배열을 추가하는 방식으로 결정. `accounts` 필드가 없는 인스턴스는 기존과 100% 동일하게 동작(하위 호환) — 계정 선택이 필요한 DB에만 나중에 추가하면 됨.

```json
{
  "id": "patent_integ_1",
  "user": "dbagent_user",
  "password": "B64(ZGJhZ2VudDAx)",
  "accounts": [
    { "user": "apdba", "password": "B64(YXBkYmE=)" }
  ]
}
```

- `accounts`는 `{user, password}` 객체들의 배열. 계정이 여러 개면 객체를 콤마로 구분해서 여러 개 나열 (`user`/`password`를 한 객체에 중복으로 넣는 게 아니라, 계정마다 별도 객체).
- `password`는 기존과 동일하게 평문 또는 `B64(...)` 인코딩 둘 다 지원.

**수정**:
- `DatabaseConfigService`: 기존 `resolve(dbId)` 로직을 `findInstance()`/`fromInstance()`로 리팩터링하고, 아래 2개 메서드 추가
  - `resolve(dbId, account)`: `account`가 비어있거나 기본 계정과 같으면 기존과 동일하게 기본 계정으로 접속. `account`가 `accounts` 배열의 `user`와 일치하면 그 계정의 user/password로 교체된 `TargetDbConfig`를 반환(host/port/sid/pool 설정은 그대로 유지). 못 찾으면 기본 계정으로 안전하게 폴백.
  - `listAccounts(dbId)`: 기본 계정 + `accounts` 배열의 계정들을 user 이름 목록으로 반환 (비밀번호는 절대 포함 안 함).
- `SqlQueryRequest`에 `account` 필드 추가.
- `SqlQueryController`: `/api/query/execute`가 `configService.resolve(dbId, account)`를 쓰도록 변경, 계정 목록 조회용 `GET /api/query/accounts?db_id=...` 신규 추가 (비밀번호 미포함).
- `index.html`: SQL 실행 화면에 "접속 계정" 드롭다운(`#sqlrunner-account-select`) 추가.
- `app.js`: `window.loadSqlRunnerAccounts()` 신규 — SQL 실행 메뉴를 클릭할 때(`switchTab`)마다 현재 선택된 DB(`window.currentDbId`) 기준으로 `/api/query/accounts`를 호출해 드롭다운을 채움. SQL 실행 시 선택된 계정을 `account` 파라미터로 함께 전송.
- 커넥션 풀(`OracleConnectionPoolManager`)은 원래부터 풀 키가 `db_id + user + dsn` 조합이라, 계정별로 별도 HikariCP 풀이 자동으로 분리됨 (별도 수정 불필요).
- 테스트 계정으로 `patent_integ_1`에 `apdba`/`apdba`를 `databases.json`, `dist/databases.json` 양쪽에 추가.

**검증**: `GET /api/query/accounts?db_id=patent_integ_1` → `["dbagent_user", "apdba"]` 확인. `POST /api/query/execute`에 `account: "apdba"`로 `select user from dual` 실행 → 실제 Oracle 접속 성공, `APDBA` 반환 확인.
