# 폐쇄망 적용 절차 (2026-09-22)

> 대상 커밋: main `2a379d3` / AIX `a286740` (+ 문서 커밋 `2d4c711` / `d4f43de`)
> 적용 내용: **DB 파일 `db_config/` 분리 + 커넥션 풀 10 통일 + 비밀번호 AES 암호화(6단계)**
>
> 폐쇄망에서는 질문할 곳이 없으므로, **각 단계의 기대 출력과 어긋났을 때의 대처까지** 적어 둔다.
> 판단이 서지 않으면 **중단하고 롤백**(9절)하는 것이 항상 안전한 선택이다.

---

## 0. 시작 전 확인

- [ ] 반입한 `dist-aix` 폴더가 있는가 (jar + `application.properties` + `start-aix.sh`/`stop-aix.sh`)
  - **주의**: `dist-aix/`는 git 제외 대상이라 pull로 안 따라온다. 개발 PC에서 물리적으로 가져와야 한다.
- [ ] 현재 서버에서 앱이 **정상 동작 중**인지 (업그레이드 전 기준선 확인 - 원래 안 되던 게 있으면 그건 이번 작업 탓이 아니다)
- [ ] 되돌릴 시간 여유가 있는가 (롤백 포함 30분 정도 여유 권장)

---

## 1. 앱 정지

```bash
./stop-aix.sh
```

**확인**: 프로세스가 실제로 없어야 한다.
```bash
ps -ef | grep dbagent-java | grep -v grep
```
→ 아무것도 안 나와야 정상. 남아 있으면 PID로 `kill` 후 다시 확인.

---

## 2. 백업 (건너뛰지 말 것)

**이번 업그레이드는 DB 파일 내용을 바꾼다(비밀번호 암호화). 구 jar로 되돌리면 암호문을 평문으로
오인해 인증 실패하므로, 롤백은 반드시 "구 jar + 백업 DB 파일" 한 세트여야 한다.**

```bash
BK=backup_$(date +%Y%m%d-%H%M)
mkdir -p "$BK"
cp -p users.mv.db metrics.mv.db dbconfig.mv.db "$BK"/ 2>/dev/null
cp -p dbagent-java-0.1.0.jar "$BK"/
cp -p application.properties "$BK"/
ls -la "$BK"/
```

**확인**: `.mv.db` 3개 + jar + properties가 백업 폴더에 있어야 한다.
- `metrics.mv.db`나 `dbconfig.mv.db`가 없다면? → 그 서버에서 아직 생성 안 된 것이니 정상. 있는 것만 백업.
- **`users.mv.db`는 로그인 계정이 들어 있으므로 반드시 있어야 한다.** 없으면 위치를 잘못 찾은 것이니 중단하고 확인할 것.

---

## 3. `db_config/` 디렉터리 생성 + DB 파일 이동

```bash
mkdir -p db_config
mv users.mv.db metrics.mv.db dbconfig.mv.db db_config/ 2>/dev/null
ls -la db_config/
```

**확인**: 있던 `.mv.db` 파일들이 `db_config/` 안에 있어야 한다.

> **왜 이동이 필요한가**: 프로퍼티만 바꾸고 파일을 안 옮기면, 앱이 `db_config/`에서 파일을 못 찾아
> **새 빈 DB를 만든다.** 오류는 안 나지만 로그인 계정·등록 DB 목록·메트릭 이력이 전부 안 보이는
> 상태로 뜬다. 그 경우 앱을 내리고 파일을 제대로 옮긴 뒤 다시 띄우면 복구된다.

---

## 4. `application.properties` 수정

아래 3줄을 추가한다(기존 줄은 그대로 두고 추가만).

```properties
spring.datasource.url=jdbc:h2:file:./db_config/users
dbagent.metrics.datasource.url=jdbc:h2:file:./db_config/metrics
dbagent.dbconfig.datasource.url=jdbc:h2:file:./db_config/dbconfig;DEFRAG_ALWAYS=TRUE
```

**주의할 점**
- 세 번째 줄의 **`;DEFRAG_ALWAYS=TRUE`를 빠뜨리지 말 것.** 이게 없으면 암호화 후에도 예전 비밀번호
  값이 파일에 남는다(8절 참고).
- 같은 키가 이미 다른 값으로 있으면 **기존 줄을 지우고** 위 값으로 바꿀 것(중복되면 뒤에 오는 값이 이긴다).
- 키 파일 관련 설정은 **적지 않아도 된다** — 자동 생성된다.

**확인**:
```bash
grep -n "datasource.url" application.properties
```
→ 위 3줄이 보이고, dbconfig 줄에 `DEFRAG_ALWAYS=TRUE`가 붙어 있어야 한다.

---

## 5. 새 jar 교체

```bash
cp -p dbagent-java-0.1.0.jar dbagent-java-0.1.0.jar.bak-$(date +%Y%m%d-%H%M)   # 2절에서 이미 했으면 생략 가능
cp -f <반입한 새 jar 경로> dbagent-java-0.1.0.jar
ls -la dbagent-java-0.1.0.jar
```

**확인**: 파일 크기/날짜가 새 jar의 것이어야 한다(약 34MB).

---

## 6. 첫 기동 — 암호화 수행

```bash
./start-aix.sh
```

로그 확인(파일명은 `dbagent-java.out.log`):
```bash
grep -E "마스터 키|재암호화|Started DbAgentApplication" dbagent-java.out.log | tail -10
```

### 기대 출력

```
INFO  ... MasterKeyProvider : 마스터 키 파일이 없어 새로 생성했습니다: /경로/dbagent.key - ...
INFO  ... MasterKeyProvider : 마스터 키 로드 완료 (활성 키 k1, 보유 키 1개, 256bit)
INFO  ... DatabaseConfigService : 비밀번호 재암호화: 변경 N건, 실패 0건 (활성 키 k1)
WARN  ... DatabaseConfigService : 재암호화가 방금 끝났습니다. 예전 비밀번호 값이 ... 아직 남아 있습니다 ...
INFO  ... DbAgentApplication : Started DbAgentApplication in X seconds
```

### 판단 기준

| 로그 | 의미 | 조치 |
|---|---|---|
| `256bit` | AES-256 사용 | 정상 |
| `128bit` + `AES-256을 허용하지 않습니다` WARN | IBM J9 정책 파일 미설치 → 자동 폴백 | **정상 동작. 그대로 진행해도 된다.** 이 값을 기록해 둘 것(개발 PC는 256이었음) |
| `재암호화: 변경 N건, 실패 0건` | N개 인스턴스 암호화 완료 | 정상 |
| `실패 M건` (M>0) | 일부 인스턴스 처리 실패 | 7절 참고 |
| `재암호화가 방금 끝났습니다 ... 아직 남아 있습니다` WARN | 예상된 안내 | 정상 (8절에서 해소됨) |
| `APPLICATION FAILED TO START` | 기동 실패 | 9절 롤백 |

---

## 7. `실패 M건`이 나온 경우

로그에 **어느 인스턴스인지** 찍힌다:
```
ERROR ... 인스턴스 'xxx'의 비밀번호를 현재 키로 처리할 수 없습니다 - 관리 화면에서 비밀번호를 다시 입력해야 합니다.
```

- 실패한 인스턴스는 **원본 값이 보존**되며, 다른 인스턴스는 정상 동작한다.
- 조치: 관리 화면(DB 관리)에서 **그 인스턴스의 비밀번호만 다시 입력**하고 저장하면 암호화된다.
- 원인은 대개 저장돼 있던 값이 손상됐거나(깨진 base64), 다른 서버에서 만든 값인 경우다.
- **전체 롤백까지 갈 필요는 없다.**

---

## 8. 정상 종료 → 재기동 (예전 값 잔재 삭제)

**이 단계를 건너뛰면 예전 비밀번호 값이 `dbconfig.mv.db` 안에 남는다.** B64는 키 없이 디코딩되므로,
그 상태로 파일이 유출되면 암호화한 의미가 없어진다.

```bash
./stop-aix.sh
./start-aix.sh
```

- `stop-aix.sh`는 SIGTERM이라 JVM 종료 훅이 돌고, 그때 H2가 파일을 재기록하며 옛 값이 지워진다.
- **강제 종료(`kill -9`)를 쓰면 정리되지 않는다.** 반드시 `stop-aix.sh`를 쓸 것.

**확인** (선택): 예전 값이 지워졌는지 눈으로 보고 싶으면
```bash
grep -c "B64(" db_config/dbconfig.mv.db
```
→ `0`이면 정리 완료. 0이 아니면 정상 종료가 안 된 것이니 `stop-aix.sh`로 다시 내렸다 올릴 것.

---

## 9. 문제 발생 시 롤백

**기준: 앱이 안 뜨거나, 로그인이 안 되거나, 등록된 DB 목록이 비어 보이면 롤백.**

```bash
./stop-aix.sh
BK=<2절에서 만든 백업 폴더>
cp -f "$BK"/dbagent-java-0.1.0.jar ./dbagent-java-0.1.0.jar     # 구 jar
cp -f "$BK"/users.mv.db "$BK"/metrics.mv.db "$BK"/dbconfig.mv.db ./   # 원위치(db_config/ 아님)
cp -f "$BK"/application.properties ./
rm -f dbagent.key
rm -rf db_config
./start-aix.sh
```

- **jar만 되돌리면 안 된다.** 구 jar는 암호화된 값(`ENC(...)`)을 평문으로 오인해 접속이 전부 실패한다.
  반드시 **DB 파일도 같이** 백업본으로 되돌릴 것.
- `application.properties`도 되돌려야 경로가 맞는다(`db_config/` 설정이 남아 있으면 안 됨).

---

## 10. 검증

- [ ] 로그인 (계정이 살아있는지 = `users.mv.db` 정상)
- [ ] 좌측 DB 목록에 인스턴스가 다 보이는지 (= `dbconfig.mv.db` 정상)
- [ ] 인스턴스 하나 선택 → 대시보드에 실제 데이터가 뜨는지 (= **복호화된 비밀번호로 실접속 성공**)
- [ ] 추이 차트에 과거 데이터가 보이는지 (= `metrics.mv.db` 정상)
- [ ] 관리 화면에서 DB 하나를 **비밀번호 빈칸으로 저장** → 접속이 계속 되는지
      (= "빈 비밀번호는 기존 값 유지" 계약 정상)
- [ ] `dbagent-java.err.log`가 비어 있는지

---

## 11. 마무리 — 키 파일 보관 (중요)

첫 기동 때 앱 폴더에 `dbagent.key`가 자동 생성됐다.

```bash
ls -la dbagent.key
```

**이 파일을 잃으면 암호화된 비밀번호는 복구할 수 없다** (관리 화면에서 전부 재입력해야 함).

- [ ] 권한이 `-rw-------`(600)인지 확인 (앱이 자동 설정하지만 확인할 것)
- [ ] **`db_config/`와 다른 장소에 사본 1부 보관** — 같은 백업 매체에 두면 암호화 의미가 사라진다
- [ ] (선택, 운영 전환 시) 앱 폴더 밖으로 옮기고 경로 지정:
  ```bash
  mkdir -p ~/.dbagent && mv dbagent.key ~/.dbagent/ && chmod 600 ~/.dbagent/dbagent.key
  ```
  `application.properties`에 추가:
  ```properties
  dbagent.security.key-file=/home/<계정>/.dbagent/dbagent.key
  ```
  옮긴 뒤에는 `stop-aix.sh` → `start-aix.sh`로 재기동해서 정상 동작을 확인할 것.

---

## 12. 이번에 확인해야 할 것 (개발 PC에서 확인 불가했던 항목)

- [ ] **AES 키 길이**: 6절 로그의 `256bit` / `128bit` 중 어느 쪽인지 기록
      (개발 PC는 256이었다. 128이면 IBM J9에 무제한 정책 파일이 없다는 뜻이며, 그래도 정상 동작한다)
- [ ] **Oracle 인스턴스 실접속**: 개발 PC의 Oracle은 host가 비어 있어 검증하지 못했다.
      폐쇄망의 실제 Oracle 인스턴스로 대시보드가 뜨는지 확인 필요
- [ ] 6개 엔진 중 이 서버에서 쓰는 것들 접속 확인

---

## 13. 함께 처리하면 좋은 잔여 항목 (4~5단계부터 남아 있던 것)

- [ ] 실서버에 남아 있는 `oracle.env` 삭제 (앱이 더 이상 읽지 않음)
- [ ] 실서버에 남아 있는 `databases.json` 삭제
      (`dbconfig.mv.db`로 이미 이관됐고 다시 읽지 않는다. 다만 **평문/B64 비밀번호가 들어 있는
      파일이므로 지우는 편이 안전하다**)

---

# 부록 A. 폐쇄망 안의 Windows(main, SQLite) 환경

같은 폐쇄망이라도 **AIX 서버가 아니라 Windows에서 main 빌드를 쓰는 환경**이라면 절차가 조금 다르다.
파일명(`.db` vs `.mv.db`), 프로퍼티, 그리고 **잔재 정리 방식**이 다르다.

## A-1. 다른 점 요약

| | AIX (H2) | Windows (main, SQLite) |
|---|---|---|
| DB 파일 | `users.mv.db` 등 | `users.db` + `users.db-wal` / `-shm` |
| 정지/기동 | `stop-aix.sh` / `start-aix.sh` | `stop.ps1` / `start.ps1` |
| 예전 값 잔재 정리 | **정상 종료 시**(8절 stop→start 필요) | **기동 중 자동**(VACUUM) — **stop/start 반복 불필요** |
| dbconfig URL | `;DEFRAG_ALWAYS=TRUE` 필요 | 불필요 |

## A-2. 절차

```powershell
# 1) 정지
.\stop.ps1

# 2) 백업 (-wal/-shm 도 같이)
$BK = "backup_" + (Get-Date -Format "yyyyMMdd-HHmm")
New-Item -ItemType Directory $BK | Out-Null
Copy-Item users.db*, metrics.db*, dbconfig.db*, dbagent-java-0.1.0.jar, application.properties $BK -ErrorAction SilentlyContinue
Get-ChildItem $BK

# 3) db_config 생성 + 이동 (-wal/-shm 포함)
New-Item -ItemType Directory db_config -Force | Out-Null
Move-Item users.db*, metrics.db*, dbconfig.db* db_config\ -ErrorAction SilentlyContinue
Get-ChildItem db_config
```

**4) `application.properties`에 3줄 추가** (AIX와 값이 다르다 — `busy_timeout`, `DEFRAG_ALWAYS` 없음)
```properties
spring.datasource.url=jdbc:sqlite:db_config/users.db?busy_timeout=5000
dbagent.metrics.datasource.url=jdbc:sqlite:db_config/metrics.db?busy_timeout=5000
dbagent.dbconfig.datasource.url=jdbc:sqlite:db_config/dbconfig.db?busy_timeout=5000
```

**5) 새 jar 교체 → 기동**
```powershell
.\start.ps1
```

**6) 로그 확인** — 본문 6절과 같되, 아래 한 줄이 **추가로** 보여야 한다:
```
INFO ... 재암호화 전 값이 남아있던 페이지를 정리했습니다(WAL 체크포인트 + VACUUM).
```
이 줄이 보이면 잔재 정리까지 끝난 것이라 **stop/start 반복이 필요 없다.**

**7) 이후** — 본문 7절(실패 건 처리), 10절(검증), 11절(키 보관)은 동일하게 적용.
단 키 파일 경로 지정 예시는 Windows 경로로:
```properties
dbagent.security.key-file=C:/ProgramData/DBAgent/dbagent.key
```

## A-3. 롤백 (Windows)

```powershell
.\stop.ps1
Copy-Item "$BK\dbagent-java-0.1.0.jar" .\ -Force
Copy-Item "$BK\*.db","$BK\*.db-wal","$BK\*.db-shm" .\ -Force -ErrorAction SilentlyContinue
Copy-Item "$BK\application.properties" .\ -Force
Remove-Item dbagent.key -ErrorAction SilentlyContinue
Remove-Item db_config -Recurse -Force
.\start.ps1
```

> **주의**: Windows `stop.ps1`은 강제 종료(`Stop-Process -Force`)라 JVM 종료 훅이 돌지 않는다.
> main(SQLite)은 기동 중에 VACUUM으로 정리하므로 상관없지만, **이 스크립트로는 H2의 종료 시
> 정리가 동작하지 않는다**는 점은 기억해 둘 것(AIX 환경에서 Windows 스크립트를 쓰지 말 것).
