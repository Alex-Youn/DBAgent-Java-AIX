# 폐쇄망 적용 절차 (2026-09-25) — 배포 ①

> 대상 커밋: main `11206d3` / AIX `ba5aaac` (전체 작업순서 A단계: A1~A6)
> 적용 내용: **jar 교체만** (DB 파일·설정 파일 변경 없음)
>
> | 항목 | 요청 원본 | 내용 |
> |---|---|---|
> | A1 | — | 장애 판정 쿼리가 매번 오류 후 재시도하던 문제 정리 (판정 결과는 이전과 같음) |
> | A2 | — | TM Lock 장애 판정 기준 60초를 프로퍼티로 조정 가능 (`dbagent.monitor.tm-holder-last-call-et-seconds`, 기본 60) |
> | A3 | 1-1 | Current Session 차트의 **CPU 코어 기준선**을 `NUM_CPU_CORES`로 변경 (CPU 사용률 %는 기존 그대로) |
> | A4 | 7-1 | 테이블스페이스 화면에서 DB를 바꿀 때 이전 DB 합계가 남던 문제 |
> | A5 | 1-5 | Current Session에서 DB를 바꿀 때 이전 DB 그래프·세션이 남던 문제 |
> | A6 | 1-2 | Current Session 첫 로딩 검은 화면 → 그래프 틀 + "불러오는 중…" 표시, 느린 원인 진단 로그 |
>
> 이번 배포는 **DB 파일을 바꾸지 않으므로** 롤백은 jar만 되돌리면 된다(5절).

---

## 0. 시작 전 확인

- [ ] 반입한 `dist-aix/dbagent-java-0.1.0.jar`가 있는가 (크기 약 34.3MB, 2026-09-25 17:4x 빌드)
  - `dist-aix/`는 git 제외 대상이라 pull로 안 따라온다. 개발 PC에서 jar를 물리적으로 가져와야 한다.
- [ ] 현재 서버에서 앱이 정상 동작 중인지 (기준선 확인)

## 1. 앱 정지 → jar 백업 → 교체

```bash
./stop-aix.sh
ps -ef | grep dbagent-java | grep -v grep     # 아무것도 안 나와야 정상
cp -p dbagent-java-0.1.0.jar dbagent-java-0.1.0.jar.bak-$(date +%Y%m%d-%H%M)
cp <반입한 jar> dbagent-java-0.1.0.jar
./start-aix.sh
```

- `application.properties`는 **수정하지 않아도 된다.** 새 프로퍼티는 없으면 기존과 같은 기본값(60초)으로 동작한다.
- (선택) 장애 판정 기준을 바꾸고 싶을 때만 `application.properties`에 한 줄 추가 후 재기동:
  ```properties
  dbagent.monitor.tm-holder-last-call-et-seconds=60
  ```
  숫자가 아닌 값을 넣어도 기동은 되고, 로그에 `... is not a positive integer - using 60s` 경고가 남는다.

## 2. 기동 확인

- 브라우저에서 로그인 → 화면이 정상으로 뜨는지
- 캐시 문제 확인: 개발자도구(F12) → Network에서 `app.js?v=161`로 불러오는지 (AIX). 옛 번호면 Ctrl+F5

## 3. 기능 확인 (각 1분 이내)

| 확인 | 방법 | 기대 결과 |
|---|---|---|
| A3 코어 기준선 | Current Session → 좌측 그래프 위 KPI "CPU 코어 수" | 실제 서버 **코어 수**(NUM_CPU_CORES, 논리 CPU 수가 아님) |
| A4 테이블스페이스 | 테이블스페이스 화면에서 좌측 DB를 다른 DB로 클릭 | 클릭 즉시 합계 3개가 `--`로 바뀌었다가 새 DB 값으로 채워짐 |
| A5 DB 전환 | Current Session 화면에서 좌측 DB를 다른 DB로 클릭 | 두 그래프가 즉시 비워지고 "불러오는 중…" → 새 DB 그래프 |
| A6 첫 로딩 | 로그인 후(또는 RDB/FO 갔다가 돌아와서) Current Session 진입 | 데이터가 오기 전에도 그래프 축·격자 + "불러오는 중…" (검은 빈 화면 아님) |

## 4. 느린 원인 확인 (A6, 꼭 부탁드림)

Current Session 첫 로딩이 여전히 느리면 **F12 → Console** 탭에서 아래 형식의 줄을 찾는다(2초 이상 걸린 응답만 찍힘).

```
[DBAgent] ash_activity db=... 전체 8200ms / 서버 처리(커넥션+DB 조회) 7900ms / 네트워크·대기 300ms
[DBAgent] ash_top_sql  db=... 전체 9100ms / 서버 처리(커넥션+DB 조회) 8800ms / 네트워크·대기 300ms
```

- **서버 처리가 대부분** → DB 쪽 ASH 조회가 느린 것 (쿼리 튜닝/조회 구간 축소 검토 대상)
- **네트워크·대기가 대부분** → 브라우저와 앱 서버 사이 구간(네트워크, 프록시, 동시 요청 대기)
- 서버 처리가 3초 이상이면 서버 로그(`dbagent-java.out.log`)에도 `ash_activity slow for db_id=...: N ms`가 남는다.

**이 결과(숫자 두세 줄)를 개발 쪽에 전달해 주면 다음 조치를 정할 수 있다.**

## 5. 롤백 (문제가 있을 때)

DB 파일을 바꾸지 않았으므로 jar만 되돌리면 된다.

```bash
./stop-aix.sh
cp -p dbagent-java-0.1.0.jar.bak-<1절에서 만든 시각> dbagent-java-0.1.0.jar
./start-aix.sh
```

---

### 개발 PC 쪽 배포 기록 (참고)

- 빌드: 두 프로젝트 `mvn clean package` (2026-09-25 17:39)
- 배포: main `dist/`, AIX `dist/`, AIX `dist-aix/` — 기존 jar는 각 폴더에 `dbagent-java-0.1.0.jar.bak-20260925-1739`로 백업
- 확인: 배포된 jar를 번들 런타임으로 기동해 화면 버전(main 173 / AIX 161), 장애 판정, 코어 16(로컬 XE), 조회 시간, 테이블스페이스 조회 정상, ERROR 0건
- AIX 런타임(H2) 테스트: 실제 블로킹 세션 판정, 테이블스페이스·Current Session DB 전환, 첫 로딩 스켈레톤 모두 통과
