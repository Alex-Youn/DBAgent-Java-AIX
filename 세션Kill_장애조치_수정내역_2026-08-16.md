# DBAgent-Java 세션 Kill / 장애조치 수정 내역 (2026-08-16)

이 프로젝트(`DBAgent-Java`) 쪽에서 진행한 작업 기록입니다. 동일한 프론트엔드 버그와 장애조치 기능은 원본 Python 프로젝트(`C:\AI-Projects\DBAgent`)에도 이식했고, 그쪽 작업 기록은 그 프로젝트 폴더의 `세션Kill_장애조치_수정내역_2026-08-16.md`에 따로 정리했습니다.

## 수정한 파일

- `src/main/resources/static/app.js`
- `src/main/resources/static/index.html`
- `dist/dbagent-java-0.1.0.jar` (재빌드 반영, 배포 산출물)

## 1. kill_session에 `db_id` 파라미터 누락 버그 수정

**증상**: Dashboard / Current Session / Lock Holder-Waiter Tree, 세 메뉴 모두에서 세션 Kill 시 `oracle-default - Connection is not available, request timed out after 3xxx ms (total=0, active=0, idle=0, waiting=0)` 에러 발생.

**원인**: `app.js`의 kill_session fetch 2곳(TM Lock 탭 전용 핸들러, Dashboard/Session 탭이 공유하는 `killSessions()` 함수)에 `db_id`가 안 붙어 있었음. `MonitorController.killSession`이 `db_id`가 없으면 `DatabaseConfigService.resolve(null)` → 실제 화면에서 보고 있던 DB가 아니라 `oracle.env`의 SYS/기본 DB(`oracle-default` 풀)로 조용히 폴백됨. 그 DB가 접속 불가 상태라 3초 타임아웃 에러로 나타남 (관련 배경: `SCAFFOLDING.md` 4.6번 항목의 "db_id 중복 파라미터로 인한 폴백" 버그와 같은 계열의 문제).

**수정**: 두 fetch 호출 모두 `?db_id=${window.currentDbId || ""}` 추가. Dashboard/Session은 같은 함수를 공유해서 한 번의 수정으로 두 메뉴 다 해결, TM Lock 탭은 별도 핸들러라 따로 수정.

**검증**: `db_id` 없이 호출 → 동일한 에러 재현 확인 → `db_id` 포함 호출 → 정상 kill 확인. 실제 Docker Oracle(`oracle19c` 컨테이너)에 holder/waiter 락을 만들어 세 메뉴 전부 실제 동작 확인 (일부는 사용자가 브라우저에서 직접 클릭해서 검증).

## 2. 로컬 Maven 부재 대응

`mvn`이 시스템 PATH/환경변수 어디에도 없어서 재빌드가 막혀있던 상태였음 (`SCAFFOLDING.md`에 적힌 `C:\Java\apache-maven-3.9.16`은 이 머신에 더 이상 존재하지 않음). 이전 세션 스크래치패드에서 다운로드해뒀던 Apache Maven 3.9.9를 프로젝트 폴더 안에 `maven39/`로 영구 복사해서, `jdk17/`와 마찬가지로 프로젝트 안에 툴체인을 동봉하는 기존 방식을 그대로 따름.

재빌드 명령:
```powershell
$env:JAVA_HOME = "C:\AI-Projects\DBAgent-Java\jdk17"
$mvnHome = "C:\AI-Projects\DBAgent-Java\maven39"
$env:Path = "$mvnHome\bin;$env:JAVA_HOME\bin;$env:Path"
mvn -q -DskipTests package
```
빌드 전에는 `stop.ps1`로 서버를 먼저 내려야 함 (jar 파일이 실행 중 프로세스에 잠겨서 repackage 단계가 실패함).

## 3. TM Lock 탭 체크박스가 자동새로고침에 풀리는 버그 수정

**증상**: Lock Holder/Waiter Tree 탭에서 체크 후 Kill을 누르기 전에 자동새로고침(기본 5초)이 한 번 돌면 체크가 풀려서 "Kill할 세션을 선택해주세요" 메시지가 뜸.

**원인**: `fetchTMLocks()`가 `tmlockTbody.innerHTML = tableHtml`로 테이블을 통째로 새로 그리는데, Session/Dashboard 탭에는 있는 "새로고침 전 체크된 sid 저장 → 새로고침 후 복원" 로직이 TM Lock 탭에만 빠져있었음.

**수정**: Session/Dashboard와 동일한 패턴으로 체크 상태 저장/복원 로직 추가.

## 4. 장애조치(TM Lock Holder 자동 Kill) 버튼 신규 추가

**목적**: 대시보드 "장애발생 가능성" 게이지가 80% 이상(화면이 붉게 blink되는 것과 같은 시점)이 되면, 원인이 되는 TM 락 Holder를 버튼 한 번으로 즉시 정리.

**설계 변경 과정** (사용자 피드백 반영):
1. 처음엔 버튼 클릭 시 Lock Holder/Waiter Tree 탭으로 이동만 시키는 안전한 버전으로 구현
2. 사용자 요청으로 "탭 이동 없이 holder 세션을 바로 자동 kill"하도록 변경
3. 이후 "TM 락만 대상으로 해야 하고 TX 락은 절대 죽이면 안 된다"는 지적을 받아, holder 추출 시 `lock_type === 'TM'` 필터 추가 (TX는 정상적인 트랜잭션 경합인 경우가 많아 자동 kill 대상에서 제외해야 함 — `/api/failure_prob`도 원래부터 TM 락만 카운트하므로 그 기준과 일치시킴)

**최종 동작**:
- `index.html`: "장애발생 가능성" 카드 하단에 `dash-incident-action-btn` 버튼 추가 (평소 숨김)
- `app.js`: `percentage >= 80`일 때 표시 → 클릭 시 `/api/tmlock` 조회 → `lock_type === 'TM'`인 holder만 추출 → 건수 confirm → `/api/kill_session` 일괄 kill → 결과 alert → 대시보드 새로고침

**Lock Holder/Waiter Tree 탭 자체는 수정 안 함**: TM/TX 구분 없이 전부 보여주고, 사용자가 체크박스로 원하는 세션(holder든 waiter든, TM이든 TX든)을 자유롭게 골라 수동으로 kill할 수 있는 기존 동작 그대로 유지.

**검증**: Docker Oracle에 `lock_test_tbl` 테스트 테이블을 만들어 TX 락(row UPDATE 경합)과 TM 락(`LOCK TABLE ... EXCLUSIVE`) 두 시나리오를 각각 재현.
- TX 락 → holder의 `lock_type: "TX"` 확인, 필터에서 제외되어 자동조치 대상 아님 확인
- TM 락 → holder의 `lock_type: "TM"`, `object_waiting: "LOCK_TEST_TBL"` 확인, kill 정상 동작 및 락 즉시 해소 확인
- 실사용 테스트: 사용자가 브라우저에서 버튼을 직접 클릭 → holder 세션이 `ORA-03113`으로 정상 종료되고 waiter가 이어받아 커밋되는 것까지 확인

**임계치 임시 테스트**: 실제로 화면에서 확인하기 위해 임계치(`percentage >= 80`)를 `1`로 임시로 낮춰서 테스트한 뒤(`TEMP TEST OVERRIDE` 주석으로 명시), 테스트 완료 후 다시 `80`으로 원복 완료.

## 5. `dist/` 배포 패키지 갱신

`dist/dbagent-java-0.1.0.jar`을 위 수정사항이 모두 반영된 최신 빌드로 교체 (jar 안의 `app.js`가 소스와 동일한지 diff로 확인). `dist/databases.json`, `dist/oracle.env`는 루트와 동일해서 변경 없음. `dist/application.properties`는 값(3000ms)은 같고 주석만 더 상세한 버전이라 그대로 둠.

## 참고: 장애발생 가능성(%) 산정 기준

`/api/failure_prob`가 반환하는 count(60초 이상 유지된 TM 락 blocking 세션 수)를 아래처럼 계단식으로 % 변환 (`app.js` fetchLocks 로직, count 0개는 0%):

| count | % |
|---|---|
| 1 | 30% |
| 2 | 40% |
| 3 | 50% |
| 4 | 60% |
| 5 | 70% |
| 6 | 80% (화면 붉게 blink + 장애조치 버튼 표시 시작) |
| 7 이상 | 90% (최댓값) |

## 참고: 로컬 테스트용 Docker Oracle

`databases.json`의 `patent_integ_1`/`patent_integ_2`(SID `XE1`/`XE2`)는 `tnsnames.ora`에서 둘 다 SID=`XE`로 매핑되어 실제로는 같은 DB를 가리킴. Docker 컨테이너 `oracle19c`(이미지 `gvenzl/oracle-xe:21-full`)로 로컬에 떠 있고, `docker exec oracle19c sqlplus -S dbagent_user/dbagent01@//localhost:1521/XE`로 직접 접속 가능. 락 재현용 테이블 `lock_test_tbl(id NUMBER PK, val VARCHAR2(50))`을 만들어뒀고(재사용 가능하도록 남겨둠), holder는 `UPDATE/LOCK TABLE ...; EXEC DBMS_SESSION.SLEEP(n); COMMIT;`, waiter는 같은 대상에 동일 문장으로 TX/TM 락 상황을 재현 가능. `patent_search_*`/`patent_portal_*`는 리스너에 해당 서비스가 없어 실제 연결 불가 — 헬스체크 시 Not Alive가 정상.
