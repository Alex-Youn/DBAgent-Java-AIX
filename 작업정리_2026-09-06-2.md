# 작업정리 2026-09-06 (2차 — 집)

> 대상: `DBAgent-Java`(원본) / `DBAgent-Java-AIX`(이관본) — 두 프로젝트 동일 적용
> 상태: clean 빌드 → dist 3곳 배포 → 8006/8007 재기동 → 실화면·API 검증 완료
> 앞선 `작업정리_2026-09-06.md`(회사, CUBRID 엔진 추가)에 이어지는 문서다.

회사에서 push 한 CUBRID 작업을 집에서 pull 받아 검증하는 것으로 시작해, 09-05 문서 15절에 남아
있던 **UI 부채 정리**와 사용자 지시 5건을 처리했다. 이어서 Lock 안내 문구를 실측에 맞게 정정하고,
장애조치 버튼의 kill 범위를 검토했다(13절).

---

## 1. 시작 — pull, 빌드, 환경 맞추기

`git pull` 후 **반드시 `mvn clean package`** (09-04 사고, 09-05 문서 10절). pull 은 `src/` 와
`target/classes/` 를 같은 mtime 으로 기록해서, `clean` 없이 빌드하면 리소스 복사를 건너뛰고 옛 정적
파일이 그대로 jar 에 들어간다. 이번에도 그 습관대로 돌렸고 stale 0건을 확인했다.

### 1-1. docker DB 기동

7개 중 `cubrid-server` 만 내려가 있어 기동했다. 확인은 문서 1절대로 **`-u cubrid`** 로 해야 한다
(root 로 `cubrid broker status` 를 보면 공유메모리를 못 찾아 "not running" 이라는 거짓 결과가 나온다).

> 09-06 문서 9-1절의 "DB 컨테이너 6개면 Docker 엔진이 죽는다" 는 **회사 PC(RAM 7.7GB)** 얘기다.
> 집 PC 는 63.8GB(여유 41GB)라 7개를 모두 띄워도 여유가 충분했다.

### 1-2. CUBRID 인스턴스 등록

`databases.json` 은 커밋 대상이 아니라 환경마다 각자 등록해야 한다(09-06 문서 10-3절 3번).
`dba` 는 기본 비밀번호가 없어 `ALTER USER dba PASSWORD` 를 먼저 걸고 4개 파일
(원본/AIX × 루트/dist)에 같은 항목을 넣었다. **포트는 브로커 포트 33000**(서버 포트 1523 아님).

`dist-aix/databases.json` 은 **손대지 않았다** — 폐쇄망 AIX 전용이라 Oracle 8개만 등록돼 있고
로컬 `127.0.0.1` CUBRID 는 그 환경에서 의미가 없다.

---

## 2. 09-06 문서 11-4절 미검증 항목 해소 (Lock 탭 실화면)

회사에서 RAM 부족으로 브라우저 검증을 못 하고 남겨둔 4가지를 전부 확인했다.

| 항목 | 결과 |
|---|---|
| CUBRID Lock 탭에 대기 세션 행이 실제로 그려지는가 | ✅ `WAITER 5 / X_LOCK (SUSPENDED) / 192.52초` |
| `.rsv-lock-note`(홀더 후보 안내) 노출 | ✅ 표 위에 표시 |
| 배지·대기시간·락 모드 칸 | ✅ 정상 |
| **회귀: 기존 3엔진의 다단계 체인 트리** | ✅ 아래 표 |

**회귀는 실제 3단 체인(A←B←C)을 만들어 확인했다.** `buildLockTree` 의 루트 판정을 `!isWaiter` →
부모 유무로 바꾼 변경이 기존 엔진을 깨뜨리지 않았는지가 핵심이었다.

| 엔진 | API | 화면 |
|---|---|---|
| MySQL | 12←13, 13←14 | `HOLDER 12` → `└─ BOTH 13` → `└─ WAITER 14` |
| PostgreSQL | 52←53, 53←54 | 동일 구조 |
| MS SQL | 74←78, 78←79 | 동일 구조 |

CUBRID 는 `blocker_note` 가 지목한 홀더 후보가 **실제 홀더와 일치**했다.

---

## 3. CUBRID Lock 탭 안내 문구 분리 (커밋 `09528cc` / `39144e0`)

공통 문구가 "Holder 를 위에 Waiter 를 아래에 붙여 **트리로** 보여준다 / **맨 위 Holder 를 처리하면**
아래가 한꺼번에 풀린다" 인데, CUBRID 는 holder 를 JDBC 로 알 수 없어 트리도 체인도 만들 수 없다.
그대로 두면 화면(평평한 대기 목록)과 어긋나고, 바로 아래 서버가 주는 `blocker_note`("holder 를
알려주지 않습니다")와도 **정면으로 모순**된다.

`dbagentInitRdbSessionViews` 에 `locksNote` 를 열어 화면이 자기 문구를 넘기게 했다. 기본값은 그대로라
기존 4엔진은 영향이 없고, **여기에 `db_type` 분기를 넣지 않은 것은 탭 구성을 마크업이 정하는 것과
같은 이유**다(09-05 문서 15-3절 원칙).

---

## 4. 09-05 문서 15-2절 부채 3건 정리

### 4-1. 착수하니 문서 기록보다 심각했다

15-2절은 "Storage 패널 사용률이 항상 100%" 라고만 적혀 있었는데, 실제로는 **그 패널을 포함한
아코디언 전체가 화면에 나오지 않고 있었다.** `overviewView` 가 `display:none` 인데 그 안에서 13행이
만들어지고, `/api/rdb/storage` 와 `/api/rdb/sessions` 를 **5초마다 계속 조회**하고 있었다(15초
관측에 각 3회). 아무도 못 보는 패널을 위해 감시 대상 DB 에 계속 질의하던 셈이다.

### 4-2. 죽은 마크업 + 중복 KPI id

`overviewView` 래퍼를 걷어내고 아코디언을 `detailView`(DASHBOARD 탭)로 옮겨 **실제로 보이게** 했다.
중복이던 KPI 카드 한 벌을 없애고 `2` 접미사를 뗐다(`kpiUptime`/`kpiUptime2` 등 4쌍 × 3페이지).

목업 행 **System Charts**(CPU/Disk IO 등)는 삭제했다 — OS 레벨 지표라 SQL 소스가 없고 docker stats
방식도 이미 배제된 상태여서, 화면에 나오게 된 이상 영영 안 채워질 가짜 패널을 보여줄 이유가 없다.

> **CUBRID 페이지는 이 부채가 없었다.** 09-06 에 새로 만들 때 물려받지 않았다(그 문서 5-1절).

### 4-3. Storage 사용률 항상 100%

세 엔진 모두 `total_mb=used_mb, free_mb=0, used_pct=100` 을 하드코딩하고 있었다. 15-3절 원칙대로
**모르는 값은 null → 화면 '-'** 로 바꿨다(용량 조회 탭이 이미 쓰는 방식).

> **MS SQL 은 값의 의미 자체가 틀려 있었다.** `sys.master_files.size` 는 파일에 **할당된** 크기인데
> 이를 `used_mb` 로 담고 있었다. 실제 의미대로 `total_mb` 에 담고 사용량은 null 로 정정했다
> (표 머리글도 "사용량 (MB)" → "할당량 (MB)").

| 엔진 | 결과 |
|---|---|
| MySQL | 전체 사용량 1.67 MB / 할당량·사용률 `-` |
| PostgreSQL | 전체 사용량 20.23 MB / 할당량·사용률 `-` |
| MS SQL | 전체 할당량 144 MB / 사용량·사용률 `-` |

`-` 옆에는 왜 계산할 수 없는지와 "용량 조회 탭에서 보라"는 안내를 tooltip 으로 달았다.

### 4-4. MS SQL `log_mb` 죽은 컬럼

쿼리가 뽑아만 놓고 응답에 안 담던 컬럼을 `capacity` 응답에 추가했다(실측 `testdb log_mb=72.0`).
표 컬럼은 `CAPACITY_COLUMNS` 고정 목록이라 화면은 그대로이고, **문서 6.5(로그 용량)에서 바로 쓸 수
있는 상태**가 됐다.

---

## 5. 유형 A 중복 7건 정리 — 같은 탭에 같은 제목이 두 개

아코디언을 노출시키자 **한 탭 안에 같은 주제가 두 군데**로 갈려 보였다. 특히 MySQL/PostgreSQL 은
`MySQL Connections`, `PostgreSQL Lock Waits` 처럼 **제목이 글자까지 똑같은 패널이 두 개** 떴다.

**진짜 중복 2건은 약한 쪽을 삭제**했다.

| 대상 | 근거 |
|---|---|
| PG `PostgreSQL Lock Waits` 시계열 패널 | 아코디언 Locks 행이 **동일한 `locksGranted`/`locksWaiting`** 을 이미 목록으로 표시 |
| MS SQL `Batch Requests (평균/초)` | 아코디언 Batch Activity 가 같은 값 + 컴파일/재컴파일까지 표시 — 삭제한 쪽이 **부분집합** |

**나머지 5건은 주제가 같은 아코디언 행으로 합쳤다**(정보 손실 없음). 함께 옮긴 비중복 패널:
MySQL `Thread Cache`→Connections, `Questions/초`→Command·Handlers, MS SQL `Page Life Expectancy`
→Memory, PG `Buffer Cache Hit Rate`→새 **Buffer Cache** 행.

결과적으로 **별도 구역이던 `detailRows` 가 완전히 사라졌다** — 09-04 Detail 탭 제거의 마지막 잔재다.

> **제목 출처를 하나로.** 시계열 패널이 자기 제목을 그리고 있어서, 아코디언 안으로 합치면 **한
> 패널에 제목이 두 줄** 뜬다. 차트가 감싸는 아코디언 패널의 `.panel-title` 을 채우도록 바꿔 출처를
> 단일화했다.

**부작용 2건도 처리**: ① 첫 칸 제목을 `현재 연결 수` 로 고치니 숫자 아래 같은 라벨이 이중 표기 →
안쪽 라벨 제거. ② 패널이 5개로 늘어 좁아지며 접속 목록 표가 잘림 → 표만 자체 가로 스크롤.

**검증**: 모든 행을 펼쳐 패널 제목을 전부 수집해 기계적으로 대조 — MySQL 31 / PG 17 / MS SQL 12개
패널, **중복 제목 0건**.

---

## 6. 글자 크기 · 시인성 개선

11.5~12px 짜리 잔글씨가 많아 읽기 어렵다는 지적. 기준을 `body` 에서 올려 크기를 따로 안 준 요소가
함께 커지게 했다.

| 대상 | 전 → 후 |
|---|---|
| 본문 기준(`body`) | 13 → **14px** |
| 패널 제목 / 데이터 표 | 11.5 → **13px** |
| 세션·Lock·용량 표(`.rsv-table`) | 12 → **13px** |
| KPI 카드 제목 | 12 → **13.5px** |
| 차트 축 눈금 / Y축 라벨 | 10 / 8 → **11 / 10px** |

**시인성**: `--text-muted` 를 `#8e8e8e` → `#a9aaae` 로 밝혀 대비를 올리고(패널 제목·표 머리글이
대부분 이 색), 표 짝수 행에 옅은 줄무늬(`rgba(255,255,255,.028)`)를 깔았다. hover 강조보다 연해서
"지금 가리키는 행" 표시를 덮지 않는다.

> **키우니 범례 표의 `avg`/`current` 칸이 잘렸다.** 패널 그리드 최소 폭을 280 → **340px** 로 넓혀
> 한 줄에 4개 → 3개가 되게 하고, 그래도 넘칠 수 있는 표는 자기 안에서 가로 스크롤되게 했다.
> 검증에서 **넘쳐서 잘리는 표 0건**을 전수 확인했다.

CUBRID 는 요청대로 **상단 해설(`.engine-note` 12px)만 그대로 두고** 나머지를 한 단계 더 키웠다
(패널 본문·상태 표 12 → 13.5px, 묶음 제목 15px). 결과적으로 해설이 본문보다 작아 위계가 생겼다.

---

## 7. 오라클 화면 상단을 RDB 와 맞추고 DB 선택 드롭다운 추가

### 7-1. zoom 90% 를 감안해야 한다

이 화면은 `.app-container` 에 **`zoom:90%`** 가 걸려 있어 CSS 값이 0.9배로 렌더된다. RDB 대시보드에는
zoom 이 없어서, **같은 숫자를 쓰면 오히려 작아진다.** 역산해서 넣어야 한다.

| 항목 | 전 | 후 | RDB |
|---|---|---|---|
| 최상단 줄 높이 | 68.6px | **54.00px** | 54.00px |
| 헤더 아이콘 | 21.6px | **18.0px** | 18.0px |
| 아이콘 간격 | 14.4px | **6.03px** | 6px |
| 상단 메뉴줄 | 92.8px(2줄) | **46.75px**(1줄) | 43px |

두꺼웠던 주된 원인은 상단 줄이 아니라 **메뉴 이름이 길어 2줄로 접히던 것**이었다. 한 줄로 고정하고
좌우 여백을 16 → 12px 로 줄였다. 1920px 창에서는 9개 메뉴가 스크롤 없이 전부 보이고, 그보다 좁으면
기존 `overflow-x:auto` 로 가로 스크롤된다(항목이 사라지지는 않는다).

### 7-2. 상단 DB 선택 드롭다운

RDB 와 같은 chevron 방식. **전환 로직을 복사하지 않고 좌측 트리의 해당 링크를 `click()`** 한다 —
그래야 위젯 초기화·세션 모니터 리셋·보고 있던 탭 유지가 한 곳에만 있고, 트리의 선택 표시도 따라온다.
Oracle 인스턴스는 host 가 비고 sid 만 있는 경우가 흔해 `· ORCL` 처럼 점만 남던 것도 고쳤다.

### 7-3. 우측 프레임 전체를 1.5mm 위로

`.top-nav` 의 `margin-top` 을 15 → **8.7px**. 이 여백이 상단 줄과 메뉴 사이의 유일한 간격이라
여기를 줄이면 아래(메뉴/인스턴스/그래프/Active 세션 목록)가 통째로 올라간다.
계산: 1.5mm = `1.5/25.4 × 96dpi` = 5.67 화면px, zoom 0.9 라 `5.67/0.9` = **6.3 CSS px**.
실측 이동량 **5.67px = 1.50mm** 로 요청값과 일치.

> ⚠️ **미해결로 남긴 것**: "메뉴와 아이콘 거리가 RDB 와 다르다"고 했는데 실측하니 오라클이 이미
> 더 좁았다(41.5 vs 75.5px). RDB 는 헤더 아래에 **필터바(Interval/Host)가 하나 더 있어** 구조적으로
> 멀다. 요청대로 위로 올려 지금은 34.8px 로 차이가 더 벌어졌다. 실제로 같게 맞추려면 반대로
> 내려야 하는데 의도와 다를 수 있어 임의로 하지 않았다.

---

## 8. 오라클 Lock Tree 를 RDB 와 같은 디자인으로

예전 화면은 Holder 행 전체를 빨강, Waiter 행 전체를 주황으로 칠했다. 색이 넓게 깔려 값이 잘 안
읽혔고, 두 화면을 오가면 같은 개념이 다른 모양으로 보였다. **역할은 배지로, 계층은 트리 가지로**
바꿨다(`.lock-badge` / `.lock-branch`, RDB 의 `.rsv-*` 와 같은 디자인).

### 8-1. 2단 응답을 다단계 트리로 잇는다

`/api/tmlock` 은 **막고 있는 세션마다 waiters 를 붙여 주는 2단 구조**다. A가 B를, B가 C를 막으면
A(waiters=[B])와 B(waiters=[C])가 각각 최상위로 나와, 예전 화면에서는 **B가 두 번**(홀더로 한 번,
A의 대기자로 한 번) 보이고 체인이라는 사실이 드러나지 않았다. waiter→holder 간선을 이어 실제 트리로
세우면 `A └─ B └─ C` 로 이어지고 중간의 B는 **BOTH** 로 표시된다.

**실측 검증** — 3단 락 체인을 만들어 확인:
`HOLDER 292` → `└─ BOTH 304` → `└─ WAITER 289` (API 도 292←304, 304←289 로 일치)

> `.data-table` 이 `table-layout:fixed` 라 14칸이 균등 분배돼 3단째 행이 `└─ …` 로 잘렸다.
> 이 표에서만 SID 칸을 210px 로 넓히고 왼쪽 정렬했다(트리는 왼쪽에서 오른쪽으로 읽힌다).

### 8-2. BOTH 의 의미와 kill 동작 (사용자 질문)

- **BOTH** = 락을 쥐고 있으면서 동시에 다른 락을 기다리는 세션 = **체인의 중간 고리**.
  2단 구조(홀더 1 + 대기자 N)에서는 BOTH 가 없고 HOLDER/WAITER 만 나온다. 3단 이상에서만 등장하며
  중간 고리 수만큼 늘어난다.
- **kill 은 "죽이는" 게 아니라 "락을 풀어 진행시키는" 것**이고, **한 단계 아래만 풀린다.**
  위 체인에서 292 를 kill 하면 304 만 진행하고, 304 가 여전히 row2 를 쥐고 있어 **289 는 계속
  대기**한다. 2단에서는 대기자끼리 서로를 막지 않으므로 홀더 하나만 kill 하면 전부 풀린다.

> ⚠️ **후속 과제**: RDB Lock 탭 상단 공통 문구 "맨 위 Holder를 처리하면 아래가 한꺼번에 풀립니다"
> 는 **3단 이상 체인에서 사실이 아니다.** 이번에 3단 체인을 실제로 만들어 보고 드러났다. 미수정.

---

## 9. 사용자 지시 5건

### 9-1. 등록된 DB 가 없을 때 버튼 안내

버튼이 안 보이는 경우가 두 가지라 **어느 쪽에 메시지를 띄울지 물어보고 진행**했다.

| 상황 | 처리 |
|---|---|
| ① 해당 계열 DB가 **아예 미등록** | 버튼 보이고 클릭 시 `등록된 오라클DB가 없습니다.` / `등록된 RDB가 없습니다.` |
| ② 등록됐지만 **이 계정에 권한 없음** | 기존대로 버튼 숨김 (2026-09-05 지시 유지) |

둘을 뭉뚱그리면 권한 때문에 못 보는 계정에게 "등록된 DB가 없다"는 **사실과 다른 안내**를 하게 된다.
구분을 위해 `dbagentCrossNavAccess` 에 **권한 필터 이전 개수** 기준의 `emptyOracle`/`emptyRdb` 를
추가했다. ORACLE 버튼은 `<a href>` 라 **`href` 를 제거**해 메시지만 뜨고 이동하지 않게 했다.

### 9-2. 신규 계정이 허용받은 DB 로 접속되게

**백엔드는 멀쩡했다.** `docker_mysql` 만 허용한 계정을 만들어 재현하니 `hidden_dbs` 저장도,
`canAccessDb` 판정도 정상이었다(허용 DB 조회 OK / 차단 DB `접근 권한이 없습니다`).

문제는 **로그인 후 무조건 `index.html`(오라클 전용 화면)에 남는다**는 점이었다. 그 계정은
좌측 트리 0건, 상단 드롭다운 0건, RDB 버튼은 "양쪽 모두 접근 가능" 규칙에 걸려 숨김, FO 버튼도
숨김 — **허용받은 DB로 갈 수단이 화면에 하나도 없는 완전한 고립 상태**였다.

- **`resolveLandingPage()`** 추가: 오라클이 하나도 허용되지 않았고 RDB 는 있으면 그 대시보드로 보낸다.
  판정 실패 시 **이동시키지 않고** 남는다 — 잘못 보내면 원인을 알 수 없는 곳에 떨어진다.
- 이미 로그인한 채 직접 진입한 경우(북마크·새로고침)를 위해 **빈 트리에 사유 + 이동 링크**를 표시.
  문구는 9-1절과 같은 기준으로 "허용된"(권한)과 "등록된"(미등록)을 구분한다.

**검증**: `t_perm`(RDB만) → MySQL 대시보드 착지 / `t_ora`(오라클만) → index.html, 트리 1건 자동 선택
/ `admin` → 기존과 동일(회귀 없음). 테스트 계정 2개는 삭제했다.

> 별건으로 발견: 신규 계정의 `fleet_overview_auto_redirect` 기본값이 **`true`** 다. 지금은
> `canFleetOverview()` 가 함께 걸려 드러나지 않지만, 나중에 FO 권한만 주면 본인이 켠 적 없는
> 자동 이동이 켜져 있게 된다. 이번 범위 밖이라 손대지 않았다.

### 9-3. Lock Tree 에서 INST_ID 제거, OS User 추가

> **`gv$` 는 두 저장소 어디에도 없다** — `src/` 전체 검색 0건. Lock 조회는 `v$lock`/`v$session`/
> `v$process` 만 쓴다. `inst_id` 도 `gv$` 때문이 아니라 **`v$instance` 를 별도로 조회해서**
> (`getInstId`) 그 상수를 SELECT 절에 끼워 넣던 값이라, 빼도 조회 방식은 달라지지 않는다.

SELECT 절의 `inst_id` 상수 2개와 `getInstId()` 호출을 제거하고 `hs.osuser`/`ws.osuser` 를 추가했다.
화면은 `INST_ID` 헤더를 빼고 맨 끝에 `OS User` 를 넣어 헤더 14칸 / 데이터 14칸 / `colspan="14"` 가
모두 일치한다.

> 세션 Kill 코드(`ALTER SYSTEM KILL SESSION 'sid,serial,@instId'`)의 `getInstId` 는 **그대로 뒀다** —
> Kill 구문 문법상 필요하고 tmlock 조회와 무관하다.

### 9-4. 우측 화면 1.5mm 위로 → 7-3절

### 9-5. Kill 버튼을 체크박스 선택과 연동

권한 변경은 파괴적이라 **물어보고 진행**했다 — `admin 전용 유지, 활성화만 구현`. 백엔드도 `isAdmin`
으로 막고 있어(403) 프론트만 풀면 403 만 받는다.

고른 세션이 없으면 버튼이 비활성이고 옆에 **선택 건수**가 뜬다. 이전에는 항상 눌렸고 아무것도 안
고른 채 누르면 경고만 떴는데, **누를 수 있다는 것 자체가 잘못된 신호**였다.

> **표는 자동 갱신마다 tbody 를 통째로 다시 그린다.** 개별 체크박스에 리스너를 달면 갱신마다
> 사라지므로 **document 레벨 이벤트 위임**으로 한 번만 걸고, 재렌더 직후 `dbagentSyncKillButtons()`
> 를 호출하도록 후크를 넣었다(총 7곳).

> ⚠️ **처음에 대시보드 것을 빠뜨렸다**(사용자 지적으로 발견). 이 화면의 Kill 버튼은 **셋**이다 —
> DASHBOARD 탭 Active Session 목록(`dash-kill-btn`), Current Session(`session-kill-btn`),
> Lock Tree(`tmlock-kill-btn`). 지시를 "세션리스트 및 lock holder/waiter" 로만 읽고 **화면 전체의
> 같은 버튼을 먼저 세어보지 않은 것**이 원인이다. 같은 종류의 UI 가 여러 곳에 있으면 전수 조사부터 할 것.

보완하면서 **표가 비는 경로**(ACTIVE 0건, TM Lock 0건)에서 버튼이 활성인 채 남는 것도 함께 고쳤다.

**검증(세 화면 전부)**: 초기 비활성 → 1개 선택 시 활성 + "선택 1건" → 전체선택 → 전체해제 시 비활성,
표 재갱신 후에도 선택·활성 유지.

---

## 10. 검증 환경 — Python CDP 로 전환

Chrome 확장은 이번에도 계정에 연결되지 않았다(**5회째**). 로컬 Chrome 을
`--remote-debugging-port=9222` 로 띄우고 CDP 를 직접 구동하는 방식은 같지만, **PowerShell 대신
Python `websocket-client`** 로 드라이버를 다시 짰더니 09-05 문서에 적어 둔 PowerShell 함정
(SendAsync 블로킹, ConvertTo-Json 멈춤, pending ReceiveAsync)이 **전부 사라졌다.**

> **함정**: `websocket-client` 는 기본으로 `Origin` 헤더를 붙이는데 Chrome 이 403 으로 거절한다.
> `create_connection(url, suppress_origin=True)` 로 해결.

계정은 **admin/admin**. 검증 중 8007 이 계속 다른 값을 보여 한참 헤맸는데, 원인은 코드가 아니라
**8007 의 admin 에 "로그인 시 Fleet Overview 자동 이동" 이 켜져 있어** 다른 페이지를 측정하고 있던
것이었다. Oracle 화면으로 직접 이동해 재측정하니 8006 과 일치했다.

---

## 11. 작업 중 낸 실수와 복구

### 11-1. AIX 고유 값을 덮어썼다 (복구 완료)

이식 스크립트에 "원래부터 다른 파일" 검사를 넣어놓고 **실제로 쓰지 않아**, `db-mgmt.html` 의 AIX
고유 값(`style.css?v=17`)을 원본 값(`v=16`)으로 덮어썼다. 09-06 문서 6-3절이 경고한 바로 그
함정이다. `git diff` 로 즉시 발견해 복원했다.

**교훈**: 두 저장소 간 이식은 **먼저 `diff --strip-trailing-cr` 로 원래 차이를 확인**하고, 차이가
있는 파일은 통째 복사하지 말 것. 이번에 확인된 고유 차이는 아래와 같다.

| 파일 | 고유 차이 |
|---|---|
| `app.js` | AIX 의 Caps Lock 구현 **213줄** |
| `MonitorService.java` | AIX 는 **Java 8** 문법(`Strings.isBlank`, `Maps.of`, `instanceof` 캐스팅) **58줄** |
| `db-mgmt.html` | `style.css?v=` 버전 1줄 |

이 세 파일은 매번 **변경 블록만 개별 패치**했다.

### 11-2. 커밋 직전 발견 — 줄바꿈만 바뀐 파일

`sed -i` 로 캐시버스터를 일괄 치환하면서 **내용 변경이 없는 5개 파일**(`fleet-overview.html`,
`fleet-overview-test.html`, `rdb-dashboard.html`, `session-list.html`, `db-mgmt.html`)이
줄바꿈(LF↔CRLF)만 바뀐 채 변경 목록에 올라왔다. 커밋 전에 `git checkout` 으로 되돌렸다.

---

## 12. 변경 파일 (15개, 두 저장소 동일)

| 파일 | 내용 |
|---|---|
| `monitor/MonitorService.java` | tmlock: inst_id 제거, osuser 추가 (9-3) |
| `rdb/MySqlMonitorService.java` | getStorage null 정정 (4-3) |
| `rdb/PostgresMonitorService.java` | getStorage null 정정 (4-3) |
| `rdb/MsSqlMonitorService.java` | getStorage 의미 정정 + capacity `log_mb` (4-3, 4-4) |
| `static/app.js` | 상단 드롭다운(7-2), 착지 화면(9-2), Lock 트리(8), Kill 버튼(9-5) |
| `static/dbagent-common.js` | `emptyOracle`/`emptyRdb` (9-1) |
| `static/rdb-session-views.js` | `locksNote`(3), 폰트·줄무늬(6) |
| `static/index.html` | 상단 줄·드롭다운(7), Lock 표 헤더(9-3), Kill 버튼(9-5) |
| `static/style.css` | 상단 정렬(7), Lock 배지(8), Kill 버튼(9-5), 1.5mm(7-3) |
| `static/mysql`/`postgres`/`mssql-overview-dashboard.html` | 부채 정리(4), 중복 통합(5), 폰트(6), 오라클 버튼(9-1) |
| `static/cubrid-overview-dashboard.html` | `locksNote`(3), 폰트(6), 오라클 버튼(9-1) |
| `static/account-mgmt.html`, `menu-visibility.html` | 캐시버스터만 |

**캐시버스터**: `app.js?v=94`, `dbagent-common.js?v=9`, `rdb-session-views.js?v=9`,
`style.css?v=32`. 전부 두 저장소 히스토리에서 미사용임을 `git log -S` 로 확인했다.

**빌드/배포**: 두 저장소 `mvn clean package` → stale 0건 확인 → dist 3곳 교체(`.bak-<타임스탬프>`)
→ 8006/8007 재기동 → 배포본에서 서빙 내용 확인.

---

## 13. Lock 안내 문구 정정 + 장애조치 버튼 검토 (코드 점검 겸)

### 13-1. 잘못된 안내 문구를 고쳤다

RDB Lock 탭 상단에 **"맨 위 Holder를 처리하면 아래가 한꺼번에 풀립니다"** 라고 적혀 있었는데
**3단 이상 체인에서 사실이 아니다.** 8-2절 실측이 근거다.

```
kill 전   : 61(INACTIVE) <- 295(ACTIVE) <- 308
61 kill 후 : 295(INACTIVE) <- 308        ← 308 은 여전히 대기, 295 가 새 홀더가 됨
```

문구를 아래로 바꾸고, "가운데 세션은 BOTH로 표시됩니다" 도 넣었다(사용자가 BOTH 의 의미를
물어봤다 = 화면에 설명이 없었다는 뜻).

> 맨 위 Holder만 종료하면 바로 아래 한 단계만 풀리고 그 아래는 계속 기다립니다 —
> 체인을 한 번에 정리하려면 Holder와 BOTH를 함께 선택해 종료하세요.

**오라클 Lock 화면에는 안내문이 아예 없었다.** 8절에서 BOTH 배지를 새로 넣어 놓고 설명을 두지
않은 상태였다. RDB 와 같은 내용을 `.lock-help` 로 추가했다.

### 13-2. kill 순서에 따른 실측 (3단 체인 A<-B<-C)

| 방법 | kill 횟수 | 결과 |
|---|---|---|
| 최상단 HOLDER 만 | 1 | ❌ 해제 안 됨 — B 가 새 홀더가 되고 C 는 계속 대기 |
| 이어서 남은 홀더까지 | 2 | ✅ 해제 |
| **BOTH 만** | **1** | ✅ **완전 해제** (C 가 row2 획득) |
| HOLDER + BOTH 동시 | 1(2세션) | ✅ 완전 해제 |

BOTH 하나로 다 풀리는 이유: B 가 막던 유일한 세션이 C 이고, A 가 막던 유일한 세션이 B 였다.
B 가 사라지면 C 는 풀리고 **A 는 아무도 막지 않는 상태**가 된다.

다만 BOTH 만 죽이면 **A 의 트랜잭션은 살아 있다.** 실측에서 A 의 상태가 계속 `INACTIVE` 였는데
이는 방치된 트랜잭션이라는 뜻이고, 다음에 같은 자원을 건드리는 세션을 또 막는다. 원인까지
없애려면 Holder 와 BOTH 를 함께 골라야 한다.

### 13-3. 장애조치 버튼 — BOTH 도 이미 포함된다 (검토 결과, 코드 변경 없음)

> 사용자 질문: *"TM 락 holder 만 모두 찾아서 죽이는 건데, 3단일 때 both 도 같이 죽여야 한다는
> 조건이 쿼리에 없는 것 같다"*

**확인 결과 이미 포함되고 있다.** `/api/tmlock` 이 `WHERE hl.block = 1` 이라 **누군가를 막고 있는
모든 세션**을 최상위 배열에 담는데, BOTH 도 (아래 세션을 막고 있으므로) 최상위 항목으로 들어온다.
장애조치는 그 배열을 `lock_type === 'TM'` 으로만 거르므로 자연히 함께 걸린다.

TX(행 락)가 아닌 **진짜 TM 락**으로 3단 체인을 만들어 확인했다(`LOCK TABLE ... IN EXCLUSIVE MODE`).

```
최상위 항목 2개
  BOTH(중간)     sid=315  lock_type=TM  ACTIVE    <- 대기 [314]
  HOLDER(최상단)  sid=306  lock_type=TM  INACTIVE  <- 대기 [315]
장애조치 필터 통과: [315, 306]   ← BOTH(315) 포함
```

핸들러와 동일한 로직을 실행하니 2건 모두 killed → **tmlock 빈 배열(체인 전체 해제)**, 대기자 C 는
죽지 않고 `C: tm_t2 획득` 으로 진행했다.

#### BOTH 도 함께 죽이는 게 맞는가 (판단)

**맞다고 본다.** 최상단만 죽이면 위 13-2 처럼 **피해자였던 BOTH 가 풀려난 뒤 새로운 INACTIVE
홀더가 되어** 체인이 남을 수 있다. 장애조치는 ①`percentage >= 80` 에서만 노출 ②admin 전용
③확인 대화상자를 거치는 **비상 조치**이므로 확실한 해소가 우선이고, 대상은 모두 미커밋
트랜잭션이라 롤백돼도 확정된 작업을 잃지 않는다.

#### 다만 노출 조건과 동작이 어긋난다 (미조치, 개선 제안)

| | 기준 |
|---|---|
| 버튼 **노출**(`failure_prob`) | `s.blocking_session IS NULL` → **최상단 홀더만** 카운트 |
| 버튼 **동작**(`tmlock`) | `hl.block = 1` → **BOTH 포함 전부** kill |

즉 **"1건 때문에 떴는데 눌러보니 2건이 죽는"** 구조다(실측: failure_prob 는 306 만, kill 은 2건).
게다가 확인 문구가 `Blocking 중인 TM Lock Holder 세션 2건...` 이라 **그중 하나가 BOTH(피해자)라는
사실이 가려진다.**

**제안**: 동작은 그대로 두고(BOTH 포함 kill 유지) **확인 대화상자에 역할 내역을 보여줄 것.**

```
Blocking 중인 TM Lock 세션 2건을 즉시 Kill 하시겠습니까?
  · 최상단 Holder 1건 (sid 306) — 락 점유의 근본 원인
  · 중간 BOTH 1건 (sid 315) — 막혀 있으면서 다른 세션도 막는 중
BOTH 는 그 자체가 대기 피해자이지만, 함께 종료하지 않으면 풀려난 뒤
새로운 Holder 가 되어 체인이 남을 수 있습니다.
```

`failure_prob` 의 카운트 기준(최상단만)은 **바꾸지 말 것.** "원인이 몇 건인가" 를 세는 지표로는
그게 맞고, BOTH 를 더하면 체인이 길수록 위험도가 부풀려진다.

> 참고: 이 버튼은 **TX(행 락)를 의도적으로 제외**한다(코드 주석에 근거 있음) - 정상 트랜잭션의
> 잠깐 경합인 경우가 대부분이라 자동 kill 대상에서 뺐고, `failure_prob` 도 같은 기준(TM만)이라
> 일관된다. 그래서 이 검증은 TX 가 아닌 TM 락으로 따로 만들어야 했다.

### 13-4. 코드 점검에서 나온 것

10개 항목을 훑어 **1건**을 고쳤다.

| 점검 | 결과 |
|---|---|
| 죽은 id 참조(`overviewView`/`detailRows`/`kpi*2`) | 3개 화면은 주석에만 남음 ✅ |
| **CUBRID 의 `kpiRowDetail`/`detailRows` 실사용** | ⚠️ **주석이 낡아서 수정** |
| `getStorage` 세 엔진 일관성 / 프론트 null 방어 | ✅ |
| `KILL_PANES` 9개 id ↔ 마크업, 체크박스 클래스 3종 | ✅ 전부 1:1 |
| `killSessions` 빈 선택 방어 | ✅ 이중(disabled + alert) |
| Lock 트리 순환/무한재귀 방어 | ✅ RDB 와 동일 |
| 디버그 잔재 / 전역 함수 중복 정의 | ✅ 0건 |
| 두 저장소 차이 전수 대조 | ✅ 전부 기존 고유 차이 |

CUBRID 주석이 *"다른 세 대시보드는 중복 카드가 남아 있는데 이 화면은 안 물려받았다"* 였는데
**4절에서 그 세 화면을 정리해 이제 사실이 아니다.** 갱신하면서, CUBRID 의 `detailRows` 는 걷어낸
"죽은 detailRows" 와 **다른 것**(누적 통계가 없어 아코디언 대신 현재 상태 표를 그리는 고유 구조)
이라는 점도 명시했다.

---

## 14. FO 링크 권한 게이팅 + 메뉴탭 굵기 맞춤

### 14-1. 좌측 상단 로고에 권한 게이팅이 없었다

> 사용자 질문: *"좌측 최상단의 DB InsightFlow 클릭 시 FO 화면으로 가는데, 이 클릭은 권한 있는
> 계정만 가능한지 확인 필요"*

**게이팅이 전혀 없었다.** FO 권한 없는 계정(`fleet_overview=false`)을 만들어 확인했다.

| | FO 버튼 | 좌측 상단 로고 |
|---|---|---|
| 오라클 화면 | ✅ 숨김 | ❌ **그대로 보이고 클릭됨** |
| RDB 화면 4종 | ✅ 숨김 | ❌ **그대로 보이고 클릭됨** |

**보안 구멍은 아니다** - 이동은 되지만 `fleet-overview.html` 자체 방어가 "Fleet Overview 접근
권한이 없습니다" 로 막고, 백엔드 `/api/fleet_status` 도 403 을 준다. 데이터는 새지 않는다.
다만 **같은 목적지로 가는 통로 둘 중 하나만 막혀 있어** "버튼은 없는데 로고를 누르니 갔다가
거부당하는" 이상한 경험이 된다.

**조치**: `dbagent-common.js` 에 `dbagentCanFleetOverview()` / `dbagentGateBrandLink(selector)` 를
추가하고 5개 화면(오라클 + RDB 4종)에서 호출한다. **로고 자체는 숨기지 않는다** - 사이드바가
허전해지므로 `href` 만 떼고 `title` 을 "Fleet Overview 접근 권한이 없습니다" 로 바꾼다.

**검증**: 권한 없는 계정 → 5개 화면 모두 `href` 제거 + 클릭해도 이동 안 함(URL 그대로), 로고는
계속 보임. admin → 5개 화면 모두 `href=fleet-overview.html` 유지. JS 에러 0건.

### 14-2. 메뉴탭 글자 굵기를 RDB 와 맞췄다 (b안)

실측 비교(오라클은 `zoom:90%` 라 화면상 크기로 환산):

| 항목 | RDB 탭 | 오라클 메뉴탭 |
|---|---|---|
| **글자 굵기** | **600** | **400** ← 확연히 가늘어 보임 |
| 글자 크기 | 16px | 16px → **화면 14.4px** |
| 아이콘 | 24px | 21.6px |
| 활성 색 | `#5794f2` | `#3987e5` |

**굵기만 600 으로 맞췄다(b안).** 크기는 손대지 않았다 - 역산해 17.8px 로 키우면 메뉴 9개의 가로
폭이 늘어 1920px 에서도 가로 스크롤이 생길 수 있기 때문이다(어제 한 줄 고정하면서 좌우 여백을
이미 16 → 12px 로 줄여 여유가 없다). 굵기만으로 시각적 차이의 대부분이 해소된다.

**검증**: `fontWeight=600`, **메뉴 넘침 0px**(1920px 창), 메뉴줄 높이 46.8px 유지.

> **c안(미채택, 나중에 필요하면)**: 크기까지 완전히 맞추려면 `font-size: 17.8px`(=16/0.9),
> 아이콘 `20px`(=18/0.9) 로 올리고 **늘어난 폭만큼 `padding` 을 더 줄여** 가로 스크롤을 막아야
> 한다. 지금 `padding: 12px 12px` 를 8px 안팎까지 줄이는 그림이다. 폭 여유를 실측으로 확인한 뒤
> 진행할 것 - 메뉴가 9개라 한 항목당 8px 만 늘어도 72px 가 는다.

---

## 15. 남은 사항

- ~~RDB Lock 탭 안내 문구 수정 필요~~ → **13-1절에서 처리 완료**(오라클 화면에도 안내 신규 추가).
- **장애조치 확인 대화상자에 역할 내역 표시** — 13-3절 제안. 동작(BOTH 포함 kill)은 그대로 두고,
  무엇을 희생하는지 관리자가 알 수 있게 Holder/BOTH 건수를 나눠 보여줄 것. **미조치.**
- **【다음 작업】 오라클에 상단 필터바 추가 + 갱신주기 ①-B 방식** — 아래 상세.
- ~~오라클 메뉴↔아이콘 간격~~ → 원인 규명 완료. **RDB 의 필터바(34px)가 그 자리를 채우고 있어서**
  생긴 구조 차이였다(빈 공간이 아니다). 아래 필터바 작업으로 함께 해소된다.
- **09-05 문서 15-1절 6절 지표 5종은 그대로 남아 있다.** 이번에 부채를 걷어내 6.1(커넥션 풀 고갈율)
  게이지를 얹을 자리가 준비됐고, 6.5(로그 용량)는 MS SQL `log_mb` 가 이미 응답에 담긴다.
  착수 순서는 **6.1(MS SQL 분모 `@@MAX_CONNECTIONS` 추가) → 6.5 → 6.2** 를 권한다.
- **신규 계정 `fleet_overview_auto_redirect` 기본값 true** (9-2절).
- **Oracle 4개 TNS 별칭 불일치** (`patent_search_1/2`, `patent_portal_1/2`) — 09-06 문서 9-2절,
  여전히 미조치. 이 PC 의 `databases.json` 에는 `patent_portal_1`/`_2` 가 **둘 다 `ORCL5`** 로
  중복 지정돼 있다(`ORCL8` 미사용).
- `databases.json`/`application.properties`/`oracle.env` 는 **커밋 대상 제외**. 이 PC 는 두 저장소
  모두 `skip-worktree` 가 걸려 있음을 확인했다(각 8개 파일).

---

## 16. 【다음 작업】 오라클 상단 필터바 + 갱신주기 ①-B

### 16-1. 배경 — 두 화면의 세로 구조가 다르다

"오라클 메뉴와 아이콘 사이 거리가 RDB 와 다르다" 의 원인을 실측으로 규명했다. **빈 공간 차이가
아니라 RDB 에만 있는 필터바(34px) 때문이다.**

```
RDB                                오라클
0.0   헤더 (54px)  아이콘◉26.5     0.0   상단줄 (54px)  아이콘◉27.0
54.0  ┃ 필터바 (34px)              54.0  ┃ (빈 여백 7.8px)
      ┃  Interval / Host
88.0  ┃ (간격 14px)
102.0 탭바 ← 아이콘에서 75.5px     61.8  메뉴바 ← 아이콘에서 35.8px
```

차이 **39.7px = 필터바 34px + 간격 차이**. 오라클에 같은 필터바를 넣으면
`54 + 34 + 14 = 102.0` 으로 **RDB 탭 위치와 정확히 일치**한다(계산 확인).

### 16-2. 지금 오라클의 갱신주기는 탭마다 흩어져 있다

| 탭 | 주기 | 초기 상태 | 출처 |
|---|---|---|---|
| DASHBOARD | **2초** | 켜짐 | `application.properties` 의 `dbagent.ui.polling-interval-ms=2000` → `/api/config` → `app.js` |
| Lock Holder/Waiter | 5초 | **꺼짐** | HTML 하드코딩 |
| Current Session | 5초 | **꺼짐** | HTML 하드코딩 |

RDB 는 필터바의 `Interval` **하나가 전 탭을 제어**하고, 선택지도 더 넓다.

| | 오라클(현재) | RDB 필터바 |
|---|---|---|
| 범위 | 1~60초(숫자 입력) | off / 5s / 10s / 30s / 1m / 5m / 직접입력(1~3600초) |
| 설정 기억 | ❌ | ✅ `localStorage('rdb_refresh_interval')` |

### 16-3. 결정 — ①-B (전역 + 탭별 덮어쓰기)

전역 통합(①-A)은 **탭마다 다른 주기를 못 준다.** 사용자 판단으로 **①-B** 를 택했다.

- 상단 필터바에 전역 `Interval` 을 두고, **기본값은 `polling-interval-ms` 에서 읽는다**
  (지금처럼 서버 설정으로 제어 가능하게 유지).
- **탭별 입력은 남긴다.** 전역값이 기본이고, 특정 탭만 다르게 쓰고 싶으면 그 탭에서 덮어쓴다.
- 주의: 컨트롤이 두 겹이라 **"어느 쪽이 이기는지" 가 화면에서 분명해야 한다.** 탭별 값이 전역과
  다를 때 그 사실을 표시하거나, "전역 따름 / 직접 지정" 을 명시적으로 고르게 하는 편이 좋다.

> ⚠️ **부하 주의**: 프로퍼티 주석에 *"1000ms proved too tight - concurrent polling across
> widgets/tabs could exhaust the connection pool"* 이 있다. 대시보드는 위젯이 많아 2초도 빠듯한데,
> Lock 조회(`v$lock` × `v$session` 조인)까지 2초로 함께 돌면 커넥션 풀 부담이 커진다.
> **전역 기본값은 5초 쪽이 안전**하고, 대시보드만 빠르게 쓰고 싶으면 탭별 덮어쓰기로 해결한다.

### 16-4. 함께 해소되는 것

- 메뉴↔아이콘 간격이 RDB 와 **구조적으로 동일**해진다(14-2절의 굵기 맞춤과 합쳐 시각적 통일 완료).
- `Host` 셀렉트도 넣으면 RDB 와 완전히 같아지지만, 오라클은 이미 상단 타이틀 드롭다운(7-2절)이
  같은 일을 한다. **RDB 쪽이 오히려 중복**(타이틀 드롭다운 + Host 셀렉트)이므로, 넣지 않거나
  RDB 의 중복을 걷어내는 방향을 함께 검토할 것.
