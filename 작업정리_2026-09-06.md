# 작업정리 2026-09-06

> 대상: `DBAgent-Java`(원본) / `DBAgent-Java-AIX`(이관본) — 두 프로젝트 동일 적용
> 상태: clean 빌드 → dist 3곳 배포 → 8006/8007 재기동 → 실화면·API 검증 완료 (**미커밋**)

09-05 문서 15절에는 "오늘은 대시보드 UI 개편 + 문서 6절 지표"를 예정으로 적어 뒀는데, 사용자가 docker
CUBRID 를 띄우면서 **CUBRID 엔진 추가**로 작업이 바뀌었다. 15절 계획은 그대로 남아 있다.

---

## 1. 시작 전 — docker CUBRID 정상 여부 확인

사용자 요청은 "기동했는데 정상인지 확인해줘" 였고, 확인 과정에서 **오해하기 쉬운 것 하나**가 나왔다.

```
$ docker exec cubrid-server cubrid broker status
++ cubrid broker is not running.        ← 거짓
```

**`docker exec` 는 root 로 실행되는데 브로커의 공유메모리는 `cubrid` 유저(uid 1000) 소유다.** root 로
조회하면 그 공유메모리를 못 찾아 "not running" 으로 나온다. 실제로는 브로커 2개(query_editor,
broker1)가 CAS 5개씩 물고 정상 동작 중이었다.

```
$ docker exec -u cubrid cubrid-server cubrid broker status   ← 이렇게 봐야 한다
```

헷갈리는 이유는 **`cubrid server status` 는 root 로도 정상 표시되기 때문**이다(그쪽은 마스터를 통해
조회한다). 상태 확인은 반드시 `-u cubrid` 로 할 것.

실측 확인 결과: 호스트에서 JDBC 접속 91ms, CREATE/INSERT/SELECT/DROP 정상, 브로커 에러 로그 0건,
컨테이너 시계도 호스트와 1초 이내 일치. **CUBRID 11.4.5 정상.**

---

## 2. 착수 전 조사 — CUBRID 는 무엇을 줄 수 있나

기존 4엔진(MySQL/MariaDB/PostgreSQL/MS SQL)은 세션 정보를 시스템 뷰로 공개한다. CUBRID 는 다르다.

**시스템 카탈로그 49개가 전부 스키마 메타데이터다.** `db_class`, `db_attribute`, `db_index` … 모니터링
뷰가 **하나도 없다.** 그 역할은 `SHOW` 구문이 한다.

### 쓸 수 있는 것 (실측 확인)

| 구문 | 얻는 것 |
|---|---|
| `SHOW TRANSACTION TABLES [WHERE ...]` | 세션 목록(46컬럼) — Tran_index, State, Isolation, Client_db_user/host/program/pid/type, Query_start_time, Tran_start_time, Waiting_for_res |
| `SHOW THREADS [WHERE ...]` | Lockwait_blocked_mode / Lockwait_start_time / Lockwait_state, 스레드 풀 구성 |
| `SHOW VOLUME HEADER OF <volid>` | 볼륨별 공간 |
| `SHOW LOG HEADER` | 버전 / 페이지 크기 / 체크포인트 LSA |
| `SHOW ACCESS STATUS` | 계정별 마지막 접속 |
| `KILL <id>` | 세션 종료 — **executeUpdate 가 실제 종료 건수를 반환**(없는 id 는 0) |

**SHOW 구문도 바인드 파라미터를 받는다**(`SHOW TRANSACTION TABLES WHERE Tran_index = ?`). 세션 상세는
문자열 조립 없이 바인딩으로 처리했다.

### 못 주는 것 — 이게 화면 구성을 갈랐다

`cubrid killtran -q <db>` CLI 는 이 컬럼들을 다 보여준다.

```
Tran index  Process id  Program name  Query time  Tran time  Wait for lock holder  SQL_ID  SQL Text
```

그런데 **`SQL Text` 와 `Wait for lock holder`(블로커 tran index)를 SQL 로 꺼내는 통로가 없다.**
`SHOW QUERY` / `SHOW SESSIONS` / `SHOW STATUS` 등 10종을 시도했고 전부 syntax error 였다. 서버는 정보를
갖고 있지만 **DB 서버 호스트에서 CLI 를 실행해야만** 나온다 — 원격 JDBC 앱이 할 수 없는 일이다.

그 밖에:

- **서버 누적 통계가 전부 0.** `SHOW EXEC STATISTICS` 도 `cubrid statdump` 도 기본 구성에서 0 이라
  QPS·버퍼 적중률 같은 KPI 를 만들 수 없다.
- **서버 기동 시각을 주는 SQL 이 없다.** `SHOW LOG HEADER` 의 Creation_time 은 DB 생성 시각이지 기동
  시각이 아니다 → 가동 시간 계열 전부 없음.
- **자기 세션을 식별할 방법이 없다.** CURRENT_TRAN_INDEX / CONNECTION_ID / SESSION_ID 전부 문법 오류라,
  다른 넷과 달리 모니터링 커넥션 자신을 목록에서 뺄 수 없다.

### 사용자와 정한 범위

이 제약들을 정리해 보고한 뒤 **"가능한 것만"** 으로 결정했다.

| 기능 | 기존 4엔진 | CUBRID |
|---|---|---|
| 대시보드 KPI | ✅ | ✅ (현재 상태 기반으로 재구성) |
| 세션 리스트 | ✅ SQL 원문 포함 | ⚠️ SQL 칸 항상 '-' |
| 세션 상세 | ✅ SQL 전문 | ⚠️ 속성만 + 사유 안내 |
| **Lock Holder/Waiter** | ✅ | ❌ **탭 자체를 두지 않음** |
| 세션 Kill | ✅ | ✅ |
| 용량 조회 | ✅ | ✅ (볼륨 기준, 할당/사용률 진짜 값) |

Lock 탭을 뺀 이유: holder 를 모르면 트리가 성립하지 않는다. 빈 blocker 칸만 늘어선 표는 "락은 있는데
원인은 모른다" 는 인상만 준다. 대기 정보는 **세션 리스트의 '대기 이벤트' 칸**으로 옮겨
`X_LOCK 대기 (20.3초)` 형태로 보여준다 — "누가 얼마나 기다리는 중" 까지는 거기서 그대로 보인다.

---

## 3. JDBC 드라이버 — Maven Central 에 없다

CUBRID 는 공식 아티팩트를 Maven Central 에 올리지 않는다(서드파티 재배포판만 있고 10.1.x 에서 멈춰
있다). 이 앱은 11.x 를 쓴다.

**서버 설치본의 jar 를 저장소에 넣고 프로젝트 로컬 Maven 저장소로 해석한다.**

```
lib/repo/cubrid/cubrid-jdbc/11.3.1.0050/cubrid-jdbc-11.3.1.0050.jar   (컨테이너 $CUBRID/jdbc/ 에서)
lib/repo/cubrid/cubrid-jdbc/11.3.1.0050/cubrid-jdbc-11.3.1.0050.pom   (직접 작성)
```

```xml
<repositories>
  <repository>
    <id>project-local</id>
    <url>file://${project.basedir}/lib/repo</url>
  </repository>
</repositories>
```

- **`<scope>system</scope>` 으로 하면 안 된다.** Spring Boot 의 repackage 가 system 스코프 jar 를 fat
  jar 에 넣지 않아 런타임에 드라이버가 사라진다.
- 이 방식이면 **폐쇄망·회사 PC 에서도 `mvn clean package` 만으로 빌드된다**(`mvn install:install-file`
  같은 수동 단계가 필요 없다).
- 드라이버는 **class 파일 버전 50 = Java 6 타깃**이라 원본(Java 17)과 AIX(Java 8)가 **같은 jar 를 공유**
  한다. MS SQL 처럼 jre8 변형을 따로 쓸 필요가 없었다.

---

## 4. 백엔드 — `CubridMonitorService`

`EngineMonitorService` 를 구현하고 `RdbMonitorController.engineFor()` 에 dispatch 를 추가했다.
**`engineFor()` 의 기본 분기가 mySqlMonitorService 라**, 여기에 cubrid 를 안 넣었으면 CUBRID 인스턴스에
MySQL 쿼리가 날아갔을 것이다.

### 4-1. 세션 리스트

`SHOW TRANSACTION TABLES WHERE Client_type <> 'SYSTEM_INTERNAL'` 로 내부 트랜잭션만 뺀다.

**"유휴 제외" 를 하지 않는다.** CUBRID 는 커넥션마다 트랜잭션 컨텍스트를 유지해 유휴 상태도
`State=ACTIVE` 로 보이기 때문에 그 값으로는 유휴/실행 중을 가를 수 없다. 대신 트랜잭션·쿼리 시작
시각으로 경과 시간을 계산해 오래된 순으로 정렬한다.

**경과 시간은 서버 시계로만 계산한다.** 시작 시각과 현재 시각을 모두 서버에서 받아
(`SELECT SYS_DATETIME FROM db_root`) 그 차이만 쓴다 — 실측 환경만 해도 DB 컨테이너는 UTC, 앱 호스트는
KST 라 앱 시계로 빼면 9시간이 어긋난다.

### 4-2. 락 대기

`SHOW THREADS WHERE Lockwait_msecs IS NOT NULL` → Tran_index 별 대기 설명으로 만들어 세션 리스트의
`wait_event` 칸에 담는다.

> ⚠️ **`Lockwait_msecs` 는 경과 시간이 아니라 타임아웃 설정값이다**(-1 = 무한 대기). 실측에서 5초를
> 기다린 세션도 -1 이었다. 대기 시간은 `Lockwait_start_time` 과 서버 현재 시각의 차이로 계산한다.

`getLockWaits()` 는 **빈 목록을 돌려준다.** 대기자만 담아 돌려주면 화면의 트리 계산이 blocker 가 null 인
행을 루트로 잘못 세워 실제와 다른 구조를 그린다.

### 4-3. 세션 Kill

`KILL <id>`. MySQL/MS SQL 과 같이 바인드 파라미터를 못 받는 명령이라 시그니처를 `long` 으로 두고
검증된 숫자만 조립한다.

**`executeUpdate` 의 반환값이 실제 종료 건수다**(실측: 없는 id → 0, 살아 있는 세션 → 1). PostgreSQL 의
`pg_terminate_backend` 가 false 를 돌려주는 것과 같은 자리 — 0 을 성공으로 세면 "성공 1건" 인데 세션은
그대로인 것처럼 보인다.

### 4-4. 용량 조회 — 넷 중 유일하게 오라클과 같은 구조

CUBRID 는 **볼륨을 미리 할당해 두고 그 안을 채워 쓴다.** 그래서 "테이블스페이스 → 데이터파일" 2단을
**"볼륨 용도 → 개별 볼륨"** 으로 그대로 대응시킬 수 있고, 할당·여유·사용률이 전부 진짜 값이다
(MySQL/PostgreSQL 처럼 null 로 비울 필요가 없다).

크기 = 섹터 수 × 섹터당 페이지 수 × 페이지 크기. 실측 검증: `128 × 64 × 16384 = 128MB` 로
`cubrid spacedb` 의 "128.0 M" 과 일치.

> **2단을 테이블별로 하지 않은 이유 — `SHOW HEAP CAPACITY OF <table>` 가 대상 테이블의 락을 기다린다.**
> 조사 중 쓰기 트랜잭션이 열려 있는 테이블에 걸어봤다가 프로브가 그대로 멈췄다. 모니터링 화면이 감시
> 대상 때문에 멈추는 것은 최악의 동작이라 채택하지 않았다.

**볼륨 열거에 함정이 있다.** 볼륨 목록을 주는 SQL 이 없어 번호를 하나씩 두드리는데,
**영구 볼륨은 0 부터 오름차순, 임시 볼륨은 32766 부터 내림차순**으로 붙는다. 0,1,2… 만 훑으면 임시
볼륨을 통째로 놓친다(이 컨테이너의 임시 볼륨이 32766 이었다).

### 4-5. 대시보드 KPI — 누적 통계가 없으니 현재 상태로

| KPI | 근거 |
|---|---|
| 활성 세션 | SHOW TRANSACTION TABLES (SYSTEM_INTERNAL 제외) |
| 락 대기 세션 | SHOW THREADS — 0 이 아니면 색이 바뀐다 |
| **워커 스레드 사용률** | SHOW THREADS — **문서 6.1 "커넥션 풀 고갈율" 의 CUBRID 대응.** CUBRID 는 커넥션당 워커 스레드를 물리므로 100% 에 닿으면 새 요청이 막힌다 |
| 볼륨 사용률(영구) | SHOW VOLUME HEADER — 임시 볼륨은 뺀다(정렬·해시 조인이 잠깐 쓰고 반납하는 공간이라 같이 더하면 사용률이 실제보다 낮게 보인다) |

가동 시간은 값이 없으므로 `uptimePct` 를 **0 이 아니라 null 로** 둔다 — 0 을 넣으면 Fleet 카드가
"가동률 0%" 로 보여 장애처럼 읽힌다.

---

## 5. 프론트엔드

### 5-1. `cubrid-overview-dashboard.html` (신규)

`mssql-overview-dashboard.html` 의 공통 셸(좌측 DB 트리, 헤더, 테마 전환, 필터바)을 그대로 쓰고 엔진별
부분만 교체했다.

- **탭 3개** — DASHBOARD / 세션 리스트 / 용량 조회. **Lock 탭 버튼을 마크업에 두지 않는다.**
- **KPI 카드는 한 벌만.** 다른 세 대시보드는 `display:none` 으로 죽어 있는 `overviewView` 안에
  `kpiUptime`/`kpiUptime2` 처럼 id 만 다른 중복 카드가 남아 있는데(09-04 Detail 탭 제거의 잔재,
  09-05 문서 15-2절), 새로 만드는 화면까지 그 부채를 물려받을 이유가 없어 걷어냈다.
- **상단 안내 문구(`.engine-note`)를 상시 노출.** SQL 칸이 비는 이유, Lock 탭이 없는 이유,
  `broker1_cub_cas_*` 로 보이는 이유를 밝힌다 — 안 밝히면 전부 조회 실패로 오해한다.

### 5-2. 탭을 엔진별로 빼는 방법 — `db_type` 분기 없이

`rdb-session-views.js` 의 `visibleTabs()` 는 버튼이 없으면 그 탭을 건너뛴다. 그래서 **버튼과 컨테이너를
두지 않는 것만으로** 탭이 빠진다.

다만 그 파일에 가드가 하나 있었다.

```js
if (!sessionsView || !locksView || !capacityView) return;   // 셋 다 요구
```

**이것 때문에 CUBRID 화면에서 세 탭이 통째로 죽었다**(locksView 가 없어 초기화 자체가 중단됨). 실측에서
`rsv-style` 미주입 / `dbagentRefreshRdbSessionViews` undefined 로 잡혔고, 이어서
`wireReloadButton(locksPane)` 이 null 참조로 터졌다. 수정:

- 가드를 `if (!sessionsView) return;` 로 (세션 리스트만 필수)
- `buildPane()` 은 컨테이너가 없으면 **null 반환**
- `guardNoDbId` / `wireKillButton` / `wireReloadButton` 이 null pane 을 건너뛴다

이제 **탭 구성이 엔진마다 달라도 프론트에 `db_type` 분기가 생기지 않는다.**

### 5-3. 세션 상세의 "SQL 없음" 구분

SQL 이 비는 이유는 두 가지고 둘을 구별해야 한다.

1. 정말로 실행 중인 SQL 이 없다 → 기존 문구 `(실행 중인 SQL이 없습니다)`
2. 그 엔진이 SQL 원문을 제공하지 않는다 → **서버가 `sql_text_note` 로 사유를 준다**

CUBRID 가 2번인데 기존 문구를 그대로 쓰면 "쿼리가 없다" 로 잘못 읽힌다. 엔진 이름으로 분기하지 않고
서버가 준 사유를 그대로 띄우고, 코드가 아니라 설명문이므로 고정폭 글꼴을 벗기고 톤을 낮췄다
(`.rsv-sql-note`).

---

## 6. 검증

Chrome 확장은 이번에도 계정에 연결되지 않아(4회째) 로컬 Chrome 을 `--remote-debugging-port=9222` +
새 `--user-data-dir` 로 띄우고 CDP 를 직접 구동했다. 원본은 8009, AIX 는 8010 으로 임시 기동했다.

### 6-1. API (원본 8009 / AIX 8010 동일 확인)

| 항목 | 결과 |
|---|---|
| `cubrid_overview` | activeSessions 3, workerBusy 1/201, volumeUsedPct **52.34** |
| `cubrid_status` | 스레드 218개(WORKER 201/SERVER 3/DAEMON 13/VACUUM_MASTER 1), 볼륨 2개, 로그헤더, 접속이력 |
| `session_list` | 세션별 계정/호스트/프로그램/상태/경과, SQL 칸 null |
| `session_detail` | 필드 15개 정상, `sql_text` null + 사유 문구 |
| `lock_waits` | `[]` (설계대로) |
| `capacity` | Permanent 67/128=52.34%, Temporary 3/128=2.34% — **`cubrid spacedb` 출력과 정확히 일치** |
| `capacity_detail` | 볼륨 파일 경로 + 할당/여유 |
| **락 대기 실측** | 실제 블로킹을 만들어 `X_LOCK 대기 (20.3초)` 표시 + `lockWaitSessions=1` 확인 |
| **Kill 실측** | 살아 있는 세션 → `killed` (목록에서 실제로 사라짐), 없는 세션 → `이미 종료된 세션입니다.`, `"abc"` → `세션 ID가 올바르지 않습니다.`(DB 까지 안 감) |

### 6-2. 실화면 (CDP)

| 항목 | 결과 |
|---|---|
| 탭 | `DASHBOARD / 세션 리스트 / 용량 조회` **3개, Lock 탭 없음** |
| KPI | 활성 3 / 락대기 0 / 워커 0.5%(1of201) / 볼륨 52.34%(67of128MB) |
| 세션 리스트 | 헤더 9개, 3행, SQL 칸 `-`, 관리자 Kill 버튼 노출 |
| 세션 상세 모달 | 필드 15개 + SQL 자리에 사유 안내(이탤릭 스타일 적용) |
| 용량 조회 | 첫 컬럼 머리글 `볼륨 용도`, 합계행 27.34% 재계산, 행 클릭 시 볼륨 드릴다운 모달 |
| **회귀: MySQL 대시보드** | 탭 4개 그대로, **Lock 탭 정상 동작**(표·카운트·Kill·새로고침 버튼) |
| 브라우저 JS 에러 | 0건 |

### 6-3. 겪은 함정

- **`SHOW HEAP CAPACITY` 가 락을 기다린다** — 프로브가 멈춰서 알았다(4-4절).
- **한글이 깨져 보였는데 앱이 아니라 콘솔이었다.** `curl | python -m json.tool` 출력이
  `\udceb뒗` 처럼 나와 소스 인코딩을 의심했지만, `\udcXX` 는 Python 의 surrogateescape 흔적이고 raw
  curl 로는 정상이었다. **`PYTHONIOENCODING=utf-8` 을 주거나 python 을 거치지 말 것.**
- **두 저장소의 정적 파일이 전부 같지는 않다.** 이식할 때 `app.js` 를 통째로 복사했다가 AIX 고유
  구현(Caps Lock 처리 209줄)을 덮어썼다. 되돌리고 의도한 1줄만 다시 적용했다.
  **복사 전에 `diff` 로 원래 차이가 있는지 확인할 것** — 커밋본 기준으로 `app.js` 209줄,
  `db-mgmt.html` 2줄(style.css 버전)이 원래부터 다르고 나머지는 동일했다.
- **AIX 저장소는 파일마다 줄바꿈이 다르다.** `RdbMonitorController.java` 는 CRLF,
  `RdbConnectionPoolManager.java` 는 LF 다. `\n` 으로 매칭하는 패치는 CRLF 파일에서 그냥 실패하므로,
  파일의 줄바꿈을 먼저 읽고 맞춰야 한다(13-9절의 CRLF 훼손 주의와 같은 맥락).
- **Bash 도구의 `cd` 가 다음 명령까지 유지되지 않는다.** 상대경로로 패치 스크립트를 돌렸다가 엉뚱한
  저장소를 건드릴 뻔했다. `git -C` 나 절대경로를 쓸 것.

---

## 7. 변경 파일

두 저장소 동일. 신규 3개(+ 벤더 jar) + 수정 15개.

| 파일 | 내용 |
|---|---|
| `lib/repo/cubrid/cubrid-jdbc/11.3.1.0050/*` | **신규** — 벤더링한 CUBRID JDBC 드라이버 + pom |
| `pom.xml` | cubrid 의존성 + `<repositories>`(project-local) |
| `rdb/CubridMonitorService.java` | **신규** — EngineMonitorService 구현 + 대시보드 전용 2종 |
| `static/cubrid-overview-dashboard.html` | **신규** — 탭 3개, CUBRID KPI, 상태 패널 |
| `rdb/RdbConnectionPoolManager.java` | cubrid JDBC URL(브로커 포트, charset=utf-8) |
| `rdb/RdbMonitorController.java` | engineFor dispatch + `/cubrid_overview`, `/cubrid_status` |
| `monitor/MonitorController.java` | fleet_status dispatch |
| `oracle/TargetDbConfig.java`, `Create/UpdateDbInstanceRequest.java` | db_type 주석에 cubrid |
| `static/rdb-session-views.js` | **pane 을 선택적으로**(5-2절), `sql_text_note` 처리(5-3절) |
| `static/dbagent-common.js` | 엔진 라벨, 대시보드 라우팅, 탭 주석 |
| `static/app.js`, `fleet-overview.html` | 대시보드 라우팅 (+ FO 엔진 라벨) |
| `static/db-mgmt.html` | CUBRID 옵션 + 기본 포트 **33000** |
| `static/account-mgmt.html` | 엔진 배지 색 |
| `static/index.html`, `menu-visibility.html`, `mysql`/`postgres`/`mssql-overview-dashboard.html` | 캐시버스터 + 라우팅 |

**캐시버스터** — `dbagent-common.js?v=7`, `rdb-session-views.js?v=6`. 두 번호 모두 두 저장소 히스토리
전체에서 미사용임을 `git log -S` 로 확인했다. **이 둘을 로드하는 페이지 전부**를 같이 올렸다.

**빌드/배포** — 두 저장소 `mvn clean package` → jar 내 정적 파일이 소스와 일치하는지 확인(09-05 문서
10절의 stale 함정) → dist 3곳 교체(`.bak-<타임스탬프>` 백업) → 8006/8007 재기동 →
`/api/rdb/cubrid_overview`, `cubrid-overview-dashboard.html`, db-mgmt 의 CUBRID 옵션까지 배포본에서 확인.

---

## 8. 남은 사항

- **인스턴스 등록 완료**(같은 날 사용자 요청). 로컬 두 인스턴스(8006/8007)에 `CUBRID` 그룹으로
  `docker_cubrid` 를 추가했다.
  ```
  종류: CUBRID / 호스트: 127.0.0.1 / 포트: 33000 / DB(sid): cubdb / 계정: dba
  ```
  **포트는 브로커 포트 33000 이다.** 서버 포트 1523 을 넣으면 접속되지 않는다.

  파일을 직접 고치지 않고 앱의 `POST /api/db_configs` 로 등록했다 - 실행 중인 앱이 databases.json 을
  직접 쓰기 때문에 손으로 고치면 덮어써지고, API 를 타야 비밀번호 인코딩·풀 관리가 기존 인스턴스와
  같은 경로로 처리된다.

  > CUBRID 의 `dba` 는 **기본 비밀번호가 없는데 등록 API 는 비밀번호를 필수로 받는다.** 그래서
  > `ALTER USER dba PASSWORD '<값>'` 으로 비밀번호를 걸고 등록했다(다른 docker 인스턴스들과 같은 구성).
  > 실제 값은 각 환경의 `databases.json` 에만 있다 - 이 문서에는 적지 않는다.

- `databases.json` / `application.properties` / `oracle.env` 는 환경별 파일이라 **커밋 대상에서 제외**
  했다. 다만 **이 PC 에는 skip-worktree 가 걸려 있지 않아** `git status` 에 계속 뜬다(09-05 문서 10절
  참고) - 커밋할 때 `src`/`pom.xml`/`lib`/문서만 골라 담을 것.
- **`dist/` 와 `target/` 도 커밋하지 않는다.** 최근 커밋들 모두 src 만 담고 있다.
- `dist-aix` 는 폐쇄망 AIX 전용이라 jar 만 교체했다(AIX 빌드 자체는 8010 임시 기동으로 4엔진+CUBRID
  API 를 검증했다).
- **CUBRID 는 대시보드 Storage 패널(`/api/rdb/storage`)이 다른 엔진과 달리 진짜 값을 준다.** 09-05 문서
  15-2절에 "Storage 패널 사용률이 항상 100%" 라고 적어 둔 부채는 MySQL/PostgreSQL/MS SQL 얘기고,
  CUBRID 는 볼륨 구조라 그 문제가 없다. UI 개편 때 참고할 것.
- **09-05 문서 15절(대시보드 UI 개편 + 문서 6절 지표 5종)은 그대로 남아 있다.** 이번에 붙인
  "워커 스레드 사용률" 이 6.1(커넥션 풀 고갈율)의 CUBRID 대응이라, 나머지 4엔진에도 같은 카드를 넣을 때
  참고할 수 있다.
- 미커밋 상태다.

---

## 9. 추가 점검 — "화면이 열리지 않는 DB" (같은 날 이어서)

사용자 지적으로 전체 DB 화면을 점검했다. **오늘 작업(CUBRID 추가)과 무관한 두 가지 문제**였다.

### 9-1. `docker_mssql` — 메모리 고갈로 사실상 응답 불능

| 호출 | 소요 |
|---|---|
| `mssql_overview` | **27초** |
| `mssql_status` / `session_list` | 55초 타임아웃 |
| `docker exec` (컨테이너 내부) | 응답 없음 |

Fleet 카드에는 `상태 조회 시간 초과`. 원인은 **호스트 메모리 고갈**이었다 — RAM 7.7GB 중 여유 0.7GB
(90.9% 사용), DB 컨테이너 6개(Oracle XE / MS SQL / MySQL / MariaDB / PostgreSQL / CUBRID) 동시 기동.
MS SQL 혼자 최소 2GB를 요구한다. 같은 시점 MySQL·PostgreSQL·CUBRID 는 정상 응답했고 MS SQL 만 무너졌다.

이 상태에서 `docker stats` / `docker stop` 조차 타임아웃했고, 결국 **Docker Desktop 엔진이 통째로
죽었다**(`npipe ... dockerDesktopLinuxEngine` 없음, WSL `docker-desktop` = Stopped). Docker Desktop 을
다시 띄우고 **Oracle + CUBRID 만 기동**하니 MS SQL 은 `DB에 연결할 수 없습니다.` 로 **즉시** 실패했다 -
느린 게 아니라 안 떠 있는 상태의 정상 응답이다. 코드 문제가 아님이 확인됐다.

> Docker Desktop 실행 파일이 기본 경로에 없다.
> `%LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe` 에 있다.

### 9-2. Oracle 4개 — TNS 별칭 없음 (기존 설정 불일치)

`patent_search_1/2`, `patent_portal_1/2` 가 `ORA-12154` 로 실패한다.

```
C:/Oracle/docker/network/Admin/tnsnames.ora 에서 SRCH1 별칭을 찾을 수 없습니다
```

tnsnames.ora 에 정의된 별칭은 **ORCL, ORCL2~ORCL8, XEPDB1, FREE** 뿐인데 이 4개의 sid 는
`SRCH1/SRCH2/PORT1/PORT2` 다. 같은 Oracle 을 쓰는 `patent_integ_1/2`(ORCL/ORCL2), `TEST01/02`
(ORCL3/ORCL4)는 정상이다. 회사 환경 기준으로 등록해 둔 항목으로 보인다 — 이 PC 에서 쓰려면 sid 를
비어 있는 별칭(ORCL5~ORCL8)으로 바꾸거나 tnsnames.ora 에 별칭을 추가해야 한다. **미조치.**

### 9-3. 그 과정에서 발견한 진짜 버그 — 워커 사용률 KPI 가 거짓말했다

CUBRID 재기동 직후 대시보드의 **워커 스레드 사용률이 98.5%** 로 떴다. 세션은 1개뿐인 유휴 서버였다.

```
WORKER 199개 : Status='RUN' 195개 / 'FREE' 4개
그중 Tran_index 가 있는 것 : 1개   <- 실제 세션 수와 일치
```

**CUBRID 에서 워커의 `Status` 는 busy/idle 지표가 아니다.** 재기동 직후에는 대부분 `RUN` 으로 있다가
오래 떠 있으면 대부분 `FREE` 로 보인다 — 같은 서버인데 가동 시간에 따라 수치가 뒤집힌다. 처음 구현이
`Status <> 'FREE'` 로 세고 있어서 "스레드 풀 고갈 직전" 이라는 잘못된 경보가 됐다. 이 앱이 계속
피해 온 "항상 100%" 류 잡음과 같은 사고다.

**수정** — 실제로 일을 받은 워커만 트랜잭션에 묶이므로 `Tran_index` 유무로 센다.

```sql
SHOW THREADS WHERE Type = 'WORKER'     -- Tran_index != null 인 것만 busy 로 집계
```

수정 후 `1 / 199 사용 중 = 0.5%` 로 실제와 일치. 두 저장소 반영 -> 재빌드 -> dist 3곳 재배포 ->
8006/8007 재기동 후 확인했다.

### 9-4. 최종 확인 (Oracle + CUBRID 만 기동한 상태)

| 화면 | 결과 |
|---|---|
| Oracle `index.html` (patent_integ_1, TEST01) | 메뉴 9개, DB 트리 8개, JS 에러 0건 |
| Oracle 4개 tablespace 조회 | 전부 정상(테이블스페이스 4개씩) |
| CUBRID 대시보드 | 탭 3개, KPI(세션4/락0/워커 0.5%/볼륨 52.34%), 볼륨표 정상 |
| CUBRID 세션 리스트 / 용량 조회 | 4건 / Permanent 52.34%·Temporary 2.34% |
| CUBRID 좌측 DB 트리 | 5개 그룹(MySQL/MariaDB/PostgreSQL/MS SQL/CUBRID) 전부 표시 |
| **내려간 DB 화면(MySQL)** | 페이지는 정상적으로 열리고 `DB에 연결할 수 없습니다.` 표시 — 멈추지 않음 |
| 브라우저 JS 에러 | 0건 |

**결론: "열리지 않는 화면" 은 없었다.** MS SQL 이 자원 고갈로 응답하지 못해 그렇게 보였고, Oracle 4개는
기존 설정 불일치다.
