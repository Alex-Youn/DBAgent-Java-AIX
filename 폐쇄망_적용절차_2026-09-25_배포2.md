# 폐쇄망 적용 절차 (2026-09-25) — 배포 ②

> 대상 커밋: main `bdd7fc0` / AIX `9dd9287` (전체 작업순서 B단계: B1~B7 + SQL 실행 읽기 전용 모드)
> 배포 ①(`폐쇄망_적용절차_2026-09-25.md`)을 아직 적용하지 않았다면 이번 jar에 ① 내용도 모두 포함되어 있으니 **이 문서만 따라 하면 된다.**
>
> 적용 내용: **jar 교체 + sqlrestapi prompt 파일 2개 복사** (DB 파일 변경 없음)
>
> | 항목 | 요청 원본 | 내용 |
> |---|---|---|
> | B1 | 4-1 | 화면 배율 90% → **80%**, Oracle·RDB 대시보드·FO 화면 모두 같은 배율로 통일 |
> | B2 | 1-4 | Current Session 세션 목록: 7행을 넘으면 세로 스크롤 |
> | B3 | 3-1 | RDB 대시보드 Client Connections: 9개째부터 세로 스크롤 (예전 20개 제한 해제) |
> | B4 | 3-2 | RDB / ORACLE / FO 이동 버튼을 같은 파란색으로 통일 |
> | B5 | 1-9 | Current Session 리프레쉬 주기 기본 **3초** (프로퍼티로 조정 가능) |
> | B6 | 5-1 | **SQL 실행 메뉴 읽기 전용 모드** — SELECT / WITH만 실행 (기본 켜짐) |
> | B7 | 6-1 | AI 챗봇: ORA 오류 코드가 있으면 오류 사전 근거 답변, 없으면 AI 일반 지식 답변 |

---

## ⚠️ 먼저 알아둘 동작 변경

1. **SQL 실행 메뉴가 기본으로 읽기 전용이 된다.** INSERT·UPDATE·DELETE·MERGE·DDL·PL/SQL·COMMIT은 "읽기 전용 모드" 메시지와 함께 실행되지 않는다.
   - 이유: 이 메뉴의 DML은 실행 즉시 자동 커밋되어 ROLLBACK으로도 되돌릴 수 없었다.
   - 데이터 변경이 꼭 필요하면 `application.properties`에 `dbagent.sql-runner.read-only=false`를 추가하고 재기동(작업 후 다시 지우기 권장).
2. **화면이 전체적으로 작아진다.** 특히 RDB 대시보드·FO 화면은 이전보다 20% 작다(원래 100%였음). 팝업 창·화면 안 모달은 아직 100%(G단계 팝업 통일 때 맞춤 예정).

## 0. 시작 전 확인

- [ ] 반입물: `dist-aix/dbagent-java-0.1.0.jar`(약 34.3MB, 2026-09-25 18:5x 빌드), `dist-aix/aidba-prompts/`(chatbot.md, chatbot-general.md 등)
- [ ] 현재 앱이 정상 동작 중인지 (기준선)

## 1. 앱 jar 교체

```bash
./stop-aix.sh
ps -ef | grep dbagent-java | grep -v grep        # 아무것도 안 나와야 정상
cp -p dbagent-java-0.1.0.jar dbagent-java-0.1.0.jar.bak-$(date +%Y%m%d-%H%M)
cp <반입한 jar> dbagent-java-0.1.0.jar
./start-aix.sh
```

`application.properties`는 수정하지 않아도 된다. 새 키는 없으면 아래 기본값으로 동작한다.

| 키 (선택) | 기본값 | 설명 |
|---|---|---|
| `dbagent.sql-runner.read-only` | `true` | SQL 실행 메뉴 읽기 전용. `false`일 때만 DML/DDL 허용 |
| `dbagent.ui.session-refresh-seconds` | `3` | Current Session 세션 목록 리프레쉬 주기 기본값(1~60) |
| `dbagent.monitor.tm-holder-last-call-et-seconds` | `60` | TM Lock 장애 판정 기준(배포 ①) |

## 2. AI 챗봇 prompt 복사 (sqlrestapi 서버)

sqlrestapi(GPU 서버)의 `prompts/` 디렉터리에 두 파일을 복사한다. **sqlrestapi 재기동 필요 없음**(파일이 바뀌면 다음 질문부터 반영).

```bash
cp aidba-prompts/chatbot.md          <sqlrestapi 경로>/prompts/chatbot.md
cp aidba-prompts/chatbot-general.md  <sqlrestapi 경로>/prompts/chatbot-general.md
# 로컬 테스트박스 기준 경로: /SqltuneRestApi/app/sqlrestapi/prompts/
```

- 기존 `chatbot.md`는 먼저 `chatbot.md.bak-날짜`로 백업해 두면 롤백이 쉽다.
- `current-sql.md`, `sql-writer.md`, `tuning.md`는 변경 없음(폴더에 같이 있는 것은 이력 보관용) — 복사하지 않아도 된다.
- 이 단계를 빼먹어도 앱은 동작한다(일반 질문이 기본 문구로 답변됨). 다만 새 규칙(위험 명령·문서 번호 금지)이 적용되지 않으므로 반드시 복사할 것.

## 3. 기능 확인

- 브라우저 F12 → Network에서 `app.js?v=166`, `style.css?v=46`으로 불러오는지 확인 (AIX). 옛 번호면 Ctrl+F5

| 확인 | 방법 | 기대 결과 |
|---|---|---|
| B1 배율 | Oracle 화면 ↔ RDB 대시보드 ↔ FO 이동 | 세 화면 모두 같은 크기감, 사이드바 폭이 Oracle·RDB에서 같음 |
| B2 세션 목록 | Current Session 하단 Active Session (활성 세션이 8개 이상일 때) | 7행까지 보이고 목록 안에서 세로 스크롤, 머리글 고정 |
| B3 RDB 연결 | MySQL/PostgreSQL/MSSQL 대시보드 → Connections → Client Connections (연결 9개 이상일 때) | 8행 높이에서 스크롤, 연결 20개 넘어도 전부 볼 수 있음 |
| B4 버튼 색 | Oracle 화면 우측 상단 RDB·FO, RDB 화면의 ORACLE·FO | 네 버튼 모두 같은 파란색 |
| B5 리프레쉬 | Current Session 진입 | "리프레쉬 주기(초)" 입력칸 기본 3 |
| B6 읽기 전용 | SQL 실행 메뉴 | 설명줄에 "읽기 전용 · SELECT / WITH만 실행", `SELECT * FROM dual` 실행됨, `DELETE ...`는 거부 메시지 |
| B7 챗봇 | AI DBA 챗봇에 "ORA-01555 원인" / "오라클 인덱스란?" | 앞은 첨부 문서가 붙은 답변, 뒤는 답변 끝에 "AI 일반 지식으로 작성된 답변" 안내 |

## 4. 배포 ①에서 부탁드린 확인 (아직이면)

Current Session 첫 로딩이 느리면 F12 → Console의 `[DBAgent] ash_activity ... 전체 / 서버 처리 / 네트워크·대기` 줄을 전달해 주세요 (배포 ① 절차서 4절).

## 5. 롤백

- 앱: jar만 되돌린다 (DB 파일 변경 없음)
  ```bash
  ./stop-aix.sh
  cp -p dbagent-java-0.1.0.jar.bak-<1절 시각> dbagent-java-0.1.0.jar
  ./start-aix.sh
  ```
- 챗봇 prompt: `chatbot.md.bak-날짜`를 되돌리고 `chatbot-general.md`는 지운다(없어도 오류 없음). 단, 옛 jar로 롤백하면 `chatbot-general.md`는 쓰이지 않는다.

---

### 개발 PC 쪽 배포 기록 (참고)

- 빌드: 두 프로젝트 `mvn clean package` (2026-09-25 18:58), 정적 파일 불일치 0, `.orig` 0
- 배포: main `dist/`, AIX `dist/`, AIX `dist-aix/` — 기존 jar `dbagent-java-0.1.0.jar.bak-20260925-1858`, `dist-aix/aidba-prompts/`에 prompt 동봉
- 스모크 테스트(번들 런타임, main·AIX): 화면 zoom 80%, `/api/config` session_refresh 3·read_only true, SELECT 실행·DELETE 거부, 챗봇 오류/일반 분기, ERROR 0건
- push: main `d0bbdef..bdd7fc0`, AIX `99b6c15..9dd9287`
